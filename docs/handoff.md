# Session 155 handoff (2026-04-23)

**Live-QA fixes for the claude bundled agent shipped as `87255e0` (~1h, one commit).** Three issues from one interactive Meet test plus a follow-up intro-timing diagnosis. (1) Bot was writing essay-length replies because the user's installed `~/.brainchild/agents/claude/config.yaml` had `personality: ''` and `ground_rules: ''` — wiped to empty by a prior `brainchild setup` run that hit Step 4 with empty input; `setup.py::_step4_system_prompt` was unconditionally overwriting both fields. Restored both blocks (matched bundled defaults), added a "Never write tool calls as text" ground-rule, and a "Claude Code is your universal escape hatch" personality paragraph so the model stops fabricating XML for tools it doesn't have (root cause of the visible `<search_web>` text in chat). (2) Wizard fix: `_step4_system_prompt` now branches on non-empty input — empty input preserves the preset's defaults; hint copy updated from "Leave empty to skip" to "Leave empty to keep the preset's defaults" so the language matches the behavior. (3) Bundled config got the same additions so future seeds inherit them. (4) Follow-up QA: `brainchild run claude` (no URL) appeared to skip the intro. Logs showed the intro IS posted in both paths — but Meet hides chat received before you opened the chat panel, and in meet.new mode the bot posts at T+3s while the user is still on Meet's pre-join screen. Fix: `chat_runner._loop` gates intro-post on `saw_others` (≥1 other participant), so bot waits for a human before introducing itself. New `test_intro_waits_for_human_in_empty_room` covers the regression; all 28 chat_runner tests green.

**Exact next step**: live-retest in a real Meet — confirm reply length drops AND confirm the intro lands the moment you join the popped browser in meet.new mode. If both clean, the next obvious lever is the **text-block-before-tool-call wiring** discussed in the QA conversation but not yet implemented (Anthropic returns text + tool_use blocks together in one response; `pipeline/llm.py:295-308` currently keeps only the tool_call and drops the text — fix is to send the text first as a "let me check…" buffer before falling through to confirmation/execute, ~30–45 min, zero added latency). After that, resume the planned **Phase 14.5 `install.sh` (~2h)**, still blocked on the GitHub `operator → brainchild` repo rename.

**Open items / blockers.** (a) **GitHub repo rename `operator → brainchild`** — still deferred, blocks 14.5. (b) **Uncommitted at session end:** `src/brainchild/__main__.py` has user-authored `brainchild edit <target>` and `brainchild where <target>` subcommands — wraps `~/.brainchild/agents/<bot>/config.yaml` (or `.env`) in `$EDITOR` / prints the absolute path. Deserves a separate commit message. Plus four `debug/admit_diagnostic*` HTML/PNG artifacts untracked. (c) The text-block-before-tool-call fix is the highest-leverage UX win still on the table — pre-tool-call ack lands without the user staring at silence. (d) Salesforce + Playwright v1.1 skills still post-MVP. (e) MVP budget envelope unchanged — session 155 was reactive QA work, not roadmap burn. Launch target Apr 28–30 aspirational, May 1–2 acceptable. (f) Git identity: commit `87255e0` carries auto-identity `jojo@jojos-MacBook-Pro.local` — consistent with prior sessions.

---

## Session 154 handoff (2026-04-23)

**Unplanned MCP boot-path hardening shipped (three commits, ~1h).** Session started when `brainchild run claude` crashed with `can't open file 'agents/engineer/claude_code.py'` — a relative-path rot from the src-layout packaging move. Fixed all four bundled agents to invoke the claude-code MCP via `python -m brainchild.agents.engineer.claude_code` (commit `b21d7fc`). Audit surfaced the same bug class in `./github-mcp-server` (all four agents — only engineer enabled by default, latent in the others); fixed by dropping `./` → bare `github-mcp-server` on PATH, preserving the 41-tool Go-binary surface the npm package can't match per session 57's broken-`search_code` finding (commit `130831c`). Added `tests/test_bundled_mcps_bootable_static.py` (commit `ddfb3d7`) — walks every bundled config, every MCP block including `enabled: false`, asserts `command+args` resolve via `shutil.which` / `find_spec` / abs-path-exists, plus a warn-only `@latest` pin check that surfaced 12 warnings matching the 15.7.5 pressure-test punch list. Proven to catch both of today's bugs. Five user-installed config copies under `~/.brainchild/agents/{claude,engineer,pm,designer,testbot}/` also patched (out-of-repo). Symlinked `github-mcp-server` into `~/.local/bin/` so the current install works end-to-end; `install.sh` (14.5) takes ownership of the binary install long-term.

**Exact next step: Phase 14.5 `install.sh` (~2h),** still blocked on the GitHub `operator → brainchild` repo rename for the `uv tool install git+...` URL. Nothing in session 154 changes the 14.5 framing. Worth asking about the rename at session start. Alternative quick-hop if still blocked: 14.7 uninstall docs (~10m, README-only).

**Open items / blockers.** (a) **GitHub repo rename `operator → brainchild`** — still deferred. (b) **Tier 2 dynamic bootability test deferred** — actually spawning each bundled MCP and calling `initialize()`+`list_tools()` needs credential fixtures for auth-required servers; belongs under `docs/deferred-live-tests.md` for now. (c) **Pre-existing 15.7.x `@latest` pins** (calendar/salesforce/slack) still unpinned pending pressure-test — the new warn-only check surfaces them in CI so they stop being silent. (d) 14.4.5 priority-ordered `.env` resolution folds into 14.5. (e) Salesforce + Playwright v1.1 skills still post-MVP. (f) MVP budget: envelope effectively unchanged — session 154 was ~1h of reactive bug work, not roadmap burn. Launch target unchanged: Apr 28–30 aspirational, May 1–2 acceptable. (g) Git identity: commits still landing with auto-identity `jojo@jojos-MacBook-Pro.local` — unchanged from session 153.

**Live-verify before calling this done.** I did not live-test either fix in a real Meet session — the claude_code MCP was only boot-smoked (`python -m brainchild.agents.engineer.claude_code` from `/tmp` blocks cleanly on stdin, no traceback) and github-mcp-server was only PATH-smoked (`github-mcp-server stdio` from `/tmp` exits with a clean "GITHUB_PERSONAL_ACCESS_TOKEN not set"). First thing worth running next session: `brainchild run claude` into a fresh Meet, confirm the `claude-code` MCP comes up without the earlier crash. If engineer gets used, sanity-check the github MCP too — the PATH lookup should Just Work with the symlink I placed, but OAuth/PAT paths haven't been exercised end-to-end since the command rename.

---

## Session 153 handoff (2026-04-23)

**Setup-wizard UX polish shipped as `777add3`.** One commit, ~1h, zero new phase — this was pure polish on top of the 15.5.5/15.11 wizard that user flagged from a real interactive run. Build card went from "expands and contracts with each selection" to absolute-width: `expand=True` + `width=N` locks the panel to exactly N cells, and `build_card.width_for(console)` shrinks the card (clamped to `MIN_WIDTH=28`) on narrow terminals instead of letting Rich clip the right border. Each MCP and skill now renders on its own hang-indented line via shared `_render_list()`; both lists cap at `MAX_LIST_ITEMS=5` with a dim `+N more` overflow marker (regex wraps the marker in `[dim]` AFTER padding so cell-length math stays correct). Lightning bolts recolored blue → magenta. "power-ups" relabelled "MCPs" (same 12-cell padding). Reveal re-ordered: card sits between "All set" and the config-path + run-command lines. Picker got viewport scrolling — `_viewport()` returns a window centered on the cursor, `_render_rows` emits `↑ N more above` / `↓ N more below` dim markers, `max_visible` auto-computes from `console.size.height`; left-column sublabel truncation is now `console.size.width - card_w - 16` floored at 24 so descriptions never wrap to a second line. `_OFFICIAL_MCPS` gained `playwright` / `salesforce` / `sentry` (first-party) while `calendar` (community `@cocal` fork) and `slack` (archived reference server) stay off.

**Claude-preset fixes.** Tagline rewritten to "Claude Code, in your meeting — sandboxed with all your skills and MCP's loaded in" (user dictated the exact copy). Removed the `[dim]editing agents/claude/ in place[/dim]` flash line from `_edit_preset`. `_auto_import_claude_setup` now flips bundled-block `enabled=true` when the user has that MCP configured in Claude Code (previously a no-op on name collision — the bundled scaffold stayed default-off even when the user was using the server). Pre-check fix: only skills with `src != "shared library"` get added to `state.enabled_skill_names`; bundled Brainchild skills stay off by default. Rationale: the claude agent's identity is "your Claude Code setup is the toolkit"; pre-selecting bundled skills (pr-review, release-notes, etc.) dilutes that signal. Mid-session the user noticed the new tagline wasn't appearing in step 1 — rediscovered the session-151 additive `_ensure_user_agents` sync behavior (bundled edits don't reach existing user installs because the seed check is "copy only if dir is missing", protecting user customizations). Resolution used here: user authorized `rm -rf ~/.brainchild/agents/*`, next boot will re-seed from bundled. Flagged as a potential Phase 14.x `--sync-bundled` flag for launch quality, not urgent.

**Prior open items still apply.** Deferred-live-smoke items in `docs/deferred-live-tests.md` unchanged. Salesforce + Playwright v1.1 skills still post-MVP. Launch target unchanged — Apr 28–30 aspirational, May 1–2 acceptable.
