# Session 147 handoff (2026-04-22)

**Docs-only planning session.** Added Phase 15.7 (MCP Hardening + Team Completion) to the pre-launch critical path and resequenced it ahead of 14.5 `install.sh` so the installer seeds the full post-15.7 env-var placeholder set in one pass. Single commit (`b2e7d57`) updates `docs/roadmap.md` + `docs/agent-context.md`. Six sub-phases scoped (15.7.1 error classification foundation, 15.7.2 proactive failure banner, 15.7.3 OAuth decoupling via mcp-remote cache-path check, 15.7.4 wizard credentials status screen, 15.7.5 five new MCPs — slack/salesforce/sentry/playwright/google-calendar, 15.7.6 six new skills — design-handoff-spec/scope-estimate/pr-review/live-bug-triage/release-notes/schedule-followup). Default-enablement rule locked: an MCP is `enabled: true` on an agent iff a bundled skill on that agent requires it — narrows two existing defaults (PM loses github, Designer loses linear; user-owned `~/.brainchild/agents/` insulates existing installs). Salesforce + Playwright ship scaffolded-but-disabled (no bundled skill paired). mcp-remote cache-path verified empirically: `~/.mcp-auth/mcp-remote-<version>/<md5(url)>_tokens.json`.

**Exact next step: Phase 15.7.1 error classification foundation** (~1h). Four touchpoints: (1) `_resolve_env_vars` in `config.py:117` returns `(resolved, missing_vars)` with the list persisted on each server's `MCP_SERVERS` block; (2) `_classify_startup_failure` in `mcp_client.py:58` checks `srv_config["missing_vars"]` first and returns structured `{kind: "missing_creds", vars: [...], fix: "..."}` when a missing var is paired with a startup crash; (3) `record_tool_result` in `mcp_client.py:159` sniffs `401`/`403`/`unauthorized` in tool-error text and tags `disabled_servers` with `kind: "auth_failed"`; (4) `inject_mcp_status` in `llm.py:144` takes the richer structured data and renders per-server reasons into the system prompt block. Unit-test via synthetic failure feeds through each classifier path. This is the data foundation that 15.7.2 banner and 15.7.4 wizard status screen both consume.

**Open items / blockers.** (a) GitHub repo rename `operator → brainchild` still deferred — needed before 14.5's `install.sh` references the repo URL but does not block 15.7 execution. (b) 14.4.5 priority-ordered `.env` resolution (CWD → `~/.brainchild/.env` → shell env) will fold into 14.5 alongside the post-15.7 env-var seeding. (c) Two v1.1 carry-forward questions live in `docs/agent-context.md` Phase 15.7 design notes: non-mcp-remote OAuth cache-path convention (deferred — all five new MCPs are API-key-based) and Salesforce/Playwright companion skills (likely `deal-brief` and `design-qa`). (d) Launch target slipped ~2 days: Apr 28–30 aspirational, May 1–2 acceptable. MVP budget: ~28–30h remaining (17.5h pre-147 + ~9–11h Phase 15.7).
