# Session 151 handoff (2026-04-22)

**Phase 15.9 shipped — `claude` bundled agent (Claude Code in your meeting).** New fourth agent alongside pm/engineer/designer whose defining trait is auto-inheriting the user's existing Claude Code setup. Hard-dependency on the `claude` CLI being installed and logged in — the agent has no identity without it. Landed as five sub-steps in one session, not split across sessions: (15.9.0) `src/brainchild/agents/claude/` scaffold with `config.yaml` + `skills/` placeholder; claude-code MCP enabled by default with beefed-up "built by Brainchild, sandboxed worktree" description + 600s tool timeout; other 9 MCPs scaffolded disabled for auto-import to flip on. (15.9.1) `_ensure_user_agents()` in `__main__.py` changed from "copy bundled only if user dir is empty" to "per-bot sync" so existing users get `claude` on next run without blowing away their agents dir — tradeoff: a deliberately-deleted bundled bot reappears on next boot. (15.9.2) New `pipeline/claude_code_import.py` (~260 lines) with three discovery sources: `~/.claude.json#mcpServers` (locally-configured stdio/HTTP/SSE), `claude mcp list` subprocess output (regex-parsed; authoritative for claude.ai-hosted connectors), `~/.claude/skills/` + `~/.claude/CLAUDE.md`. HTTP/SSE entries auto-wrapped via `mcp-remote@0.1.38` with `auth: oauth` + `auth_url` so the existing 15.7.3 cache-path check + `brainchild auth <name>` flow work unchanged. 32 unit tests in `tests/test_claude_code_import.py` — all green. (15.9.3) Wizard hooks in `pipeline/setup.py`: block selection of `claude` preset at step 1 if CLI not ready (sends back to gallery), `_edit_preset("claude")` triggers `_auto_import_claude_setup(state)` which merges imports into `state.bot_cfg` (dedup by name so user curation sticks), step 2 appends env-var placeholders to `.env`, step 3 surfaces auto-imported skills summary, step 4 offers optional CLAUDE.md → ground_rules append. (15.9.4) `__main__.py::_run_bot` gains two branches when agent is `claude`: hard-fail gate (exit 2 + clean stderr when CLI not ready) and idempotent `_bootstrap_claude_imports()` first-run auto-import (marker `_claude_import_done: true` prevents re-running the ~3s `claude mcp list` shell-out on subsequent boots). End-to-end smoke: throwaway home + bundled claude config = 10 → 14 MCPs after bootstrap, marker set, second call no-op. Hard-fail mock test confirms exit 2 + clean stderr block. (15.9.5) Docs — this handoff, roadmap Phase 15.9 block added with all five sub-steps marked ✅, deferred-live-tests.md has 4 new entries (wizard pick, CLI first-run, hard-fail, `claude mcp list` format-drift canary), CLAUDE.md updated with an explicit "the `claude` agent is different" paragraph. Regression: 80+ tests green across 4 adjacent suites (claude_code_import/1574_readiness/chat_hardening/mcp_client). Changes uncommitted — stage + commit with the 15.9 work.

**Key implementation finding.** `claude mcp list` is the authoritative source for claude.ai-hosted connectors (Gmail/Drive/Calendar/Linear), **not** `~/.claude.json#mcpServers`. Most Claude Code power users configure MCPs via claude.ai account connectors rather than local JSON edits, so `~/.claude.json#mcpServers` is empty for them. Parsing `claude mcp list` text output (10s timeout, non-match skip, degrades to empty on format drift or non-zero exit) is the only way to reach those MCPs. Regex lives at `claude_code_import.py::_CLAUDE_MCP_LIST_RE`; if Claude Code changes the output format, imports silently return zero hosted MCPs — Phase 17 upstream-drift watch should monitor this.

**Exact next step: Phase 14.5 `install.sh` (~2h),** still blocked on the GitHub `operator → brainchild` repo rename. Nothing in 15.9 changes 14.5 framing — the installer will seed `~/.brainchild/.env` with the full post-15.7.5 env-var set + now land the four-agent bundled roster (`pm / engineer / designer / claude`) on user disk. Worth asking about the repo rename at session start. **Alternative quick-hop if 14.5 stays blocked**: 14.7 uninstall docs (~10m, README-only per the in-roadmap revision).

**Open items / blockers.** (a) **GitHub repo rename `operator → brainchild`** — still deferred, needed for 14.5's install URL. (b) Deferred-live-smoke items in `docs/deferred-live-tests.md`: 15.7.2 banner, 15.7.3 OAuth round-trip, 15.7.4/15.7.4.5 wizard + runtime pre-flight UX, 15.7.5 per-MCP pressure-tests, session 131+132 carry-forward — and now **4 new 15.9 entries** (wizard pick against a live Claude Code install, CLI first-run with real `~/.claude.json`, hard-fail on a machine without `claude` CLI, `claude mcp list` format-drift canary). (c) 14.4.5 priority-ordered `.env` resolution folds into 14.5. (d) Salesforce + Playwright v1.1 skills (`deal-brief`, `design-qa`) still post-MVP. (e) **Known limitation in 15.9:** PyYAML round-trip drops comments from `config.yaml` on first-run auto-import write (matches existing wizard behavior at `_step7_write`). Users who want to edit the file afterward should use `brainchild setup` or edit directly — the comments-as-doc value diminishes post-customization anyway. (f) **Rejected in-session:** five translated slash-command skills (plan / meeting-recap / security-review / file-bug / code-cleanup) from the session-151 `/` audit — user deferred post-launch per "ship the agent, skills later." Audit findings captured mid-session in chat; not persisted to a doc because scope was deliberately cut. If resurrected as Phase 15.9.6, the five-skill pack is ~2h. (g) MVP budget: ~19.75–21.75h remaining after burning ~4h on 15.9 in this session. Launch target unchanged: Apr 28–30 aspirational, May 1–2 acceptable. (h) Git identity: commits still landing with auto-identity `jojo@jojos-MacBook-Pro.local` — unchanged from session 150.

---

## Session 150 handoff (2026-04-22)

**Phase 15.8 CLI verb rename shipped.** Commit `892419e` applies 21 documented renames atomically across 7 audited files (`__main__.py`, `README.md`, `CLAUDE.md`, `agents/README.md`, pm/engineer/designer READMEs, `pipeline/setup.py`, `config.py`) plus 4 test updates the 15.8.0 audit missed — a string-literal grep doesn't see `patched_argv(["pm", "<url>"])` as exercising the bare form, so `test_main_known_bot_dispatches_to_run_bot` + `test_main_unknown_bot_returns_2` slipped through. Fixed in-commit: renamed the first to `test_main_run_known_bot_dispatches_to_run_bot`, added three new tests for the new dispatcher branches (bare-known-bot pointed hint, `run` with no name, `run` with unknown bot). 26/26 `test_entry_cli.py` pass. Dispatcher shape: `brainchild run <name> [url]` is canonical; `brainchild run` with no name prints usage + exit 2; `brainchild <known-bot>` bare-form prints a pointed hint (``Run agents via `brainchild run pm`. Bare `brainchild pm` is no longer supported.``) + exit 2; truly-unknown first arg stays with the generic "Unknown bot or subcommand" message. No dual-accept. `_print_usage` collapsed to one `brainchild run <name> [url]` line with "auto-opens one if no url" inline. Smoke-verified via `python -m brainchild` + `python -m brainchild pm`. Burned ~1h of the 1.5h budget (~30m shaved).

**Exact next step: Phase 14.5 `install.sh` (~2h).** Execution order `15.7 → 15.8 → 14.5 → 14.6 → 14.7 → 14.8 → 15.1 → 16`; 15.7 + 15.8 now closed (15.7.5 shipped async in commit `5453080`, 15.7.6 in `4986743`, 15.8 just now). 14.5 blocks on the **GitHub repo rename `operator → brainchild`** — the `uv tool install git+https://github.com/.../brainchild.git` URL needs a canonical location. Carried forward from sessions 146+; ask about the rename at session start. 14.5 scope unchanged from roadmap row: detect macOS/Linux + Python ≥3.10, bootstrap `uv` if missing (Astral installer), `uv tool install git+...`, `playwright install chromium` (170MB download), seed `~/.brainchild/.env` with commented API-key placeholders (now including the full post-15.7 env set — slack/sentry/salesforce/playwright/google-calendar added by 15.7.5), verify `brainchild` on PATH, print "what's next" pointing at `brainchild setup`. The 14.4.5 priority-ordered `.env` resolution side-commit (CWD → `~/.brainchild/.env` → shell env) folds into 14.5. **Alternative quick-hop if 14.5 stays blocked on repo rename**: 14.7 uninstall docs (~10m per the in-roadmap revision) — three README lines covering `uv tool uninstall brainchild` + `rm -rf ~/.brainchild` + PATH-symlink cleanup, no dedicated subcommand.

**Open items / blockers.** (a) **GitHub repo rename `operator → brainchild`** — still deferred, needed for 14.5's install URL. (b) Deferred-live-smoke items in `docs/deferred-live-tests.md`: 15.7.2 banner, 15.7.3 OAuth round-trip, 15.7.4/15.7.4.5 wizard + runtime pre-flight UX, 15.7.5 per-MCP pressure-tests (one entry each for calendar / playwright / salesforce / sentry / slack — covers first-run auth + tool-list verification + pin-pending for three unpinned `@latest` entries), session 131+132 intro-on-join + personality/ground-rules split carry-forward. (c) 14.4.5 priority-ordered `.env` resolution folds into 14.5. (d) Salesforce + Playwright v1.1 skills (`deal-brief`, `design-qa`) still post-MVP. (e) MVP budget: ~23.75–25.75h remaining after session 149 async (burned ~2h on 15.7.5 + 15.7.6, within the 4–6h envelope) + session 150 (burned ~1h on 15.8.1). Launch target unchanged: Apr 28–30 aspirational, May 1–2 acceptable. (f) One minor meta-note: commit `892419e` landed with git's auto-identity warning (`jojo@jojos-MacBook-Pro.local`); `git config --global user.*` untouched this session to stay consistent with prior commits — `5453080` + `4986743` also carry the same auto-identity. Flag if you want it normalized.

---

## Session 149 async addendum (2026-04-22)

**Phase 15.7.5 + 15.7.6 shipped.** Two commits landed during the session 150 CLI-verb-rename window, both YAML/markdown-only, no Python changes.

- **`5453080` — 15.7.5 new-MCP scaffolding.** Five blocks (`calendar` / `playwright` / `salesforce` / `sentry` / `slack`) added to all three bundled agent configs, bringing the full-union scaffold to 10 MCPs per agent. Enablement matrix matches session-147's "MCP enabled iff a bundled skill requires it" rule: **PM** flipped github true→false, added calendar enabled:true (ships with linear). **Engineer** added sentry enabled:true (keeps claude-code + github). **Designer** flipped linear true→false (keeps only figma). **slack / salesforce / playwright** scaffolded `enabled: false` everywhere. Three entries use `@latest` with a "pin after pressure-testing" inline comment rather than fake a version (calendar / salesforce / slack); playwright@0.0.70 + mcp-remote@0.1.38 are real pinned. **Salesforce readiness UX gap**: `env: {}` + `auth: env` means `report_mcp_readiness` reports ✓ even when `sf` CLI isn't logged in — flagged in deferred-live-tests for a post-launch prereq-probe follow-up. Pressure-test matrix captured in `docs/deferred-live-tests.md` (one entry per MCP). User installs at `~/.brainchild/agents/` intentionally unchanged (session-147 insulation promise).

- **`4986743` — 15.7.6 six new skills.** `design-handoff-spec` (Designer), `scope-estimate` / `pr-review` / `release-notes` / `live-bug-triage` (Engineer), `schedule-followup` (PM). Every SKILL.md follows the existing frontmatter + Shape + Rules pattern; descriptions lead with trigger phrases. Engineer had no bundled skills dir before this commit — created `agents/engineer/skills/` and wired `agents/engineer/skills` under `skills.paths` in engineer/config.yaml. All nine bundled skills now load cleanly via the real `pipeline/skills.py:load_skills()` path (PM 3, Engineer 4, Designer 2). Three skills (pr-review, release-notes, scope-estimate) are pressure-testable today; the other three depend on 15.7.5 MCPs and light up for users who auth them.

**Exact next step for the next session: still Phase 14.5 `install.sh` (~2h),** blocked on the GitHub `operator → brainchild` repo rename. Nothing in this addendum changes the main handoff's 14.5 framing. Worth asking about the repo rename at session start — it's the only thing standing between now and `curl | sh` being live.
