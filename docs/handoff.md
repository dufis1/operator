# Session 142 handoff (2026-04-21)

**Phase 14.3 closed in full this session** — the src-layout package restructure is done and the tree is ready to consume a `pyproject.toml` in Phase 14.4. Six commits landed on `main` today: (1) DNS async for 14.6 (`f299f81`) — `brainchild.build` onboarded to Cloudflare Free plan, Porkbun DNS imported, nameservers migrated Porkbun → Cloudflare, propagation <15min, Pages wire-up deliberately deferred into 14.5 so it's one clean git-connected project instead of a throwaway upload. (2) **14.3 Commit 1 of 3** (`7be6fd4`) — src-layout moves: `git mv __main__.py config.py pipeline/ connectors/` into `src/brainchild/`, absolute imports rewritten across 53 files (`from pipeline.x` → `from brainchild.pipeline.x`, etc.), path anchors updated, tests' `sys.path.insert` switched to `src/`, launcher now `python -m brainchild` with `PYTHONPATH` prepend. (3) **14.3 Commit 2 of 3** (`3504302`) — `git mv agents/` → `src/brainchild/templates/agents/` (17 files, pure rename); `_AGENTS_DIR` switched to `Path.home() / ".brainchild" / "agents"` in `__main__.py`, `config.py`, `pipeline/setup.py`; added `_TEMPLATES_DIR` + `_ensure_user_agents()` first-run hook that copies all three bundled templates to the user dir when it's missing or empty; `setup.py`'s `_PM_CONFIG` now points at the bundled template (immutable schema reference). (4) Session-142 doc sync for Commits 1+2 (`02b1938`, `4d5b257`). (5) **14.3 Commit 3 of 3** (`731d662`) — `main()` already existed at `__main__.py:289` with clean import-time semantics (no top-level `brainchild.*` subpackage imports; all pipeline imports are lazy inside functions). The `subprocess.Popen.__init__` monkeypatch at lines 57–65 runs at import time and IS load-bearing (puts Chrome/Playwright children in their own session so Ctrl+C doesn't SIGINT them — without it, Chrome survives ~60s in-meeting until Meet's heartbeat times out); comment block at lines 47–56 already documents this. Commit adds one smoke test (`test_package_import_exposes_main`) asserting `import brainchild.__main__` works and exposes `main` as a zero-arg callable — the exact contract Phase 14.4's `[project.scripts] brainchild = "brainchild.__main__:main"` will consume. 22 test scripts, 21 pass (`test_caption_late_bind` unchanged pre-existing failure). (6) This handoff note.

**Exact next step:** **Phase 14.4 — `pyproject.toml` + console script entry.** ~45min. Scope: (a) write `pyproject.toml` at repo root with `[build-system] requires = ["hatchling"]` + `build-backend = "hatchling.build"` (uv's default, no separate setup.py); (b) `[project]` metadata: `name = "brainchild"`, `version = "0.1.0"`, `description`, `license = { text = "MIT" }`, `requires-python = ">=3.10"`, `authors = [...]`, `readme = "README.md"`; (c) `dependencies = [...]` sourced from `requirements.txt` — pinned exact versions for the 7 direct deps, the 4 `>=` security floors stay as `>=`; (d) `[project.scripts] brainchild = "brainchild.__main__:main"`; (e) `[tool.hatch.build.targets.wheel] packages = ["src/brainchild"]` so hatchling finds the src-layout package; (f) include `src/brainchild/templates/**` in the wheel (templates must ship inside the package for the auto-copy hook to work); (g) verify locally with `uv tool install .` (or `pip install .` in a throwaway venv) → `brainchild list` must auto-copy templates and list three agents. Once the console script works, the `brainchild` bash launcher at repo root is deprecated — the Phase 14.5 `install.sh` will rely on the pip-installed console script, not the launcher.

**Open items / blockers.** None hard-blocking. Soft items: (a) **GitHub repo rename `operator → brainchild`** still deferred per user call at session 141 start — needed before Phase 14.5 `install.sh` references the repo URL, so before this week's end. (b) **Cloudflare Pages project creation** still to do — rolled into Phase 14.5 execution (~30min inside 14.5's ~2h budget). (c) **`test_caption_late_bind.py`** was already broken pre-restructure with "BRAINCHILD_BOT unset" at import time — not a regression, low priority (adapter-only test, not in the CLAUDE.md canonical test list). (d) **Launch target:** Apr 25–26 aspirational / Apr 27–28 acceptable slip — on track; 14.3 took ~4.25h across three commits (matched estimate), leaving ~7.5h for 14.4–14.8 plus 15.1 Linux + 16 launch, totaling ~22h MVP budget remaining.
