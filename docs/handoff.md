# Session 158 handoff (2026-04-23)

**Shipped a path-resolution audit + regression guards as three commits (`4777a66`, `1b6a74d`, `7fff5c1`), ~3h total.** Session opened with a user question about adding a Google-signin step to the setup wizard — discussion surfaced that the Chrome profile lived at the repo root via a `_BASE` walk-up (`os.path.dirname(...×4... __file__...)`), which breaks for installed/site-packages use. User asked to resolve the prereq first rather than defer. Commit 1 moved `browser_profile/` and `auth_state.json` to `~/.brainchild/` via absolute `config` constants + a one-shot migration shim in `__main__.py`; user's 1.1 GB profile relocated cleanly and Google session survived the live-QA run.

**Commit 2 closed three more bugs the audit surfaced.** Explore-agent swept the package and found: `setup.py` wrote wizard-collected API keys to `<repo>/.env` while `__main__._bootstrap_claude_imports` and `brainchild edit env` targeted `~/.brainchild/.env` (latent divergence — silently wrong-file for any code path that took the home-anchored read); `session.save_debug` dumped to `<repo>/debug/` via the same `_BASE` pattern; adapter chat-button-failure screenshots used CWD-relative `"debug/..."`. Fix added `config.ENV_FILE` + `config.DEBUG_DIR`, gave `load_dotenv()` an explicit absolute path, extended the migration shim to cover `.env` (renamed `_migrate_legacy_browser_artifacts` → `_migrate_legacy_user_artifacts`), and routed session.py + both adapters through `config.DEBUG_DIR`. User's live `.env` migrated (1064 bytes preserved, API keys now load from the new location).

**Commit 3 added regression guards so the class-of-bug can't recur silently.** User asked why unit tests didn't catch any of it — answer was that existing tests checked logic, not filesystem invariants. `tests/test_path_resolution.py` extended from 5 → 9 tests. Layer 1: `test_all_config_paths_rooted_under_home_or_tmp` enumerates every `*_DIR`/`*_FILE`/`*_PATH` attr on config, asserts absolute + under HOME, and fails when a new path attr isn't in the allow-list (caught `BOT_DIR` during dev as designed). Layer 2: `test_env_file_canonical_across_modules` asserts byte-exact agreement between `config.ENV_FILE`, `setup._ENV_FILE`, and `_resolve_config_target('env')` — the exact session-158 divergence can't drift again. Companion `test_debug_dir_canonical_across_modules` verifies session.py + both adapters reach `mod.config is config`. Full sweep green across config_loader, connectors, entry_cli, chat_hardening, mcp_client, 1571/1574/15745, claude_code_import. Roadmap got a new Phase 14.10 entry for the wizard Google-signin step (~2h, ⬜) — the path relocation is its explicit prereq.

**Exact next step: Phase 14.10 — wizard Google-signin step (~2h).** Plan already drafted in the roadmap entry: slot as the first wizard step (before fighter-select); detect-or-launch shape (probe `~/.brainchild/browser_profile/` for a valid `.google.com` SID cookie via `session.validate_auth_state` and skip with `✓ signed in` if present, else launch a visible Chrome to `accounts.google.com` and poll for completion); save `storage_state()` to `config.AUTH_STATE_FILE` for Linux/Docker recovery; skippable with a visible warning; shared profile across all bots. Tests: mocked-Playwright detection + skip path; live sandbox test for the full sign-in dance.

**Open items / blockers.** (a) GitHub repo rename `operator → brainchild` still deferred; blocks Phase 14.5 `install.sh`. (b) Session 155's Issue 3 — text-block-before-tool-call wiring in `pipeline/llm.py:295-308` (~30–45m) — still open as a chat-feel polish task. (c) Debug scratch files (`debug/admit_*`, `debug/post_admit_*`) plus new `admit_keyboard_fail.{html,png}` that appeared this session all still untracked since session 151 — not cleaned up. (d) Git identity: all three session-158 commits carry auto-identity `jojo@jojos-MacBook-Pro.local` — consistent with sessions 153–157, flag if you want it normalized. (e) MVP budget: ~13.5–15.5h remaining (session 158 burned ~3h). Launch target unchanged — Apr 28–30 aspirational, May 1–2 acceptable.
