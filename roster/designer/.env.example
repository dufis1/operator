ANTHROPIC_API_KEY=
# Figma personal access token — create at figma.com/settings → "Personal access tokens".
# Scope "File content: Read" is sufficient for the default (GLips) server, which is read-only.
FIGMA_TOKEN=
