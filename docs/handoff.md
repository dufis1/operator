# Session 144 handoff (2026-04-21)

**This was a small clean-up session.** Audited `requirements.txt` + `pyproject.toml` against a clean-Python-3.14-venv install of the 7 pinned direct deps and confirmed which of the 4 `>=` CVE floors are actually pulled transitively. Three are real (`cryptography` via `mcp → pyjwt[crypto]`, `pygments` via rich, `python-multipart` via mcp); `requests>=2.33.0` was phantom — the session-138 `pip-audit` flag had come from residual voice-pipeline packages (`elevenlabs`, `spacy`, `google-api-core`) + dev tools (`pip-audit`, `twine`) polluting the audit venv, none of which ship to end users. One commit landed on `main` (`e1e1c0d`): dropped `requests>=2.33.0` from both files, corrected the stale "via google-auth" comment on `cryptography` to `mcp → pyjwt[crypto]`. Re-verified by rebuilding the wheel from the updated `pyproject.toml`, installing into a second clean venv, and confirming `requests` absent + `cryptography` present + `brainchild list` works (first-run template auto-copy triggers, three bundled agents listed). All 9 canonical test files in CLAUDE.md pass.

**Exact next step: Phase 14.5 — `install.sh`** (~2h). Bash install script at repo root. Scope: (1) detect macOS/Linux + Python ≥3.10; (2) bootstrap `uv` if missing via `curl -LsSf https://astral.sh/uv/install.sh | sh`; (3) `uv tool install git+https://github.com/.../brainchild.git` (blocked on GitHub repo rename `operator → brainchild`); (4) `playwright install chromium` (~170MB browser download); (5) seed `~/.brainchild/.env` with commented API-key placeholders; (6) verify `brainchild` on PATH; (7) print a "what's next" pointing at `brainchild setup`.

**Open items / blockers.** (a) **GitHub repo rename `operator → brainchild`** still deferred from session 141 — must happen before 14.5's `install.sh` references the repo URL. Not hard-blocking today since the script can be drafted against the future URL, but the first real smoke needs the rename done. (b) **Priority-ordered `.env` resolution** (CWD → `~/.brainchild/.env` → shell env) still wants to ship as a 14.4.5 side-commit *before* 14.5 so the installer can seed `~/.brainchild/.env` and trust it gets read post-install. (c) **Cloudflare Pages project creation** still folded into 14.5 execution (~30min inside 14.5's ~2h budget). (d) **Launch target:** Apr 25–26 aspirational, Apr 27–28 acceptable slip — on track; ~19h MVP budget remaining (14.5 + 14.6 Pages + 14.7 uninstall + 14.8 clean-mac smoke + 15.1 Linux + 16 launch).
