# Session 143 handoff (2026-04-21)

**Phase 14.4 closed in full this session, plus same-session cleanup.** Five commits on `main`: `4cd7a21` (fix `test_caption_late_bind` to run standalone — pre-existing `BRAINCHILD_BOT` import-time issue, not a regression from 14.3), `dd7e416` (Phase 14.4 `pyproject.toml` + hatchling build backend + `[project.scripts] brainchild = "brainchild.__main__:main"` — verified end-to-end via `python -m build` → throwaway `pip install` into fresh Python 3.14 venv → `brainchild list` with `~/.brainchild/agents/` moved aside → first-run auto-copy populates the user dir from bundled templates), `e7f5240` (removed stale build artifacts: root `connectors/`, root `__pycache__/`, `build/`, `dist/`, `.eggs/`, plus 6 tracked HTML+PNG auto-admit debug snapshots from sessions 113–116; kept `debug/flaky_mcp_server.py` and `test/` which are active fixtures), and `3d8582a` + `3e4f23e` (collapsed `src/brainchild/templates/agents/` → `src/brainchild/agents/` since `templates/` only ever held `agents/`; renamed `_TEMPLATES_DIR` → `_BUNDLED_AGENTS_DIR` for clarity; fixup commit `3e4f23e` wired up code + doc edits that I accidentally left unstaged when the original commit only captured the git renames). All 10 canonical tests green post-fixup. Two design decisions locked to memory this session: **hatchling** as build backend over setuptools (PyPA's current recommendation, what `uv` assumes); **priority-ordered `.env` lookup** for installed CLI (CWD → `~/.brainchild/.env` → shell env) rejected XDG in favor of consistency with the existing `~/.brainchild/` anchor. Docs synced at close (roadmap status line + 14.4 row flipped ✅; agent-context session 143 entry added + `git commit` without `-a` footgun appended to Hard Won Knowledge).

**Exact next step:** **Phase 14.5 `install.sh`** (~2h). Scope: (1) detect macOS/Linux + Python ≥3.10; (2) bootstrap `uv` if missing via Astral's installer (`curl -LsSf https://astral.sh/uv/install.sh | sh`); (3) `uv tool install git+https://github.com/.../brainchild.git` (depends on the GitHub repo rename `operator → brainchild` happening first); (4) run `playwright install chromium` (~170MB browser download); (5) seed `~/.brainchild/.env` with commented API-key placeholders; (6) verify `brainchild` on PATH; (7) print a "what's next" pointing at `brainchild setup`. **Strong recommendation: do a 14.4.5 side-commit before starting 14.5** — rewrite `load_dotenv()` in `src/brainchild/config.py` and `_ENV_FILE` in `src/brainchild/pipeline/setup.py` to use the priority-ordered resolution (CWD → `~/.brainchild/.env` → shell env) decided this session. That way `install.sh` can assume the seeded target location is actually where the config loader looks, and we avoid a scope drag inside 14.5 itself. Roll the Pages wire-up for 14.6 (~30min) into 14.5 execution as planned session 142. After 14.5: 14.6 Pages wire-up + domain activation, 14.7 uninstall path, 14.8 clean-mac user-flow smoke (absorbs the 5-sessions-overdue live-smoke of 131+132+136+137).

**Open items / blockers.** (a) **GitHub repo rename `operator → brainchild`** must happen before 14.5 because `install.sh` will embed the git URL. Deferred since session 141; budget ~15min whenever you kick it off. (b) **Cloudflare Pages project creation** still to do — rolled into 14.5 execution (~30min). (c) **`.env` priority-ordered resolution** — strongly prefer as a 14.4.5 side-commit before 14.5; touches `config.py:7` (swap bare `load_dotenv()` for priority-searched path) and `pipeline/setup.py:63` (swap `_ENV_FILE = _ROOT / ".env"` for same priority search). ~20min. (d) **Launch target** Apr 25–26 aspirational / Apr 27–28 acceptable slip — on track; session 143 shaved ~1.25h (14.4 took ~45m as budgeted, plus ~30m of cleanup that wasn't budgeted but was cheap); ~19.5h remaining across 14.4.5 + 14.5–14.8 + 15.1 Linux + 16 launch. (e) **Hard-won-knowledge entry added** about `git commit` without `-a` dropping unstaged edits after `git mv` — worth a read before the next refactor that mixes renames with file edits.
