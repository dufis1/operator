ANTHROPIC_API_KEY=
# FIGMA_TOKEN is not required — the official Figma MCP authenticates via OAuth
# in your browser on first run (mcp-remote caches the token). If you ever swap
# in the community figma-developer-mcp instead, set FIGMA_API_KEY here.
