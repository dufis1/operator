# Session 148 handoff (2026-04-22)

**Phase 15.7 MCP hardening trio shipped.** Three commits (`6beff98` 15.7.1, `43dae3f` 15.7.2, `ed63026` 15.7.3) in ~5h of the ~9–11h Phase 15.7 envelope. 15.7.1 structured error classification — `_resolve_env_vars` returns `(resolved, missing_vars)`, `_classify_startup_failure` returns `{kind, fix, raw, [vars]}` with `missing_creds` precedence, renamed `failed_servers`/`disabled_servers` → `startup_failures`/`runtime_failures` (both `dict[str, dict]`) to force AttributeError on stale consumers instead of stringified-dict log spam. Auth-error sniff in `record_tool_result` upgrades tripped servers to `kind="auth_failed"` when substring matches (`_AUTH_ERROR_PATTERNS` grounded in empirical captures + documented Slack/Sentry/Salesforce/Google patterns). 15.7.2 `ChatRunner._post_mcp_failure_banner` posts one compact line at join before the async intro so it lands deterministically; silent on the happy path. 15.7.3 new `auth: env|oauth` + `auth_url` fields; `_oauth_cache_exists` checks `~/.mcp-auth/mcp-remote-*/<md5(auth_url)>_tokens.json` (lex-latest version dir); `connect_all` pre-checks before spawning oauth servers; new `brainchild auth <mcp>` CLI subcommand foreground-spawns mcp-remote, polls for tokens.json, SIGTERMs on success. Bundled Linear configs on all 3 agents got `auth: oauth` + `auth_url: https://mcp.linear.app/mcp`; user's `~/.brainchild/agents/` intentionally not modified (session 147 insulation promise). 29 new unit tests across `test_1571_error_classification.py` (13), `test_1573_oauth_cache_check.py` (10), entry CLI (3), banner (6), plus updates to existing classifier tests. Empirical auth-capture script archived at `tests/probe_auth_errors.py` + findings at `docs/mcp-auth-errors.md` for 15.7.5 MCP additions. New `docs/deferred-live-tests.md` tracks pressure-tests that need real Meet/browser (15.7.2 banner + 15.7.3 OAuth round-trip).

**Exact next step: Phase 15.7.4 — wizard credentials status screen + claude-code prereq check (~2h).** Touchpoints: (1) add `credentials_url` field to every bundled MCP block; (2) new `_report_mcp_readiness(mcp_servers) → {name: {status: "ok|missing_env|oauth_needed|prereq_missing", fix_url, detail}}` helper (probable home: new `src/brainchild/pipeline/readiness.py`, or extend `mcp_client.py`) combining env-var checks (15.7.1's `missing_vars`) + oauth cache-path checks (15.7.3's `_oauth_cache_exists`) + claude-code-specific prereqs (`shutil.which("git")` + `shutil.which("claude")` + a claude-is-authed probe, only when user toggles claude-code on; skip cleanly if user defers); (3) `pipeline/setup.py` renders `✓/⚠/✗` status screen at end of MCP step; (4) optional inline "authorize Linear now? y/N" that invokes the same code path as `brainchild auth <mcp>` — requires pulling `_run_auth` from `__main__.py` into a plain function the wizard can call directly. After 15.7.4: **Phase 15.7.4.5 runtime pre-flight** (~45m, session 148 split-out) — same helper runs in `_run_bot()` before browser spin-up so hand-edited configs that enable OAuth MCPs outside the wizard don't have to wait for the mid-meeting banner; interactive prompt offers inline auth + "Continue without X?" for missing env. `--no-preflight` escape hatch keeps CI/scripted launches unchanged.

**Open items / blockers.** (a) Three deferred-live-smoke items on the tracker — 15.7.2 banner, 15.7.3 OAuth round-trip, and session 131+132 carry-forward (intro-on-join + personality/ground-rules split). (b) GitHub repo rename `operator → brainchild` still deferred; 14.5 `install.sh` is the first thing that actually needs the new URL. (c) 14.4.5 priority-ordered `.env` resolution still folds into 14.5. (d) Salesforce + Playwright still v1.1 questions for companion skills (`deal-brief`, `design-qa`). (e) Launch target unchanged: Apr 28–30 aspirational, May 1–2 acceptable. MVP budget: ~23–26h remaining (~6–8h for 15.7.4/4.5/5/6 + ~2h for 14.5 install.sh + 14.6/7/8/15.1/16).
