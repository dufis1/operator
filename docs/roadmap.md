# Brainchild — Roadmap

*Last updated: April 25, 2026 (session 164)*

> **Current status: session 164 shipped six commits hardening the live-test UX from Test 1.2.** (`1413318`) confirmation-decline UX fix — `chat_runner._handle_confirmation` now sends an unambiguous `[TOOL NOT EXECUTED]`-prefixed synthetic tool_result when the user replies non-yes during a pending confirm, so the LLM stops hallucinating "running, waiting for results" on a tool that never actually fired. Also added TTFT + first_flush instrumentation to `anthropic.py:complete_streaming` TIMING line — paid for itself the same session by definitively pinning a 50.3s call as `ttft=48.3s` (Anthropic-side request-queue spike, not our code). (`d0ff5a7`) stream-json side-log + live worktree progress — switched the inner claude CLI from `--output-format json` to `--output-format stream-json --verbose`, `_run_claude` reads stdout line-by-line and appends events to `~/.brainchild/debug/claude-stream.jsonl` (truncated per call), `chat_runner` spawns a tailer thread for `claude-code__delegate_to_claude_code` calls that posts real inner-tool_use events ("Reading config.py…", "Editing chat_runner.py…", "Running: pytest tests/test_x.py…") instead of cute placeholder verbs. Closes the demo gap where a 5-minute delegate looked like the bot was hung. (`20a6d58`) drop verb heartbeat infrastructure — deleted `pipeline/heartbeat_verbs.py` + `TOOL_HEARTBEAT_SECONDS`/`TOOL_HEARTBEAT_MAX_SECONDS` from config; `_execute_and_respond` collapsed to a `done_event.wait()` since stream-json covers claude-code progress and short MCPs (linear/github) are quick enough that placeholder pings added more noise than signal. Tests in `test_912_tool_timeout.py` rewritten to assert silent execution. (`b1b5058`) stuck-LLM watchdog with one-shot status ping — `LLM_STUCK_THRESHOLD_SECONDS=45` in config; watchdog Timer in `complete_streaming` posts `"Anthropic is taking longer than usual to respond — hang tight."` exactly once if no token has arrived by 45s. Cancel in try/finally so it doesn't fire stale on a failed call. Retry semantics deferred (would require chat-reader/dispatcher concurrency refactor; informational ping is the minimum viable). (`5af360c`) drop trailing newlines in stuck-LLM watchdog message — live retest caught a self-reply cascade: bot sent `"...hang tight.\n\n"`, Meet's DOM stripped trailing newlines on read-back, exact-text own-message dedup mismatched, bot reprocessed its own watchdog as a user prompt and the LLM looped on it. Trailing `\n\n` removed. (`1e7e3e1`) ID-based own-message dedup + stripped text-match fallback — the underlying brittleness was that `chat_runner._send` told the connector to send but never tracked the resulting `data-message-id`, so dedup relied on exact-text comparison that any DOM normalization could break. Connector contract updated: `send_chat` now returns the new `data-message-id` (macos_adapter snapshots existing IDs pre-send, polls every 50ms up to 1s for a new one to appear). ChatRunner adds the returned ID to `_seen_ids`, short-circuiting at the existing line-341 ID check. Text-match fallback hardened: `_own_messages` now stores `text.strip()`, comparison strips both sides — catches the linux-adapter (still returns None) path and the race window where the post-send DOM read times out. **Exact next step: re-run Test 1.2 (`migration-plan`) cleanly with the new dedup + stream-json + watchdog stack live; if it passes, move to Test 1.3 (`test-generation`), 1.4 (live coding delegation), Sessions 2 and 3, then Phase 15.10.5 (`brainchild worktrees` CLI — added this session as a deferred follow-up to ship before Phase 14.5 install.sh).** **Launch target: Apr 28–30 aspirational, May 1–2 acceptable.** MVP budget: ~5–7h remaining (session 164 burned ~2.5h on six commits + diagnosis of the cascade bug).

> **Prior status: session 163 shipped the headless bundled-Chromium pivot (`273b4b1`) + heartbeat fixed-cadence (`0d6f0e1`).** During Test 1.2 (`migration-plan` skill) the user instinctively quit Chrome to open a new browsing window — discovered macOS treats `Chrome.app` as a singleton: brainchild was launching the user's installed Chrome with `--user-data-dir` + `--headless=new`, but the `headless=False` Playwright flag won (so it ran *headed*), and the shared bundle ID meant the user's dock click + ⌘N got eaten by brainchild's process. Quitting Chrome killed the bot's tab silently and `chat_runner._send` blindly enqueued LLM paragraphs against a dead browser thread for ~70s with empty `queue.Empty` errors. Five self-contained probes in `debug/headless_meet_*.py` validated the pivot end-to-end against a real Meet URL with `heyoperator2026@gmail.com` invited: unauthed walls headless + headed equally (so the long-standing "Meet blocks headless" lore was actually "Meet blocks unauthed/uninvited"); auth + invite + headless → admitted normally; chat read (DOM observer), chat send (textarea fill + Enter), and caption observer (Shift+C + `CAPTION_OBSERVER_JS` + `__onCaption` bridge) all work in headless bundled Chromium. Production join needed a small modal-dismiss patch — a camera-only "Do you want people to see you in the meeting?" dialog (no decline link) was blocking Join now; new pre-camera-toggle step prefers the existing "Continue without microphone and camera" link, falls back to a single Escape keypress. Live `brainchild run claude` against the patched code joined a fresh Meet in 9.2s (vs ~13s headed), captions enabled, intro posted, real chat exchange + `linear__list_issues` MCP tool call posted formatted output end-to-end. Heartbeat cadence flipped from exponential (8→16→32→60s) to fixed 30s — stretching gaps read as "the bot hung" once long enough; steady pings read as "still alive." `tests/test_912_tool_timeout.py` updated (`test_heartbeat_interval_backs_off → test_heartbeat_interval_is_fixed_cadence`); test_config_loader assertion bumped to 30. **Stale memory entry `project_meet_bot_detection.md`** is now wrong (Meet does NOT block headless when auth + invite present) — flag for next-session update. **Session-160 Chrome-runtime preflight code is now dead weight** — bundled Chromium ships with Playwright, no real-Chrome detection needed; safe to retire in a follow-up cleanup commit. **Exact next step: re-run Test 1.2 (`migration-plan`)** in `docs/live-skill-tests.md` against the new headless build (the original test 1.2 hung last session because of the Chrome-quit-mid-tool issue we just fixed), then 1.3 (`test-generation`), 1.4 (live coding delegation), Sessions 2 and 3, then Phase 14.11 wizard MCP copy tweak. **Launch target: Apr 28–30 aspirational, May 1–2 acceptable.** MVP budget: ~6–8h remaining (session 163 burned ~2h on diagnosis + 5-probe validation + production patch + cadence flip).

> **Prior status: session 159 shipped Phase 14.10 — wizard Google sign-in step (`46057ae`).** New `pipeline/google_signin.py` (~230 lines) runs as the *last* wizard step (after API keys, before the bot reveal — so a skip/failure leaves a valid agent on disk). Detect-or-launch shape: fast-path reads `~/.brainchild/auth_state.json` + cached `~/.brainchild/google_account.json` and renders `✓ signed in as <email>`, asks `Sign in with a different account? (y/N)` — re-auth navigates Google `Logout → ServiceLogin` inside the same persistent profile (no nuking the 1.1 GB `browser_profile/`). First-time path launches visible Chrome (real `Google Chrome.app`, not bundled Chrome-for-Testing — profile compatibility), polls for `.google.com` SID, captures email from live `myaccount.google.com` DOM (free — page already open), persists both `auth_state.json` (Playwright `storage_state`, seeds Linux/Docker recovery) + `google_account.json` (cache for next wizard run). Skip = strong `y/N` warning, no default. **Per session-158 audit discipline**, `GOOGLE_ACCOUNT_FILE` lives on `config.py` and is wired into `tests/test_path_resolution.py`'s allow-list + anti-drift scanner; new `test_google_signin_paths_canonical_across_modules` enforces three-way agreement between the wizard's inlined paths and config (10 path tests now, all green). **Three live-QA loops with the user ironed out:** (1) `BRAINCHILD_BOT` import-gate trip — `google_signin.py` originally imported `brainchild.config` at module-top, dying when the wizard ran pre-bot-selection; fixed by inlining the three path constants (same compromise `setup.py` makes for `_ENV_FILE`) and a private `_auth_state_has_sid()` helper that mirrors `validate_auth_state` without the import chain. (2) Playwright launched bundled "Chrome for Testing" but the user's `browser_profile/` was created by real `Google Chrome.app` — Chrome refuses to open profiles across binaries, browser died on launch with `Target page... has been closed`; fixed by passing `executable_path` to match `macos_adapter.py:512`. (3) Re-auth prompt rendered as garbled text because Rich was eating `[enter]`/`[r]` as color markup; reframed as conventional `Sign in with a different account? [y/n] (n)` — matches git/brew/apt convention, default = continue. **Tests:** `tests/test_wizard_signin.py` (11 cases) covers detection across present/absent/malformed `auth_state.json` + `google_account.json` + email regex; live launch flow unit-untested by design (Playwright persistent-context mocking is brittle), gets its smoke test in Phase 14.8's clean-mac live-QA marathon. Full sweep green (test_wizard_signin/test_path_resolution/test_setup/test_chat_hardening/test_config_loader/test_entry_cli/test_connectors/test_918_wizard_mcp_lock/test_911-913/test_915/test_mcp_client_units). **Exact next step: Phase 14.11** — wizard MCP "context, not just tools" copy tweak (~20m); pure copy change to `pipeline/setup.py` step-2 picker intro + per-server captions, mechanics unchanged. Then Phase 15.10 (pre-launch hardening: edge-case audit + claude top-10 use-case research + other-bot live-test checklist). **Launch target: Apr 28–30 aspirational, May 1–2 acceptable.** MVP budget: ~7.5–9.5h remaining (session 159 burned ~2.5h on Phase 14.10 + three live-QA fix loops).

> **Prior status: session 158 completed a path-resolution audit and hardened the class of bug** — three commits across ~3h. (a) `4777a66` relocated `browser_profile/` and `auth_state.json` from repo-root (walked up via `_BASE` 4 dirs from `macos_adapter.py` — broken for installed/site-packages use) to `~/.brainchild/`; one-shot migration shim in `__main__.py` moved the 1.1 GB profile in place, live-QA confirmed Google session survived. (b) `1b6a74d` finished the audit — Explore agent found three more HIGH-severity findings: `setup.py` wrote wizard-collected API keys to `<repo>/.env` while `__main__._bootstrap_claude_imports` + `brainchild edit env` read from `~/.brainchild/.env` (latent divergence — users' API keys silently landed in the wrong file on any code path that took the home-anchored read); `session.py:save_debug` dumped to `<repo>/debug/` via the same `_BASE` pattern; adapter screenshot paths used CWD-relative `"debug/chat_btn_not_found.png"`. Added `config.ENV_FILE` + `config.DEBUG_DIR` constants, migration shim extended to relocate legacy `.env` (target-exists branch protects a live home .env from stale-legacy overwrite), renamed `_migrate_legacy_browser_artifacts → _migrate_legacy_user_artifacts` for the broader scope, `load_dotenv()` given an explicit absolute path. (c) `7fff5c1` added regression guards in `tests/test_path_resolution.py` — Layer 1 enumerates every `*_DIR`/`*_FILE`/`*_PATH` attr on config and asserts absolute + under HOME/tmp, with an anti-drift scanner that fails when a new path attr isn't in the allow-list (caught `BOT_DIR` during dev as designed); Layer 2 asserts byte-exact agreement between `config.ENV_FILE`, `setup._ENV_FILE`, and `_resolve_config_target('env')` — the exact three-way divergence that caused the .env bug can't recur silently. Parallel guard for `DEBUG_DIR` verifies session.py + both adapters reach `mod.config is config`. 9 tests in the file (up from 5), all green; full sweep green across config_loader, connectors, entry_cli, chat_hardening, mcp_client, 1571/1574/15745, claude_code_import. Also added **Phase 14.10 wizard Google-signin step** to the roadmap (~2h, ⬜) — prereq user surfaced at session start; relocation/audit work was the "fix this while we're here" dependency before 14.10 can land. **Exact next step: Phase 14.10** (wizard Google-signin step). Carry-over options if 14.10 is deferred: Phase 14.5 `install.sh` still blocked on GitHub `operator → brainchild` repo rename; Phase 14.7 uninstall docs (~10m, README-only) is the quick-hop; session 155's still-open Issue 3 text-block-before-tool-call wiring in `pipeline/llm.py:295-308` (~30–45m) remains available. **Launch target: Apr 28–30 aspirational, May 1–2 acceptable.** MVP budget: ~13.5–15.5h remaining (session 158 burned ~3h on path relocation + audit + regression guards). — resolves the false-failure bug live QA surfaced when a Claude Code delegation reported "the delegation failed" but the landing page actually appeared seconds later. Root cause: `_ServerHandle.call_tool` had a hidden hardcoded `timeout=30` that silently undercut the bot's `tool_timeout_seconds: 600` config override; its `concurrent.futures.TimeoutError` stringifies to `""`, producing the empty-signature `Tool 'foo' failed: ` error line; meanwhile the asyncio task kept running and the subprocess finished the work, so the user saw both "failure" and a completed delivery. User rejected the initial two-stopwatches-with-margin proposal in favor of a single source of truth: the MCP layer owns the timeout, chat_runner only drives heartbeats. Implementation: `config.DEFAULT_TOOL_TIMEOUTS` map with ship-level defaults per MCP (claude-code 600, playwright 300, figma 90, github/salesforce 60, linear/sentry/slack/calendar/gmail/drive 30, notion 45); `_effective_timeout_for` walks override → default map → global fallback; `_ServerHandle.call_tool` now accepts a required `timeout`, cancels the task on expiry, and raises an informative `MCPToolError` that flows through the existing error signpost; `chat_runner._execute_and_respond` lost its deadline/`timed_out` branch entirely. Heartbeat became exponential (8s → 16s → 32s → 60s cap via new `TOOL_HEARTBEAT_MAX_SECONDS`). Tests: `test_912_tool_timeout.py` rewritten to match the new contract (added `test_heartbeat_interval_backs_off` + `test_mcp_timeout_is_signposted_to_llm` + `test_mcp_timeout_fallback_when_llm_fails`), `test_mcp_client_units.py` gained `test_effective_timeout_precedence`. Full sweep green (912/911/913/915/mcp_client_units/mcp_client/mcp_shutdown/config_loader/chat_hardening). Docs: `agents/README.md` `tool_timeout_seconds` row updated to describe the new 3-tier precedence; stale "global 60s" comments in claude/engineer bundled configs rewritten. **Two follow-ups offered but not taken** (per user's explicit "disregard" on #3 and "not yet" framing on #2 in the architecture conversation): (a) ambient acknowledgement — keep chat loop polling during tool call so side messages get a "still finishing, will answer after" reply, cheap half-day work; (b) full parallel side-chat threads or cancel-and-restart, bigger lifts deferred to their own phase. **Debug scratch files** (`debug/admit_diagnostic.{html,png}`, `debug/post_admit_success.{html,png}`) untracked since session 151, not cleaned up this session. **Exact next step: live-QA the timeout fix**, then Phase 14.5 `install.sh` (~2h, still blocked on GitHub `operator → brainchild` repo rename). Quick-hop alt: 14.7 uninstall docs (~10m, README-only). Session 155's open Issue 3 — text-block-before-tool-call wiring in `pipeline/llm.py:295-308` (~30–45m) — also still open if you want a chat-feel polish task. **Launch target: Apr 28–30 aspirational, May 1–2 acceptable.** MVP budget: ~16.5–18.5h remaining (session 157 burned ~1h on diagnosis + refactor + tests).

> **Prior status: session 156 shipped `brainchild edit` and `brainchild where` subcommands (`01b0868`) — zero-memorization access to per-user configs under `~/.brainchild/`. Closes the config discoverability papercut surfaced during live QA (user found the bundled `src/brainchild/agents/<name>/config.yaml` before the runtime `~/.brainchild/agents/<name>/config.yaml` and edited the wrong file). `edit <bot|.env>` opens in `$VISUAL`/`$EDITOR`/`vi` with `shlex.split` for multi-word editors; `where <bot|.env>` prints the absolute path for shell composition. Both wired into `main()` dispatch and `_print_usage`. ~45 min of work. Also fixed the bundled `claude/config.yaml` caption-comment contradiction ("off by default" comment above `captions_enabled: true` value) → now "on by default" with why-and-opt-out framing; landed via session 155's commit `87255e0`. Session 155 live-QA fixes are the prior commit set (claude personality + ground_rules restore, wizard step-4 wipe bug, intro-post timing on meet.new). Phase 15.7/15.8/15.9/15.10/15.11 remain CLOSED. Next up: Phase 14.5 `install.sh` (~2h, still blocked on GitHub `operator → brainchild` repo rename). Quick-hop alt: 14.7 uninstall docs (~10m, README-only).** Session 154 work (unplanned — triggered by a live `brainchild run claude` failure): bundled `claude-code` MCP was booting via `python agents/engineer/claude_code.py` which only resolved because the launcher `cd`'d to REPO_DIR pre-packaging; path rotted to `src/brainchild/agents/engineer/...` and crashed at MCP-server spawn with `[Errno 2] No such file or directory`. Fix 1: flipped args to `-m brainchild.agents.engineer.claude_code` across all 4 bundled agents (commit `b21d7fc`). Audit surfaced `./github-mcp-server` (same bug class — relative command path) across all 4 agents; fix 2: dropped `./` → `"github-mcp-server"` and symlinked `/Users/jojo/Desktop/operator/github-mcp-server → ~/.local/bin/` so user-local install works immediately while 14.5's `install.sh` takes ownership long-term (commit `130831c`). Preserved the full 41-tool Go-binary surface (the npm `@modelcontextprotocol/server-github` remains ruled out per session 57's broken-`search_code` finding — `docs/mcp-pressure-test.md:193`). Added `tests/test_bundled_mcps_bootable_static.py` — three tests that walk every bundled `config.yaml` (enabled AND disabled blocks), verify every `command+args` combo resolves (bare → `shutil.which`, `-m <mod>` → `find_spec`, `python <script>` → abs-path-exists, absolute → exists+executable, relative → FAIL with pointed message about CWD under `uv tool install`), plus a warn-only version-pinning check that flagged 12 `@latest` entries (calendar/salesforce/slack × 4 agents — matches the 15.7.5 pressure-test punch list). Proven to catch both of today's bugs by directly feeding them to `_classify_command`. Five user-installed config copies at `~/.brainchild/agents/{claude,engineer,pm,designer,testbot}/` also patched for both bugs so the current install works without re-seeding. **Tier 2 dynamic bootability** (actually spawn each MCP, call `initialize() + list_tools()`, kill after 5s) deferred — needs credential fixtures for auth-required servers, belongs in deferred-live-tests until then. Session 153 work (uncoded — pure UX polish on the existing wizard): absolute-width build card (`expand=True` + terminal-aware `build_card.width_for()` with `MIN_WIDTH=28`), each MCP/skill on its own line, lightning bolts recolored blue→magenta, list cap at 5 with dim `+N more` overflow marker, `power-ups` label renamed to `MCPs`, reveal re-ordered so the card sits between "All set" and the run command. Picker: viewport scroll with `↑/↓ more` markers on narrow screens (computed from `console.size.height`), source tag dropped from skill sublabel, terminal-aware sublabel truncation. Claude preset: tagline rewritten, `editing in place` flash removed, auto-import now flips bundled-block `enabled=true` when user has that server in Claude Code (instead of no-op on collision), and only `~/.claude/skills/`-sourced skills get pre-checked (bundled shared-library skills stay off by default — identity signal). `_OFFICIAL_MCPS` expanded with `playwright/salesforce/sentry`. Tests green (setup + claude_code_import). 15.7.1/2/3 shipped session 148 (`6beff98` / `43dae3f` / `ed63026`), 15.7.4 + 15.7.4.5 shipped session 149 (`0432bad` + `a97d4fc`), 15.7.5 + 15.7.6 shipped session 149 async (`5453080` + `4986743`), 15.8.1 atomic CLI verb rename shipped session 150 (`892419e`), 15.9 `claude` agent shipped session 151. 15.7.1 ✅ structured error classification: `_resolve_env_vars` returns `(resolved, missing_vars)` persisted on each server block; `_classify_startup_failure` returns structured `{kind, fix, raw, [vars]}` with `missing_creds` > `binary_missing` > `startup_timeout` > `handshake_crash` > `unknown` precedence; renamed `failed_servers` → `startup_failures` and `disabled_servers` → `runtime_failures` (both now `dict[str, dict]`, rename forces every consumer to surface the type change as `AttributeError` instead of silently stringifying dicts into log lines); `record_tool_result` now accepts `error_text` and upgrades tripped servers from `runtime_failure` → `auth_failed` when any sub-threshold failure matched `_AUTH_ERROR_PATTERNS` (grounded in empirical captures: GitHub `401 Bad credentials`, Figma `403 Forbidden`, plus documented Slack/Sentry/Salesforce/Google patterns); `inject_mcp_status` now renders per-server `kind: fix` lines so the LLM can answer "why is X broken" specifically. 13 new tests in `test_1571_error_classification.py` + classifier-test rewrites. Archive: `tests/probe_auth_errors.py` (reusable empirical-capture script for 15.7.5 MCP additions) + `docs/mcp-auth-errors.md`. **Key empirical finding**: OAuth-over-mcp-remote servers do NOT surface auth failures as tool errors — they stall initialization waiting for browser auth (cancellation produces the same stall). Runtime sniff is a no-op for them; 15.7.3's cache-path check is the only real signal. **15.7.2 ✅ proactive failure banner:** `ChatRunner._post_mcp_failure_banner` posts one compact line at join (before the async intro so it lands deterministically), rendering per-kind labels ("missing SLACK_BOT_TOKEN" / "binary not found" / "didn't respond" / "crashed on startup" / "needs auth — run brainchild auth <name>" / "error") joined with "; " and closing with "Ask for details." Silent on the happy path. 6 new tests. **15.7.3 ✅ OAuth decoupling:** new `auth: env \| oauth` + `auth_url` fields on MCP blocks; `_mcp_remote_cache_dir()` globs `~/.mcp-auth/mcp-remote-*/` and picks lex-latest, `_oauth_cache_exists(auth_url)` checks `md5(auth_url)_tokens.json` (hash matches mcp-remote's `getServerUrlHash`). `connect_all` now pre-checks cache before spawn for oauth servers — missing → skip + record `kind=oauth_needed`. New `brainchild auth <mcp>` subcommand foreground-spawns the configured oauth MCP with stdin piped + stdout/stderr inherited, polls for tokens.json to appear, SIGTERMs on success and returns 0. Bundled Linear configs across all 3 agents declare `auth: oauth` + `auth_url: https://mcp.linear.app/mcp` (user's `~/.brainchild/agents/` intentionally NOT modified — preserves 15.7-session-147 insulation promise). 10 new tests + 3 CLI dispatch tests. Three deferred live-smoke items tracked in new `docs/deferred-live-tests.md`. **Exact next step: Phase 15.7.5 — new MCP scaffolding** (~2-3h). Add five MCPs to all three agent configs: Slack, Salesforce, Sentry, Playwright, google-calendar. Default-enablement rule: each MCP is `enabled: true` on an agent iff a bundled skill on that agent requires it (PM → Linear + Calendar; Designer → Figma; Engineer → GitHub + Sentry + claude-code; Slack/Salesforce/Playwright → scaffolded `enabled: false`). Each new block gets `auth`, `credentials_url`, `env`, `read_tools`, `confirm_tools`, `hints` — reuses the 15.7.1-onward schema. Existing installs insulated via session-147 `~/.brainchild/agents/` promise. Pressure-test per MCP: boot with real creds, confirm tools appear, one read tool executes cleanly. After that: 15.7.6 new-skill rollout (~2–3h) → 15.8 CLI verb rename (~1.5h) → 14.5 install.sh. **Launch target: Apr 28–30 aspirational, May 1–2 acceptable.** MVP budget: ~26–28h remaining (session 148 burned ~5h + session 149 burned ~2.75h of the 9–11h 15.7 envelope; ~3–5h remaining on 15.7).

> **Prior status: Phase 13.2 log cleanup COMPLETE (session 136).** Carved out a dedicated user-facing narrative layer on stderr so a first-time skeptical user gets a confidence-building progress story instead of the log-level firehose. Aligned with user on a three-tier model first: **stdout** = narrative (join / participants / tool confirm / tool result / disconnect / goodbye / errors), **`/tmp/brainchild.log`** = forensic DEBUG trail (every LLM call, every MCP frame, every chat poll, every participant-count change), **`BRAINCHILD_DEBUG=1`** = dev firehose (reserved for later, not yet wired). Changes landed in one atomic commit `e5bd0ef`: new `pipeline/ui.py` (64 lines) with `say/ok/warn/err/chat_in/chat_out` helpers that auto-disable ANSI when `NO_COLOR` is set or stderr isn't a TTY; `_run_macos` lost its root stderr `StreamHandler` (was piping every module's `log.info` to the terminal — the main noise source); `_run_linux` switched from `basicConfig(level=INFO, stream=stderr)` to the same file-based DEBUG pattern; `chat_runner.py` added a `✓ Joined meeting` milestone and routed auth-fail / already-running / disconnect / alone-leave `print()` calls through `ui.*`; errors now append `— see /tmp/brainchild.log` where a detail trail exists; Linux DISPLAY/MEETING_URL errors now go to stderr; added `ui.say("Launching Chrome…")` heartbeat right after the banner so the terminal isn't silent during the 3–8s browser spin-up. Also wrote a short **privacy-focused README** seed (0 bytes → 40 lines) flagging what lands in `/tmp/brainchild.log` and `~/.brainchild/history/<slug>.jsonl` (chat content, sender names, Meet URL as capability token, captions when enabled) — all local-only but plain text in a shared dir. Deferred post-launch: log rotation + per-instance logfile namespacing (if two brainchilds run concurrently they clobber `/tmp/brainchild.log`). Full test suite green (143 tests across 12 files). **Live-smoke for sessions 131 (intro-on-join) + 132 (personality/ground_rules split) is now three sessions overdue** — worth running `brainchild pm` once before kicking off 15.6. Next up is the phase queue: **15.6 security (~4.5h)** → 14 package (~1h) → 15 cross-platform (~4h) → 16 launch. Budget: ~9h vs April 19, 2026 launch (one day past the original target).

> **Prior status: Test-plan unit sprint COMPLETE — 11/11 components ☑ (session 135).** Components D (OpenAI provider parity), Entry (CLI + `brainchild try`), I (Guardrails gap-fill), and H-unit (Connectors unit surface) all landed in one sitting: 46 new tests across `tests/test_openai_provider.py` (12, mirrors Anthropic shape — request/response/error translation w/ `SimpleNamespace` fakes, `parallel_tool_calls=False` when tools present, `context_length_exceeded` → `ContextOverflowError` branch), `tests/test_entry_cli.py` (18, imports `__main__.py` via `importlib.util.spec_from_file_location` so its `if __name__ == "__main__"` guard doesn't fire; covers bot discovery, tagline resolution, every main() dispatch branch, `_run_bot` URL+flags+`BRAINCHILD_BOT` env, `--check-mcp` short-circuit, `_run_try` unknown-bot bail), `tests/test_connectors.py` (11, TerminalConnector with `threading.Thread` patched out so the stdin reader doesn't EOF+SIGINT the test, `JoinStatus`, `validate_auth_state`, `inject_cookies`, `_chrome_lock_is_live` three branches), and 5 gap-fill tests appended to `tests/test_guardrails.py` (GIF/WebP base64 prefixes, 1000-char scan-window boundary, 10%-non-printable strict-`>` boundary, `\n\r\t` carve-out, `log_rejection` WARNING format). **Also cleaned tests/ of legacy scaffolding:** deleted `tests/test_playwright.py`, `tests/test_playwright_basic.py`, `tests/test_meet_guest.py`, and six `tests/NN_N_*_testing.md` step-specific run-sheets now absorbed into `docs/test-plan.md`. Commit `24212d1`. `tests/` is now 22 files — 18 structured suites + 4 targeted keep-files (`test_face.py`, `test_caption_late_bind.py`, `test_transcript_dedupe.py`, plus the provider/entry/connector files added this session). **H integration deferred** (macos/linux Playwright-driven adapters stay in manual smoke — `ALONE_EXIT_GRACE_SECONDS`, `LOBBY_WAIT_SECONDS`, `CAPTION_SILENCE_SECONDS`). **Live-smoke for sessions 131 (intro-on-join) + 132 (personality/ground_rules split) still deferred** — user explicitly chose "delete legacy, defer live smoke" at the end of session 135. Next up is the post-test-plan queue: **13.2 log cleanup (~2h)** → 15.6 security (~4.5h) → 14 package (~1h) → 15 cross-platform (~4h) → 16 launch. Budget: ~11h vs April 19 launch (day-of). 

> **Prior status: Test-plan sprint — Components E (ChatRunner) + Setup wizard both shipped at declared depth (session 134).** 33 new tests across `tests/test_chat_runner_gaps.py` (19, new file, Gap-fill across buckets M1-M6: 1-on-1+auto-leave, confirmation, skills routing, intro-on-join, server trip, `_send` bookkeeping) and `tests/test_setup.py` (+14 gap-fill across buckets S1-S6: `_parse_env`, `_append_env`, `_collect_env_refs`, `_is_valid_skill_source`, `build_card.render`, `_first_line`). All 29/29 setup tests pass, all 19/19 new chat_runner_gaps tests pass. **Incidental test hygiene (session 134):** `test_911_size_management.py`, `test_913_tool_history_collapse.py`, `test_915_reconnection.py` all gained the one-line `os.environ.setdefault("BRAINCHILD_BOT", "pm")` fix so they execute their bodies standalone instead of silently exiting on config import — same hazard session 133 patched in `test_skills.py`. Seven of eleven components now ☑ in `docs/test-plan.md`. Commit `94c89d4`. **Remaining test-plan components:** D — OpenAI provider parity (Boundary, M), Entry — CLI + `brainchild try` (Boundary, M), I — Guardrails (Gap-fill, S), H — Connectors (Boundary unit / manual integration). **Still not live-tested:** session 131's intro-on-join and session 132's schema/wizard redesign both want `brainchild try pm` + `brainchild pm <url>` smoke — carried over from session 133. Budget: ~13h vs April 19 launch (day-of). Pick up with either the live-smoke of 131+132 or continue test-plan at Component D or Entry. After test-plan passes: 13.2 log cleanup → 15.6 security → 14 → 15 → 16.

> **Prior status: Session 162 (April 25, 2026) — live-skill-tests Test 1.1 walk-through plus streaming-paragraph infra + polish landed in working tree (no commit yet — carryover triage still owed from session 161).** Worked through Session 1 / Test 1.1 of `docs/live-skill-tests.md` (`codebase-walkthrough` against the operator repo) end-to-end with the user. Test exposed several real issues that got fixed in-band: (1) **Streaming + paragraph-flush across both providers** — added `complete_streaming(...)` to `LLMProvider`, `flush_paragraphs()` helper in `providers/base.py`, real implementations in `providers/anthropic.py` (via `client.messages.stream()`) and `providers/openai.py` (via `chat.completions.create(stream=True)` with tool-call delta accumulation by index). `LLMClient.ask()` and `send_tool_result()` thread an `on_paragraph` callback through to the providers; chat_runner builds a `_streaming_callback()` closure with 250ms min spacing between flushes, wired into all 5 LLM call sites. Provider-neutral contract per `feedback_provider_neutral.md`. New `tests/test_streaming_paragraph_flush.py` (9 cases). Net effect: the bot now posts each paragraph of a long reply as its own Meet chat message in real time; first paragraph appears ~3s after generation starts instead of after the full call completes; decoration-only fragments (`---`, `***`) are dropped. (2) **Skill rewrite — deliver in one turn, not paged across turns.** `codebase-walkthrough`, `live-bug-triage`, `migration-plan`, `prd-from-discussion` all rewrote their "Shape" + sequence rules from "post each phase as a separate message, do not pre-emit the whole thing" → "single turn, one phase per paragraph, harness streams each as a distinct Meet message." Kept `schedule-followup`'s two-message split (it's a side-effect confirmation gate, not a "pause mid-finding" anti-pattern). Edited both bundled (`src/brainchild/skills/<name>/SKILL.md`) and runtime (`~/.brainchild/skills/<name>/SKILL.md`) copies. (3) **MAX_TOKENS bump 1000 → 2000** in `config.py:133` — the original `codebase-walkthrough` reply hit the 1000-token cap mid-trace; multi-paragraph skill outputs need ~1200–1800. (4) **`claude-code` MCP wrapper fixes** in `agents/engineer/claude_code.py`: (a) added `stdin=asyncio.subprocess.DEVNULL` to the inner-claude subprocess (was inheriting MCP JSON-RPC stream as stdin → 3s probe timeout → exit code 1 → 188-byte error result); (b) removed our wrapper's `[workdir: ...]\n[repo: ...]\n[Completed in Xs, cost $Y]` footer from the tool result — operators can read it from `/tmp/brainchild.log`; (c) added `_CLAUDE_WORKTREE_TRAILER_RE` to strip the inner `claude` CLI's *own* auto-appended workdir/repo trailer (different format with backticks; the inner CLI emits this when run with `--worktree`). (5) **429 retry-with-backoff** in `providers/anthropic.py` — both `complete()` and `complete_streaming()` wrapped in retry loops that read `anthropic-ratelimit-input-tokens-reset` or `retry-after` headers, sleep up to 60s, retry up to 2× before raising. Real safety net for transient bursts; doesn't fix single-call prompts that exceed the user's per-minute tier (those need either tier upgrade or prompt trimming). (6) **Wizard control-char input filter** in `pipeline/setup.py:_prompt_with_hint` — strips `\x00-\x1f\x7f` before returning, so accidental ESC presses (Rich's `Prompt.ask` captures them as single-char input) can't poison fields. The user's `personality: "\e"` (`\x1b` ESC byte) corruption is now impossible to introduce; cleared the existing bad value to `personality: ''`. (7) **Intro prompt anchored on `config.AGENT_NAME`** in `llm.py:intro()` — explicitly tells the model its name and forces "Hi, I'm <name> — <substance>" pattern instead of the previous "Hi, <name> — <substance>" which read as the bot greeting Claude. **Working-tree state at session end:** all session 161 carryover (heartbeat verb wiring, confirmation reformat, intro greeting tweak, setup→build CLI rename pile from prior sessions) plus session 162's streaming infra + polish + skill rewrites all sit unstaged. **No commit landed this session by design** — session 161 explicitly designated the next session as the triage session, and we did live-tests instead, so the pile got bigger. **Exact next step:** triage the now-larger carryover (session 161 + session 162 + earlier-session pile). Recommend isolating session 162's streaming infra as one atomic commit (providers/base.py + providers/anthropic.py + providers/openai.py + pipeline/llm.py partial + pipeline/chat_runner.py partial + tests/test_streaming_paragraph_flush.py + skill rewrites for codebase-walkthrough / live-bug-triage / migration-plan / prd-from-discussion + claude_code.py + config.py MAX_TOKENS + setup.py wizard filter), then session 161's chat-polish wires (heartbeat verb call site, confirmation reformat, intro greeting tweak), then the rename pile. Then continue Test 1.2–1.4 of `docs/live-skill-tests.md`. Then Phase 14.11 wizard MCP copy tweak. **Hard-won knowledge logged in `agent-context.md`:** the inner `claude` CLI auto-appends a markdown-formatted `[workdir: \`...\`]\n[repo: \`...\`]` trailer when run with `--worktree` — separate from any wrapper-side footer; need to strip it post-receipt; regex must be anchored to end-of-string (`\Z`) so it can't strip a similar-looking pattern mid-document. **MVP budget unchanged** (~6.5–8.5h remaining; launch target Apr 28–30 aspirational / May 1–2 acceptable; no roadmap rows closed this session — work was live-test prep + bug fixes that surfaced during testing, not phase-tracked).
>
> **Session 161 (April 25, 2026) — chat-polish micro-pass during live-bot testing. One commit (`48a34b2`), ~30m.**

> **Prior status: Test-plan sprint — Components B, A, C, G, F all shipped at declared depth (session 133).** 31 new unit tests across `tests/test_meeting_record.py` (8, Boundary+race), `tests/test_config_loader.py` (6, Boundary), `tests/test_llm_client.py` (6, Boundary), `tests/test_skills.py` (+5 gap-fill), `tests/test_mcp_client_units.py` (6, Gap-fill). All pass. **Incidental fix:** `tests/test_skills.py` was missing `BRAINCHILD_BOT` default — one-line addition unblocked 5 silently-dead pre-existing LLM/ChatRunner wiring tests. **Behavioral finding confirmed (LLMClient):** captions skip the first-contact hint and don't mark the speaker as greeted — ambient talk is not a direct greeting trigger. **MCPClient race carve-out deviation:** there is no reconnect path in current code, so "tool timeout racing with reconnect" has no target; orphan-cleanup is already covered by `test_mcp_shutdown.py` — revisit if reconnect is added post-launch. Commits `e37b1c9` (tests) + `a23099b` (doc updates). **Prior status: Schema + wizard redesign shipped (session 132) — four-layer customization frame (Tools → Playbooks → Ground Rules → Personality) with yaml split and 7-step wizard.** User flagged that the three central customization levers (system prompt, MCPs, skills) had overlapping properties and no internal terminology signalling how they build on each other. Mapped the ambiguity and locked a four-layer frame with wizard-facing labels: Tools (MCPs) → Playbooks (Skills) → Ground Rules → Personality. Implementation split `llm.system_prompt` into two top-level yaml blocks (`personality` + `ground_rules`), composed at load time as `personality\n\nground_rules` so rules land last and get LLM end-of-prompt weighting. Hard cut — all three bundled bots migrated in same commit, no backward-compat shim. Uniform split rule: voice/identity → personality, always-true constraints → ground_rules. Only the text differs across bots; designer's personality kept its opinion-forward register ("That spacing is cramped — bump it to 16px" is the register), engineer's ground rules preserved the full tool-routing + delegation blocks. Wizard grew 5 → 7 steps with instructional copy on each: step 2 "Tools (MCPs)", step 3 "Playbooks (Skills)", step 4 "Ground Rules", step 5 "Personality", step 6 API keys, step 7 atomic write. Steps 4+5 pop `$EDITOR` (via `VISUAL` then `EDITOR` then `vi` fallback) with two modes — edit-preset gets a two-path picker (inherit-with-cursor vs start-blank), from-scratch is always blank. Wizard pedagogical order is ground_rules → personality (opposite of concatenation order — more intuitive to author rules first, then voice). `agents/README.md` gained a new "Anatomy of a bot" section naming the four layers and explaining the compose order; field reference tables updated (removed `llm.system_prompt` row, added new top-level `personality`/`ground_rules` tables). `CLAUDE.md` config bullet list rewritten to match. Tests: `test_setup.py` fixtures cleaned (`system_prompt` removed from llm blocks), `_step5_write` → `_step7_write` renamed. Commit `1fdbc4e`. **Not yet live-tested.** Pick up with live-smoke (intro-on-join from 131 still unvalidated too), then `docs/test-plan.md` Component B (MeetingRecord), then pre-existing `test_912` failure, then 13.2 log cleanup → 15.6 → 14 → 15 → 16. Budget: ~14h vs April 19 launch (day-of). **Prior status: Self-intro on join shipped (session 131) — discoverability fix, not a roadmap-phase item.** Strategic conversation surfaced a real product gap: a user joining a meeting with Brainchild has no idea what the bot can actually do, and no idea where to tweak/shape its behavior. Mapped the four steering surfaces (tools = capability, system prompt = identity/always-on, MCP hints = server-scoped framing, skills = playbooks) and concluded the abstraction is right — what's missing is *visibility*. User scoped a one-toggle solution: new `agent.intro_on_join: true` field (defaulted true on pm/designer/engineer); on join, the bot generates a 2-sentence self-intro via the LLM (using already-injected skills + MCP context, no message history) and posts it to chat. Implementation lives in three places: `config.py` reads the flag; `LLMClient.intro()` fires a single `provider.complete` call with a strict prompt (2 sentences, "I can…" framing, no tool/MCP terminology, no greeting filler — focuses on outcomes/use cases to inspire actual usage); `ChatRunner` runs generation in a background thread (`_generate_intro`) so the polling loop starts immediately, with the main loop owning the actual `send_chat` call to keep Playwright single-threaded. Messages that land during the ~2s generation window are persisted to the meeting record as normal but their LLM dispatch is deferred — buffered into `_pre_intro_buffer` and drained in order on the first iteration after `_intro_ready` fires. On generation failure: log + skip silently (per user — fake-success masks the real problem; if the LLM is broken the next message will surface it honestly). Reconnect re-intros (no per-meeting deduplication — explicit edge-case skip per user). Updated `docs/test-plan.md` Notes section so the next session's test inventory must include intro-on-join coverage in components A (config loader), C (LLMClient), and E (ChatRunner). **Not yet live-tested** — `brainchild try pm` is the cheapest sanity check (TerminalConnector skips the join wait so intro fires within ~2s of boot); real Meet test via `brainchild pm`. Pick up from: live-smoke the intro on a real Meet, then start walking the test plan at Component B (MeetingRecord) per the per-session protocol. Carried-over: `test_912::test_timeout_sends_failure_message` still broken from session 129 (pre-existing MagicMock issue). Budget: ~14h vs April 19 launch (day-of). **Prior status: Testing strategy locked in + calendar residue swept (session 130).** No roadmap-phase work this session — meta work to pay down accumulated test debt before launch. Built `docs/test-plan.md` as a component-scoped testing strategy: 11 components ordered by risk × value, each with a declared edge-case depth (Boundary for never-tested, Gap-fill for partially-tested, Boundary + race carved out for MeetingRecord and MCPClient's concurrency surfaces). Adversarial and Exhaustive depths explicitly deferred post-launch. Three-layer testing model locked in: L1 unit tests via inline `MagicMock` matching the existing `test_9xx` style, L2 integration via `brainchild try <bot>` with real LLM + real MCP through the `TerminalConnector`, L3 manual Meet smoke run by the user pre-release. Explicitly **rejected** formal `Fake*` provider classes after reconsidering — they duplicate what MagicMock already does and drift from real-provider behavior (parity-first violation). `TerminalConnector` already *is* the honest L2 harness. Shared test helpers will emerge in `tests/_helpers.py` only when duplication surfaces, never upfront. Separately: found and swept **calendar residue** left behind by the session-113 deletion of `pipeline/calendar_poller.py`. Deleted orphan `tests/test_calendar.py` (deps weren't in requirements), removed the Calendar-session block in `scripts/auth_export.py`, pruned `browser_profile_calendar/` from `.gitignore`, and rewrote six comments in `connectors/macos_adapter.py` + `tests/test_caption_late_bind.py` that framed late-bind via "calendar-polling mode" (late-bind is a real design, but motivated by post-navigation slug resolution from `meet.new`, not a feature that no longer exists). Preserved the deleted implementation on a new **`calendar-preserved`** branch (pushed to origin) pointing at `a0e6d74^` — matches the `voice-preserved` archival pattern. Local OAuth artifacts (`credentials.json`, `token.json`, both `browser_profile_calendar/` dirs) moved out of the repo to `~/Desktop/brainchild-archive/calendar-2026-04/` with a README documenting revival steps. Confirmed via grep that `credentials.json`/`token.json` were **not** used by the headless browser auth (that's `auth_state.json` + `browser_profile/`) — they were exclusively for the deleted Google Calendar API test flow. Calendar mode is explicitly post-MVP. Also ran 3 of 6 smoke tests from the 129 handoff via `brainchild try`: (1) pm/engineer/designer all boot clean with MCPs connected and banners rendered; (2) confirmed `MCPClient.tool_timeout_for()` wiring — delegate→600, github→60 global fallback, both in `tool_timeout_for` directly and in `chat_runner.py:441`'s resolution pattern; (3) pm's Linear `list_issues` round-trip end-to-end, LLM→MCP→reply in ~6s with correct output. Tests 4/5/6 (LOBBY_WAIT_SECONDS, ALONE_EXIT_GRACE_SECONDS, CAPTION_SILENCE_SECONDS) deferred to pre-launch manual smoke since they need real Meet state. Committed as `2434cf5`. Budget: ~15h remaining vs April 19 launch (day-of). Pick up from `docs/test-plan.md` **Component B (MeetingRecord)** as the first component test session — small, zero current coverage, every other component reads from it. Also still pending from 129 handoff: fix `test_912::test_timeout_sends_failure_message` (pre-existing MagicMock issue, not ours). After a few component test sessions, Phase 13.2 (log cleanup, ~2h) is next on the phase roadmap. **Prior status: Phase 13.1 COMPLETE — config trim + per-server tool timeout override shipped (session 129).** Closed the last 13.1 thread: audited every field in the three shipped `agents/*/config.yaml` files against user-facing value vs. tuned-once internals. Removed nine fields from per-bot YAML and hard-coded them into a new `INTERNAL TUNING` block in `config.py` (ALONE_EXIT_GRACE_SECONDS=60, LOBBY_WAIT_SECONDS=600 [renamed from the misleading `idle_timeout_seconds`], CAPTION_SILENCE_SECONDS=0.7, MAX_TOKENS=1000, TOOL_RESULT_MAX_CHARS=50000, TOOL_TIMEOUT_SECONDS=60, TOOL_HEARTBEAT_SECONDS=8, BROWSER_PROFILE_DIR, AUTH_STATE_FILE). Deleted `conversation_timeout` entirely — it was loaded as `CONVERSATION_TIMEOUT` but never referenced at runtime. Added per-server `tool_timeout_seconds` override in `mcp_servers` blocks (wired via new `MCPClient.tool_timeout_for()`), so engineer's delegate MCP can carry `600` for Claude Code's 3–10min runs while pm/designer keep the global 60s default — resolves the session-128 caveat about delegate-on-pm timing out mid-run. User-facing config now has 4 fields under `agent`, 5 under `llm`, plus `skills` / `transcript.captions_enabled` / `mcp_servers` — every remaining field is something a persona owner would genuinely tune. Updated `CLAUDE.md` + `agents/README.md` schema tables to match; pointed power users at the INTERNAL TUNING block in `config.py`. Tests: `test_setup.py` fixture cleaned, `test_911` and `test_912` got `mcp.tool_timeout_for.return_value = None` mock stubs; all chat-hardening + 911/913/915/guardrails/mcp_client/mcp_shutdown/setup suites green. **One pre-existing test failure confirmed and deferred:** `test_912::test_timeout_sends_failure_message` was already failing on main before session 129 edits (verified via `git stash`) — unrelated MagicMock issue where `mcp.server_for_tool()` returns a MagicMock that falls into the wrong `record_tool_result` branch. User flagged for session 130. **Not yet live-tested:** config changes need end-to-end smoke in a real Meet — see `docs/handoff.md` for the explicit test plan. Committed as `d9ad5e1`. Budget: ~15h remaining vs April 19 launch (day-of). Pick up with the live smoke-test of trimmed configs + fix the test_912 pre-existing failure before moving to 13.2. **Prior status: Phase 13.1 partial — MCP baseline config normalized across all three shipped bots (session 128).** Open threads cleanup first: 10 stale `.claude/worktrees/` from the 15.5.7 smoke were removed + their matching `worktree-*` branches deleted (all clean, no uncommitted work). Then sat down on 13.1 itself — the session-124-flagged "incomplete" that no prior handoff ever articulated. User scoped it as: every shipped bot should declare the same full-union MCP scaffold in `config.yaml` so the setup wizard has a consistent surface to toggle; only role-appropriate + actively-tested MCPs enabled by default; pin versions so runtime behavior stays stable through launch. Checked npm on the 5 npx-launched candidates and found **two deprecation bombshells**: `@modelcontextprotocol/server-slack` and `@modelcontextprotocol/server-brave-search` are both upstream-deprecated (Anthropic archived a swath of its reference servers mid-2025). User opted to drop both from the baseline rather than ship known-deprecated packages or chase community forks at T-1. **Rewrote all three `agents/*/config.yaml` files** to the same canonical shape: 5 MCP blocks in alphabetical order (`delegate`, `figma`, `github`, `linear`, `notion`), identical env/read_tools/hints across bots, per-bot `enabled` flags set appropriately — pm: github+linear, engineer: github+delegate, designer: figma+linear. Hints merged to the union across prior versions (github picks up engineer's `search_code` + multi-step-chain hints; linear keeps pm's "spoken commitment" assignee hint). Pinned `mcp-remote@0.1.38`, `figma-developer-mcp@0.10.1`, `@notionhq/notion-mcp-server@2.2.1`. Updated `docs/mcp-pressure-test.md`: glance table now includes figma + delegate, slack + brave sections deleted, last-updated stamp bumped, deprecation note added. Verified: `import config` loads clean on all three bots with expected enabled sets; `test_chat_hardening.py` + `test_mcp_client.py` green. **Caveat surfaced but deferred:** engineer has `tool_timeout_seconds: 600` for delegate's 3–10min Claude Code runs, pm/designer have `60` — if a user enables delegate on pm/designer via the wizard, delegations will timeout mid-run. User opted to ship as-is; fix during wizard work. **13.1 is partial, not done** — user has more baseline-config changes queued for next session before closing the step (scope not yet articulated). Pick up by asking the user which additional baseline-config keys they want to revisit. Budget: ~16h remaining vs April 19 launch (day-of). **Prior status: Phase 15.5.7 fully shipped — live-smoked end-to-end via `brainchild try engineer` (session 127, commit `2a7de34`).** Live smoke surfaced one tangential bug in `pipeline/chat_runner.py` unrelated to the delegate path itself: the harness's tool-confirmation prompt ("I'd like to run X. OK?") was being persisted to the meeting record as `kind="chat"`, then replayed back to the model on the next turn — the model started mimicking the pattern verbatim as plain text *before* raising its hand for a real `tool_call`. Two visible symptoms: (a) every confirmed tool call required the user to type "ok" twice (once to the model's parroted prompt, once to the harness's real one), (b) sometimes after the first "ok" the model just hallucinated a result instead of emitting the real `tool_call` — caught live when Engineer claimed `/tmp/test-delegate-2` contained `README.md` when it actually contained `f.txt` (a file from a different repo's earlier turn). Root cause: `_send()` had no way to distinguish harness plumbing from real conversation; both ended up in the same `kind="chat"` bucket that `pipeline/llm.py:155-157`'s `tail()`-replay reads from. Fix: `_send(text, kind="chat")` now takes an optional `kind`; `_request_confirmation()` passes `kind="confirmation"`; the inbound-message persistence in `_loop()` tags the user's reply with `"confirmation"` whenever `_pending_tool_call` is set. Confirmations stay in the JSONL audit trail but the LLM prompt-builder skips them (only `chat` and `caption` are replayed), so the model literally cannot see the pattern it was mimicking. Re-ran the smoke after the fix — single confirmation prompt per call, repo-2 listing returned ground truth, per-repo session isolation intact (three independent sandboxes coexist across `/tmp/test-delegate/.claude/worktrees/sorted-shimmying-shore`, `/tmp/test-delegate/.claude/worktrees/vivid-mapping-hoare`, `/tmp/test-delegate-2/.claude/worktrees/dynamic-inventing-owl`). 15.5.7 done. Pick up with **Phase 13 — log cleanup (~2h) + 13.1 gap check (~30m)** — 13.1 was flagged by the user at session 124 start as "actually incomplete" and the specific gap still hasn't been captured. Ask the user what remains under 13.1 before touching 13.2. Budget: ~19h vs April 19 launch. **Prior status: Phase 15.5.7 shipped in session 126 (commit `23bf258`) — delegate multi-repo + per-repo continuity + land flow.** Live `brainchild try engineer` run at session start exposed three coupled regressions in the delegate path: (1) every call spawned a fresh `--worktree` so follow-ups couldn't see prior files and the LLM silently re-created state; (2) sandbox worktree paths were never surfaced, so Engineer reported "confirmed working" for files the user would never find in their working tree; (3) delegate was hard-pinned to whatever repo Brainchild launched from — effectively "only edit the Brainchild repo," which is the wrong default for almost every user. Fix rolled into one rewrite of `agents/engineer/delegate_to_claude_code.py`: `delegate_to_claude_code(task, repo_path)` where `repo_path` is required; module-level `_SESSIONS: dict[str, {session_id, worktree_path, base_sha}]` scoped by canonical repo path. First call per repo runs `claude -p --worktree` with `cwd=repo_path`, diffs `git worktree list --porcelain` before/after to recover the worktree, stores the session id from JSON output. Follow-ups run `claude -p --resume <id>` with `cwd=worktree_path` (the key insight from verification: `--resume` looks up sessions by project dir = cwd, and you MUST NOT pass `--worktree` on resume or it spawns a new sandbox). Falls back to fresh worktree if resume returns "No conversation found." New `land_worktree_changes(repo_path)` tool stages all changes in the worktree (Claude Code leaves new files untracked so a naive `git diff` misses them), diffs against `base_sha` captured at first call, and applies into the user's working tree — fails cleanly on divergence. Tool result footer carries `[workdir: ...]` + `[repo: ...]`; Engineer's system prompt + delegate hints teach the model to ask for `repo_path` on first delegation, reuse it for follow-ups, mention the workdir when reporting back, and offer to land after success. README gains "How delegation works" section. Spec doc at `docs/delegate_multi_repo_spec.md`. Verified in isolation on three `/tmp/` fixture repos — create/resume-read/land round-trip clean, two repos in parallel hold independent sessions, no cross-contamination. **Not yet live-smoked via `brainchild try engineer` — explicit 6-step test plan saved to `docs/handoff.md` for session 127 kickoff.** Pick up with that smoke, then Phase 13 log cleanup (~2h) + 13.1 gap check (~30m). Budget: ~19h remaining vs April 19 launch. **Prior status: Build card polish + config hygiene shipped in session 123.** Build card frame now uses plain white `Panel` for wizard steps 2 & 3 (MCP + Skills pickers) and the per-character rainbow ANSI frame only on the final reveal — user flagged that showing the rainbow every step diluted the "gift" moment. Separately, all wrapping logic moved out of the frame renderers and into `_compose_body`: tagline now wraps within the right-hand meta column (width = `_INNER - 5 - portrait_w` cells) *before* being zipped with the portrait so a long tagline extends vertically under the name instead of crashing across the face grid; power-ups and skills lines use a hanging indent (`_hang_wrap`) so continuation rows align under the first item instead of flush-left against the border. Every emitted body row is pre-padded to exactly `_INNER` cells, and Panel is created with `no_wrap=True, overflow="crop"` so Rich doesn't re-flow and strip leading indent. **Config hygiene (Phase 13.1 partial):** removed the dead `user_display_name` / `USER_DISPLAY_NAME` key completely — `config.py:36` constant, `pipeline/setup.py:248` defensive pop, 3 `agents/*/config.yaml` files, 3 per-agent README setup snippets, `CLAUDE.md:89` field list, `docs/plan.md:32` wizard-prompts description, and 3 `tests/15_5_*_testing.md` prep sections (renumbered to stay sequential). Field was vestigial from voice era (used to filter own captions) and never referenced at runtime post-chat-pivot. Separately restored `agents/pm/config.yaml` + `agents/engineer/config.yaml` from HEAD before the strip — both had accumulated wizard-round-trip damage (comments/blank-lines gone, `linear: enabled: true` + `delegate: enabled: false` flipped on engineer) from prior session smoke-tests that `yaml.safe_dump` can't preserve. Added **Config reference** section to `agents/README.md` — one markdown table per top-level block (`agent`, `llm`, `connector`, `skills`, `transcript`, `mcp_servers`) documenting every field with type, default, and meaning. That's now the source of truth for the schema so the shipped YAMLs stay comment-free on purpose. Two historical `user_display_name` references in `docs/agent-context.md` + `docs/roadmap.md` left in place intentionally — they're session-log narratives explaining *why* the key was removed, not runtime references. YAML comment-loss bug (`ruamel.yaml` round-trip) left out of scope per user — 1–2h, adds a dep, purely ergonomic, no user hitting it today. Also cleaned up untracked test artifacts at session start: `agents/test{,2,3}/`, `agents/engineer/skills/`, and 5 debug screenshots. 15/15 setup + 3/3 chat-hardening tests green. Committed as `60cadef`. Pick up from Phase 13.2 (log cleanup, ~2h) against April 19 launch (~16h remaining). **Prior status: Phase 15.5.5 wizard final polish shipped in session 122.** Custom-mode prompt count trimmed from 5 → 2: dropped `display_name` (auto-equal to `name`), dropped `trigger_phrase` (auto-set to `@<name>`), dropped `user_display_name` (it was a dead config key — `USER_DISPLAY_NAME` is loaded in `config.py:36` but never read anywhere in the runtime), and simplified tagline prompt to a plain `tagline:` label (removed "(one line, under ~60 chars)" hint, added `show_default=False` to suppress the empty `()` Rich was rendering for `default=""`). Build card frame now renders a per-character rainbow via raw ANSI — every border glyph (`╭ ─ ╮ │ ╰ ╯` incl. title chars) cycles through red/green/yellow/blue/magenta using `itertools.cycle(_COLORS)`; replaced Rich `Panel` with hand-composed `Text.from_ansi` since Panel only accepts one `border_style`. Body padding uses `rich.cells.cell_len` for cell-accurate alignment around the 2-wide `⚡` glyph. All other wizard text demoted to plain white — stripped color from `⚡`/`★` glyphs, picker cursor (`bold cyan` → `bold`), banner/reveal header (`bold magenta` → `bold`), and error/success lines (`[red]`/`[green]`/`[yellow]` markup dropped). `[bold]` and `[dim]` kept as weight/intensity modifiers (not colors). 15/15 setup + 3/3 chat-hardening tests green. Committed as `037ef87`. Pick up from Phase 13 (config audit + log cleanup, ~4h) against April 19 launch (~18h remaining). **Prior status: Phase 15.5.5 wizard UX re-polished in session 121 — second pass driven by user smoke-test feedback on the session-120 picker/card rebuild.** Step 1 renamed "Fighter select" → "Choose your base agent" (picker title emptied to avoid duplication). Step 2 picker title "Equip your MCPs" removed + MCP sublabels (hints tagline) dropped — just MCP name. Step 3 restructured into a single bundled-skills picker followed by `Add your own skills? [y/N]`; sentinel-row approach tried first but removed when the user pointed out that toggling a list row can't pop an input field mid-picker (Rich `Live` blocks it) — y/n gate is the simpler mental model. Step 3 header reads "3. Add-ons (Skills)" with `Your <name> agent comes with the following:` subtitle. Auto-find of `~/.claude/skills/` as default path dropped entirely (deferred engineer-specific variant for a future pass). `_skill_subtitle` no longer caps descriptions at 60 chars — the cut-off the user was seeing came from that truncation, not the layout; uncapped descriptions now wrap naturally under the label since picker sublabels moved to their own indented line (picker left-column was getting squeezed by the 40-col build card so the on-label-suffix sublabels were being truncated by Rich — fixed by restructuring rows to `label` / `<indent>sublabel` two-line layout in `picker.py:_render_rows`). Build card: `border_style` flipped from `cyan` → `bright_magenta`, fixed `width=40` for uniform sizing across steps 2/3/reveal, "based on X" meta line removed (dropping `based_on` kwarg from `build_card.render` and `WizardState.card`). Wizard banner color `cyan` → `magenta`. `console.clear()` now fires between steps 1→2→3→4 so the previous step + card disappear before the next one draws. Final `Confirm.ask("Write bundle ...")` replaced with `console.input("Press Enter to reveal your bot ✨🎁")` — no "are you sure" gate, just a dramatic beat. Step 1's preview pane now centers the bot portrait using `rich.align.Align.center` + `rich.console.Group` (extended `Choice.preview` to accept `RenderableType` in addition to `str`; picker's fallback preview branch wraps strings in `Text()` and passes renderables through). **Smoke-test revealed a real edit-in-place side effect:** `yaml.safe_dump` strips comments, blank lines, and hand-formatting when rewriting `config.yaml` in edit-mode — logged as Hard Won Knowledge for a future pass (would need `ruamel.yaml` or comment-preserving diff-merge to fix properly). All 15/15 setup tests + chat-hardening green. Pick up from Phase 13 (config audit + log cleanup, ~4h) against April 19 budget (~22h remaining). **Prior status: Phase 15.5.5 polished in session 120 — wizard UX rebuilt around an arrow-key picker primitive (`pipeline/picker.py`) and a persistent live "Your build" card (`pipeline/build_card.py`) that updates as the user toggles MCPs and skills, with a final portrait-reveal moment in step 5.** New `readchar` dep. Step 1 = arrow-key gallery (`Custom` + each existing bot, right pane shows the highlighted bot's portrait). Steps 2 & 3 = arrow-key multi-select with the build card on the right that re-renders on every space-toggle (`⚡` per equipped MCP, `★` per equipped skill — disabled items hidden, no clutter). Custom-mode portraits stay as a `?? / ??` placeholder face through every step until step 5 writes the bundle and `_reveal()` swaps in the resolved real portrait under a `✨ bot reveal 🎁` flourish; edit-in-place mode shows the existing portrait from the start (no surprise to give). Picker primitive supports a `right_pane: Callable[[cursor, checked], Renderable]` so the same code drives both per-choice portrait previews (step 1) and global build-card previews (steps 2/3); `key_source` injection makes it deterministically testable without a TTY. `pipeline/setup.py` rewritten around a `WizardState` dataclass that owns name/display/tagline/based_on/portrait/bot_cfg/user_sources/bundled_skill_dirs and exposes `equipped_mcps()` / `equipped_skills()` / `card(mcps=, skills=, title=)` for the right-pane closures to call. Removed `_render_toggle_list` + `_parse_number_list` (the comma-separated-numbers UX is gone). `tests/test_setup.py` grew from 10 → 15: dropped the number-list test, added WizardState equipped-views, `_resolve_user_skill_names` for all three input shapes, `_reveal` portrait swap, picker `select_one` / `select_many` with injected `key_source`, and picker cancel-on-`q`. All 15 setup tests + chat-hardening suite green. Session 119's docs (handoff narrative for the original wizard ship) included in the same commit since they hadn't landed yet. Two open UX questions resolved live: text-input phases (custom name prompts, API-key prompts) drop the card briefly — picker phases are where the live vibe lives; step 1 stays as gallery-preview mode (the card debuts in step 2 — narrative beat). **Prior status: Phase 15.5.5 COMPLETE (session 119) — `brainchild setup` wizard shipped.** Five-step `rich` TUI in `pipeline/setup.py` (~500 lines): (1) fighter-select with two modes (from-scratch clones pm baseline and prompts for name/display/trigger/tagline/user-display, with collision re-prompt reserving `setup`/`list` and existing agent dirs; preset inherits name and operates in edit-in-place mode); (2) power-ups — numbered-list `[ ]`/`[✓]` toggle against each MCP block's `enabled` flag (originally `[x]` but Rich's markup parser swallowed it — switched to `[✓]`); (3) skills — user-added paths (folder with SKILL.md, parent of such folders, or single `.md` file wrapped in a stem-named folder as SKILL.md) plus bundled skills from the base bot as the same toggle pattern (all-checked default, physically copied into `agents/<name>/skills/`); (4) API keys — prompt for any `${VAR}` referenced by an enabled MCP that isn't already in `.env`, append-only (never overwrites); (5) atomic write via `tempfile.mkdtemp` + `os.rename` — edit-in-place seeds the tempdir from the existing bundle first so unknown files (`.env.example`, delegate scripts, hand-written README) survive the rewrite; `skills/` is fully re-authored; owned files (`config.yaml`, `portrait.txt`) are overwritten; README stub only written when absent. `__main__._run_setup` now dispatches to the wizard. `rich` added to `requirements.txt`. `tests/test_setup.py` — 10 cases all green (name validation + reserved names, MCP flip round-trip, three skill-copy shapes, atomic rollback on build failure, edit-in-place preservation, from-scratch write, env-append safety, number-list parser). Smoke-tested both modes end-to-end: from-scratch wrote clean `agents/smoketest/` bundle that imports under `BRAINCHILD_BOT=smoketest`; edit-in-place on designer diffed only `config.yaml` (comments stripped — known tradeoff, no ruamel dep) and preserved siblings, then restored via git. Edit-in-place preservation was a **live smoke-test finding**: initial implementation wholesale-replaced the target dir, wiping `.env.example` + hand-written README. Fix: seed the tempdir with the existing bundle before overwriting owned files. Also merged the `rename-roster-to-agents` branch (3 commits, fast-forward) into main at session start. Next: Phase 13 (polish) then 15.6 (security) then 14 → 15 → 16. **Prior: Phase 15.5.5 pre-step shipped (session 118 continuation) — MCP `enabled: true/false` refactor. Every bot's `config.yaml` now spells out every MCP it could plausibly use with an explicit `enabled` field; `config.py` filters `enabled: false` blocks at load time (zero behavioral change per-bot — smoke-verified across pm/engineer/designer). pm is the canonical from-scratch baseline (linear+github enabled, notion/slack/brave_search disabled, all with full hints/env/read_tools in place so the wizard can toggle them without re-authoring). engineer/designer preserve their current live MCPs (github+delegate / figma) and inherit pm's disabled blocks. Tests green (28/28 `test_mcp_client.py`, 9/9 `test_mcp_shutdown.py`). `docs/plan.md` now captures the pre-step + numbered-list `[x]`/`[ ]` toggle UX + pm-as-baseline decision. Next: build the wizard itself (`pipeline/setup.py`, five steps, ~4h). **Prior (session 118, earlier):** Directory rename `roster/` → `agents/` shipped. The session 109 rename (agents/ → roster/) is now reverted via `git mv` on a clean branch; identifier `_ROSTER_DIR` → `_AGENTS_DIR`, all path references and docstrings updated, prose collapsed `roster/agents member` and `roster/agents bot` to plain `agent`. Tests green; `brainchild list` still discovers all three. Branch `rename-roster-to-agents` (commit `83e3652`) ready to merge to main. Note: roadmap historical narrative below still references `roster/` — that's accurate to the time the work happened. Going forward (15.5.5 wizard, 15.6 contribution gate, 16.1 README) the directory IS `agents/`. **Prior status: Phase 15.5.4 COMPLETE (session 117) — face system + startup banner shipped and live-verified. Also fixed a tool-call correctness bug diagnosed at session start: `brainchild pm <url>` reruns against the same URL were echoing prior assistant replies instead of calling tools, because `MeetingRecord.tail()` returned all accumulated entries across reopens of the same slug. Fix: write a `{"kind": "session_start"}` boundary marker on every open; `tail()` now scopes to entries after the most recent marker. Raw JSONL preserves full audit. Tests green (`tests/test_chat_hardening.py` updated to reflect new semantics). 15.5.4 shipped in one session: `pipeline/face.py` deterministic glyph-face generator (~500 combos, 25 eyes × 20 mouths, sha256-seeded picks, hand-curated overrides for engineer/pm/designer matching the fighter-select reference mock); `tests/test_face.py` (9 cases); committed `roster/*/portrait.txt` per MVP bot; `agent.tagline` config field with punchy per-bot one-liners (engineer = "spins up Claude Code worktrees while you meet"; pm = "turns decisions into Linear tickets before you forget them"; designer = "opens your Figma and pulls no punches"); `_print_startup_banner()` in `__main__.py` = face + name + tagline + loadout (MCP names · N skills · model), fires FIRST before MCP/browser so it sits at the top of scrollback as a boot splash (initial attempt put it after MCP connects and it got buried in ~40 lines of startup logs — user caught it); `--plain` flag swaps to ASCII-safe frame + glyphs for screen readers / hostile terminals; first-run hook mints a portrait for any contributor-added bot. `_bot_tagline()` in `__main__.py` now prefers `config.yaml`'s `agent.tagline` over README fallback. Live-verified via `brainchild pm` — banner rendered correctly with the PM face (⊙⊙/‿‿), tagline, and `linear · github · 4 skills · claude-sonnet-4-5`. **Prior status (session 116) — Phase 15.5.3 LIVE-VALIDATED end-to-end; teardown hang fixed.** Smoke-test of `brainchild pm` surfaced a flaky 5–15s shutdown hang specific to the tool-call path (trial C: Linear `list_issues`/`create_issue` → reply → Ctrl+C). Stack dump showed the Playwright greenlet event loop wedged on `kqueue.control(None, ...)` inside `sync_playwright.__exit__` → `playwright.stop()` → greenlet `run_as_sync` — the Node driver's CDP socket is dead (Chrome self-exits in `--headless=new` after Leave click) but the greenlet is waiting for responses that never arrive. Removing the explicit `browser.close()` alone wasn't enough: the same wedge can happen in the context-manager exit. Fix landed in `connectors/macos_adapter.py`: new `_kill_playwright_drivers()` helper `pgrep -P <pid>`s for `playwright/driver/node ... cli.js run-driver` child processes; `leave()` now does `_browser_thread.join(timeout=2)` → if still alive, SIGKILL the driver directly → re-join 2s. Killing the driver unblocks the kqueue.select and lets the greenlet exit cleanly. Also fixed a secondary symptom: a stray `participant count changed 2 → 0` log appearing *after* `left meeting` — added `if self._stop_event.is_set(): break` guards in `ChatRunner._loop()` after `read_chat()` and `get_participant_count()` so a blocked call finishing during SIGINT handling doesn't slip through one more iteration. Verified live: 3 clean pm runs (happy path ~1s shutdown, no safety-net line) + one tool-call run that hit the wedge and recovered in 3.1s (`force-killed stuck Playwright driver pid=84330` → `left meeting` 4ms later, no outer Safety net line, no stray participant-count log). **Prior status (session 115) — Phase 15.5.3 LIVE-VALIDATED end-to-end.** Auto-admit confirmed working: `brainchild pm` → meet.new spins a fresh room → human knocks → tray-click admits → `participant count 1 → 2`. Two iterations to land it. **Iter 1:** added post-action "pill gone" verification; revealed that admission actually succeeded *but* Meet leaves the "Admit N guest" pill widget sticky in the DOM (visible, not hidden) even after the guest is admitted, so a "wait for pill to disappear" verifier loops forever and a keyboard-fallback path was logging false-positive "admitted" lines every 2s. **Iter 2 (the fix):** swapped verification from pill-disappearance to **participant-count delta** (poll `_do_get_participant_count` for up to 3s after click; success = count went up). Added a `(pill_text, participant_count)` cooldown so a stale pill silently skips re-attempts until either text or count changes. Result: clean log of `admit pill detected text='Admit 1 guest' count=1` → `admit_btn count=1 visible=True` → `admitted via tray click (verified — participant count 1 → 2)` and then silence — no spam. New diagnostic snapshot `debug/post_admit_success.{png,html}` saved on verified admission. Bonus: graceful shutdown clean too (Leave call → browser closed in <1s, no 10s timeout). **Prior status (session 114) — Phase 15.5.3 partially LIVE-VALIDATED.** Walked `brainchild pm` from cold start through meet.new → user admission scaffolding. Positional CLI works, meet.new redirect + slug capture works, URL-path validator (`_is_real_meet_room`) correctly blocks the `/new?authuser=0&hs=178` interstitial from being treated as a real room. Bot runs headless in Chrome as host of the meet.new-spun room; `webbrowser.open(resolved_url)` pops a visible tab so the human can join the same meeting. Auto-admission logic in `macos_adapter.py` hold loop: find the "Admit N guest(s)" pill by text → `pill.hover()` (real CDP mouse move, since synthetic MouseEvent dispatch doesn't trigger Google's hover-tray) → click `role=button name="Admit"` in the revealed tray → keyboard fallback (focus + ArrowDown + Enter). **NOT yet live-confirmed: human user could not be admitted end-to-end.** Previous iteration over-clicked ancestor containers via `:has-text()` (matches parents whose subtree contains the text); current iteration uses accessible-name match and tray-hover flow but is untested. Also this session: `./brainchild` → `brainchild` scrubbed across all docs/tests, handoff from 113. Pick up from admission debug — `debug/admit_diagnostic.{png,html}` will capture the tray DOM on next run, and we've already confirmed (via a console DOM dump from the user's own visible Meet) that the pill wraps nested divs with no inner role=button UNTIL hovered; the question is whether Playwright's real hover actually opens the tray inside headless Chrome. Budget: ~23h remaining vs April 19 launch. 15.5.4 (face system) + 15.5.5 (wizard) ~8h; 13 / 15.6 / 14 / 15 / 16 after that. **Prior status (session 113) — Phase 15.5.3 code complete, 15.5.2b + 15.5.2c live-validated (session 112). Walked `tests/15_5_2b_testing.md` against fresh Meet `cyv-utza-jxp`. Official Figma MCP via `mcp-remote` rejected at T1 with `HTTP 403 Forbidden` during OAuth dynamic client registration (`registerClient`) — the "MCP catalog" gate turned out to be hard, not soft. Fell back to GLips (`figma-developer-mcp`, PAT-authenticated, read-only) and re-ran. T1 ✓ (2 tools, 4.2s→0.9s on second start), T2 ✓ (captions stream to JSONL, Brainchild silent on non-addressed speech), T3 ✓ (auto-exec on GLips `get_figma_data`, anti-hallucination guard live-demonstrated — Designer iterated three fetches on a URL that turned out to be a single-RECTANGLE image, refused to fabricate card-level critique, then produced grounded "5 cards × 386×512px, 40px gap too tight, increase to 64–80px" critique when pointed at Plant-shop-curved-carousel from Figma Community). T4 N/A (GLips read-only). T5 ✗ first-fire (Sonnet 4.5 loaded `design-review-feedback` skill on casual "gut check" prompt, produced structured output where conversational 1-3 sentence was wanted) but ✓ on redirect (user's "that was long, brief thoughts?" prompt produced perfect 1-sentence conversational register — persona can do both registers, trigger sensitivity is the gap). T6 ✓ (structured critique with What works / What breaks / Suggestions, numeric specifics, file URL appended per skill). T7 ✓ graceful shutdown, safety-net caught one orphaned `figma-developer-mcp` grandchild at Ctrl+C (MCP client doesn't directly terminate `npx`-spawned subprocesses — resolves itself, pgrep clean after a few seconds). **GLips promoted to bundle default** — `roster/designer/config.yaml` swapped (FIGMA_TOKEN env var mapping, 2 read_tools, pruned write-specific hints, added anti-hallucination hint); `roster/designer/.env.example` flipped to require `FIGMA_TOKEN`; `roster/designer/README.md` rewrote auth story + added "Which Figma MCP server?" section documenting GLips default, Grab's `cursor-talk-to-figma-mcp` as write-enabled power-up (plugin + `bun socket` bridge, only operates on file open in plugin), and Figma's official server as catalog-gated-future-option; `tests/15_5_2b_testing.md` restructured (Prep requires FIGMA_TOKEN, T1 expects silent PAT start + 2 tools, T3 expects `figma__get_figma_data` explicitly + anti-hallucination guard documented as expected pass behavior on thin fixtures, T4 marked N/A, Fallback section → Power-ups with both Grab and official-server configs). Bundle ship-ready. Execution order: 12 ✅ → 15.5 ▶ (15.5.0 ✅, 15.5.1 ✅, 15.5.2 ✅, 15.5.2b ✅, 15.5.2c ✅, 15.5.3 ⬜, 15.5.4 ⬜) → 13 → 15.6 → 14 → 15 → 16. Next: 15.5.3 (brainchild setup wizard, ~2h). MVP ship target still April 19, 2026.** **Session 110 (April 15)** — Phase 15.5.2 LIVE-VALIDATED. Built `roster/pm/` — complete self-contained bundle (config.yaml, README.md, .env.example, two bundled SKILL.md skills) patterned on `roster/engineer/`. Linear MCP active as the "one killer MCP" + GitHub active for repo access; Notion/Slack/Brave commented as power-ups. Captions on by default (PM's value depends on hearing commitments + blockers). `max_tokens: 400` (bumped from 150 — PM writes structured summaries + PRD drafts). Two bundled skills: `prd-from-discussion` (Problem/User/Goal/Scope/Non-goals/Open questions structure, marks sections "not stated in discussion" rather than hallucinating) and `standup-summary` (Decisions/Action items/Blockers/Tickets filed structure, empty sections omitted, only lists tickets THIS session filed). `skills.paths: [~/.claude/skills, roster/pm/skills]` — user skills first, bundle second so bundled skills win on name collisions (last-wins per skills.py:138). Authored 7-test plan at `tests/15_5_2_testing.md` and walked it against a real Meet (tfb-tpnb-kpw). All 7 passed: startup (Linear 31 tools + GitHub 41 + 4 skills), captions streaming as [spoken] without triggering replies, Linear read auto-exec, Linear write (save_issue) confirmation gate → execute → bare URL in reply (MOJ-18 filed), standup-summary skill via load_skill (structured output, pulled Figma blocker from earlier captions into the summary), prd-from-discussion skill (structured output, "Owner / next step — not stated in discussion" rule honored), GitHub MCP co-exists (startup get_me call validated end-to-end). Bonus behavior validated: PM's "ask rather than guess" system-prompt rule fired live in T4 ("which team should I use?" instead of hallucinating). Execution order: 12 ✅ → 15.5 ▶ (15.5.0 ✅, 15.5.1 ✅, 15.5.2 ✅, 15.5.2b ⬜, 15.5.3 ⬜, 15.5.4 ⬜) → 13 → 15.6 → 14 → 15 → 16. Next: Phase 15.5.2b (`roster/designer/`, ~1h). MVP ship target still April 19, 2026.** **Session 109 (April 15)** — Phase 15.5.1 LIVE-VALIDATED. Walked 7-test suite in `tests/15_5_1_testing.md` against a real Meet — MCP startup, delegate tool discovery, confirmation gate, approve→heartbeat→summary loop, GH co-residence, empty-task guard, and clean shutdown all passed. Real `claude -p --worktree` delegation in T4: 10 Python files counted, 5.4s, $0.069, heartbeat fired exactly on schedule at 8s. One observation: `claude --worktree` leaves worktrees + branches behind after delegation; documented prune steps in `roster/engineer/README.md`. One nuance: the LLM paraphrases duration/cost footer out of chat by default — not a ship blocker. Test plan corrected mid-run (footer-in-chat was over-spec). Bundle ship-ready. Phases 15.5.0 + 15.5.1 complete. `agents/` renamed to `roster/` on disk with all references updated in roster README, config.yaml, docs/roadmap.md, docs/agent-context.md, docs/mvp-bar.md. `roster/engineer/` built (was planned as `coder`, renamed late-session): complete `config.yaml` (Anthropic + GitHub MCP + bundled delegate MCP + Linear commented as power-up), `delegate_to_claude_code.py` mini MCP server (~130 lines — handles missing CLI, subprocess failure, JSON parse failure, claude-reported errors; returns result + duration/cost), `README.md` (5-section format + worktree cleanup section added session 109), `.env.example`. `tool_timeout_seconds` bumped to 600 in engineer config only (Claude Code tasks run 3–10 min; heartbeat keeps chat informed). Execution order: 12 ✅ → 15.5 ▶ (15.5.0 ✅, 15.5.1 ✅, 15.5.2 ⬜, 15.5.2b ⬜, 15.5.3 ⬜, 15.5.4 ⬜) → 13 → 15.6 → 14 → 15 → 16. Next: Phase 15.5.2 (`roster/pm/`, ~1.5h). MVP ship target still April 19, 2026.** **Session 107 (April 15)** — Phase 12 completed (~1h vs. ~3h estimated — gap covered by 11.x testing). OpenAI provider spot-checked live: basic chat, read-only MCP auto-exec, write confirmation + correction flow, tool chaining — all passed. Config reverted to Anthropic/Claude Sonnet 4.5. Planning for Phase 15.5: expanded 15.5 from 2 to 3 roster members (coder + PM + designer), renamed `agents/` → `roster/` in the plan, added `delegate_to_claude_code` mini MCP server for coder, added Figma MCP for designer. **Session 106 (April 15)** — planning session. User rattled off four ideas to assess against `docs/mvp-bar.md`: (1) configurable participation scalar — agreed post-MVP, queued under new "Behavior & Personas" post-MVP block. (2) auto-launch with `meet.new` — agreed pre-MVP (15.5.4, ~1h), removes the on-camera friction step in demos. (3) security audit covering both runtime prompt-injection + write-tool surface and `agents/` contribution gate — agreed pre-MVP and added as Phase 15.6 (~4.5h split across runtime audit + `agents/CONTRIBUTING.md` + CODEOWNERS); the user's flywheel argument (security gates contribution gates adoption) carried weight. (4) launch agents — committed `standup-pm` as the second MVP agent (was TBD), pairs with `claude-code` to cover dev+PM and read+write personas, directly serves hero framing #2. Other agents (debriefer, spec-scribe, incident-commander, customer-call, research) queued as week-1 seeded PRs; heavier ones (claude-code-bash, scraper, computer-use) deferred pending security pass. Also added Tier-1 landing page (16.3) — static site with embedded demo + Calendly fallback; explicitly *not* the Tier-3 hosted-bot route which would lean into Joinly's lane. Resequenced phases after user prompt: original order had 15.5 *after* 15 (cross-platform tested before new product surface added) and 13 polish *before* 15.5 (audited config before adding new keys); fixed by moving 15.5 to right after 12, then 13 → 15.6 → 14 → 15 → 16 so polish/security/package/cross-platform all run against final code. Renumbered within 15.5 (standup-pm before wizard so the picker has both fighters at write-time) and within 16 (landing page before campaign so posts can link the URL). User committed to launch date with no triage; "Do not cut 15.6.1" called out in MVP-total footer (OpenClaw-precedent item, hard to retrofit). **Session 105 (April 15)** — tiny code session followed by architectural discussion. Phase 11.7 shipped: changed `OPENAI_API_KEY = os.environ["OPENAI_API_KEY"]` → `.get(..., "")` and added a symmetric `if not config.OPENAI_API_KEY: raise RuntimeError(...)` guard inside `build_provider()`'s openai branch (matching the existing anthropic pattern). Verified both paths: `OPENAI_API_KEY= python -c "import config"` imports cleanly; `build_provider()` with `llm.provider: anthropic` returns `AnthropicProvider` OK; forcing `llm.provider: openai` with empty key raises the expected RuntimeError. Committed as `d756962`. Rest of session was exploratory discussion (no code): how to enable bash/code execution for Brainchild (bash-MCP vs skill-as-CLI-recipe); why bash-MCPs have low adoption (security/sandboxing is the hard part, not the server); clarifying that Phase 15.5.1 is a *gallery entry* (`agents/claude-code/` preconfigured starter), **not** a Claude-Code-as-subprocess integration — user correctly pushed back on my earlier mischaracterization; how to architect modular sub-agent spawning (Codex/Claude Code/scrapers) — recommended modeling as MCP tools with spawn/status/result, defaulting to async-detached, delivering artifacts around Brainchild (Linear/GitHub) rather than through it; and where CLI-as-skill fits (good for breadth + fast authoring, but loses per-tool confirm gating — additive, post-MVP). All architectural threads deferred to post-MVP; nothing blocks 15.5.1. **Session 104 (April 14)** — walked `docs/11_4_testing.md` T1–T9 end-to-end. All passed. T5 (the critical behavioral check) was clean — three chit-chat turns produced zero `load_skill` tool_calls; menu wording is correctly non-tempting. T6 only fired the error-path after a second, more forceful prompt — the model's first instinct was to reason from the menu and reply directly, which is actually ideal UX; the raw error path exists and works when forced. Bonus: `tools=72` vs `tools=73` in progressive vs full-body mode confirmed `load_skill` gating. Shutdown summary `turns=N load_skill_calls=M by_name={...}` emitted correctly with slash invocations counted. Cleanup done: `/tmp/op-skills` removed, `config.yaml skills.paths` reverted to `[]`. **Session 103 (April 14)** — implemented Phase 11.4 in one pass. New `pipeline/skills.py` loads SKILL.md folders or parent dirs (one level deep), parses frontmatter, WARNs + skips on any error, resolves duplicates last-wins. `LLMClient` gains `inject_skills(skills, progressive)` — menu (name + description) when progressive-disclosure is on, full bodies inlined when off. `ask()` picked up an `extra_system` one-shot kwarg so slash-invocation can thread the skill body through without duplicating the user turn or mutating the persistent system prompt. `ChatRunner` accepts `skills` + `skills_progressive`, exposes a synthetic `load_skill` tool alongside MCP tools (progressive only), routes `load_skill` tool calls locally (auto-exec, no confirmation), and has a slash-invocation fast path (`/<name>` after trigger strip) that bypasses the round-trip. SKILLS logging: per-turn `SKILLS turn=N ...` lines for every slash-invoke / load_skill call / unknown slash, plus a session summary `SKILLS session summary: turns=N load_skill_calls=M by_name={...}` on `stop()`. `__main__` wires the loader into both macOS and Linux paths before `inject_mcp_hints/status`. New `tests/test_skills.py` with 13 cases — loader happy path, parent-dir scan, missing path, malformed YAML, missing fields, last-wins, deep nesting ignored, allowed-tools WARN, LLM menu vs full-body injection, `load_skill` tool gating, call-rate sanity (no load_skill on chit-chat), slash-invocation counters. Existing chat/hardening/timeout/guardrail tests all still green. Live-test plan written at `docs/11_4_testing.md` (9 tests using unique-token skills to prove bodies actually reach the model). **Session 102 (April 14)** — planning-only session. **Session 102 (April 14)** — planning-only session. Reviewed 11.4 (skill loading) and 11.5 (Claude Code skill import) together; **merged 11.5 into 11.4** after realizing the simpler path is to adopt Claude Code's `SKILL.md` folder format + progressive disclosure + `allowed-tools` filtering up front instead of shipping a flat-`.md` loader first and rewriting. Phase now ~3h, was 11.4 ~2h + 11.5 ~2h. Key design decisions: progressive disclosure via a synthetic `load_skill(name)` tool registered alongside MCP tools (our translation of Claude Code's framework-driven skill injection, which we can't replicate); toggleable via `skills.progressive_disclosure` config; slash-invocation (`@brainchild /commit this`) always on as a fast-path shortcut bypassing the round-trip; name collisions resolved last-wins + WARN (Claude Code does project > user, which matches when users order `skills:` broader → more specific). User flagged concern about the model over-calling `load_skill` on unrelated requests; plan includes dedicated `SKILLS turn=N ...` logging and a call-rate sanity test on canned inputs to monitor on first real runs. Handoff written with full task breakdown at `docs/handoff.md`. **Session 101 (April 14)** — walked the captions live-test plan against a real Meet (`fqx-xndc-odd`). Found three issues, fixed all three, retested clean. (1) `LLMClient` and the captions path were holding two different `MeetingRecord` instances because `__main__.py` constructed the LLM before the record existed and never wired them — every `@brainchild` call returned 400 `messages: at least one message is required` whenever captions were enabled. One-line fix: `llm.set_record(meeting_record)` after the record is created. (2) Same-speaker continuations were persisted as "prior + new" because Meet keeps the prior caption text visible in the same speaker's region; ported the `_normalize_for_match` token-prefix idea from `voice-preserved/pipeline/captions.py` into a new `_strip_prior_prefix` helper that's tolerant of Meet's casing/punctuation rewrites between finalizes. (3) `caption_finalized` log line was truncating text at 80 chars (`text[:80]`), making it look like the JSONL was getting wrong text — purely cosmetic, removed the slice. Added `tests/test_transcript_dedupe.py` with 7 cases. Re-walked T1/T2/T6/T9 in a fresh meeting; all pass. Captions flipped back to default-off. Phase 11.3b complete. Next up: Phase 11.4 (skill loading, ~2h). **Session 100 (April 14)** — landed the late-bind caption fix: `MacOSAdapter` now exposes `window.__onCaption` and injects the observer at browser startup whenever `CAPTIONS_ENABLED` is true, independent of whether a callback is registered. `set_caption_callback()` is now safe to call before OR after `join()`, so calendar-polling mode can wire a per-meeting `MeetingRecord` + `TranscriptFinalizer` once a URL arrives via the calendar queue without a browser restart. Added `tests/test_caption_late_bind.py` with 7 cases (drop-with-no-callback, register/receive, late-bind, swap routes, None unregisters, exception swallowed without breaking bridge, `filter_caption` short-circuit) — all pass; full chat-only suite still green. Per-meeting calendar wiring itself still needs `ChatRunner.run_polling()` (referenced from `__main__.py:304` but not present on chat-only main — voice-era leftover). **Session 99 (April 14)** — ported the Google Meet caption observer from `voice-preserved` into main: new `connectors/captions_js.py` holds the verbatim MutationObserver JS + enable/filter helpers; `MacOSAdapter` gains `set_caption_callback()` and the JS bridge; new `pipeline/transcript.py` buffers deltas and flushes finalized utterances on speaker-change or silence gap; `MeetingRecord` gets `kind: caption` entries interleaved with chat by timestamp; `LLMClient._tail_messages` renders them as `[spoken] <name>: <text>` so the model reads them as ambient room talk, not prompts to respond. Gated behind `transcript.captions_enabled: false` (privacy default). Unit-tested the finalizer + interleaving; all existing tests still green. Test plan written at `docs/11_3_b_testing.md`. **Known limitations to address post-live-test:** (a) calendar-mode captions need a separate wiring pass (currently only direct-URL path wires captions), (b) no automated regression test for observer drift if Meet's caption DOM shifts. **Session 98 (April 14)** — Phase 11.3a COMPLETE and live-Meet validated. MVP ship target still April 19, 2026.** **Session 98 (April 14)** — walked the full `docs/11_3_a_testing.md` sheet against a fresh meeting (`bxb-jytq-tmc`). All 12 tests passed (Test 5 skipped per doc; Test 3 config-driven proof skipped). Confirm flow + correction re-propose works (`save_issue` `demo` → MOJ-16; `foo`→`gloo` correction → MOJ-17). Tool-loop scratchpad never persists (zero `tool_use`/`tool_result` lines on disk). Alone-exit grace fired at 61s. Two minor non-blockers noted: one DOM-poll missed a bare `ok` confirmation (recovered on retry), and the LLM occasionally needs a nudge to surface non-addressed context. Closed the session by auditing `CLAUDE.md` against the post-11.3a codebase — fixed three stale sections (architecture overview missing `meeting_record.py`, data-flow describing pre-11.3a "rolling history", config block listing wrong fields under `agent`/`llm`/`connector`). **Session 97 (April 14)** — positioning session after competitive research surfaced [joinly](https://github.com/joinly-ai/joinly) as a near-peer (multi-platform, voice, MCP, BYOLLM, MIT, hosted cloud, shipped 6+ months ago). Response: lean into **"Claude Code in your Google Meet"** as the launch framing and **opinionated quickstart** as the implicit differentiator. Added new **Phase 15.5 (Opinionated Quickstart)** — agents gallery + minimal `brainchild setup` wizard + a second chat-native agent slot. `agents/` directory created with format `README.md`; `claude-code` is the canonical starter. `docs/mvp-bar.md` updated with sharpened 3-part differentiation and a new Launch Strategy section (hero framings, visual hooks, distribution ranking, gallery as distribution primitive). Session 96 shipped 11.3a — per-meeting JSONL at `~/.brainchild/history/<slug>.jsonl` as the single source of truth for chat history. Phase 11 remaining: 11.3b (captions, ~3h), 11.4 (skill loading, ~2h), 11.5 (Claude Code skill import, ~2h), 11.7 (provider keys optional, ~15m). 11.6 absorbed into 11.3a. After Phase 11: Phase 12 validation, 13 polish, 14 package, 15 cross-platform testing, 15.5 opinionated quickstart (new), 16 launch. Fast-follow audit items remain opportunistic. Post-MVP: Hosted Brainchild (~30-35h). Zero-telemetry intact.

---

## Completed Phases

<details>
<summary>Phases 1–6 + Caption Refactor + MCP Integration (all ✅)</summary>

### Phase 1: Extract the Agent Pipeline ✅
Pulled all "brain" logic out of `app.py` into `pipeline/` with zero macOS-specific code.

### Phase 2: Define the Connector Interface ✅
Defined `MeetingConnector` abstract interface. Wrapped macOS logic as `MacOSAdapter`.

### Phase 3: Docker/Cloud Adapter ✅
Built headless Linux adapter in Docker. Verified end-to-end in live Google Meet.

### Phase 4: Reorient — Cloud Cleanup + Linux Local Adapter ✅
Moved cloud artifacts into `cloud/`. Adapted Docker adapter into `LinuxAdapter` for local Linux machines.

### Phase 5: Config System ✅
All hardcoded constants moved into `config.yaml` with `config.py` reader.

### Phase 6: Consolidate Entry Points ✅
Shared loop extracted into `pipeline/runner.py`. OS auto-detection via `python -m brainchild`. Calendar poller replaced `calendar_join.py`.

### Caption Refactor ✅
Replaced ScreenCaptureKit + Whisper with Google Meet DOM caption scraping. Eliminates echo problem, privacy issues, and Whisper dependency on macOS.

### Phase 11 (original): MCP Integration ✅
MCP client connects to configured servers at startup, discovers tools. Tool-call loop in LLMClient handles tool_call → execute → result → re-prompt. Chat-specific LLM settings. Config schema for `mcp_servers`. Validated with Linear and GitHub MCP servers end-to-end in live Meet. GitHub MCP server upgraded from deprecated npm package to official Go binary (`github/github-mcp-server` v0.32.0).

### Phase 7: Performance Iteration ✅ (partial)
Audio quality, TTS 3-tier architecture, latency masking, STT accuracy (mlx-whisper), streaming classification, playback interrupt classification, latency docs. Deferred to voice phase: TTS error handling/retry, premature finalization at 0.7s silence threshold.

</details>

---

## Phase 8: Chat-First MVP (THE CURRENT FOCUS)

*The pivot. Ship a chat-based task delegation bot as v1. Voice layers on top later.*

**Why:** A real user (engineer at a remote company) wants to delegate tasks to the bot via Google Chat during meetings. Chat I/O is simpler than voice — no wake detection, no latency tuning, no TTS. Ship something, get feedback, expand from there.

**Scope:** Google Meet only. Mac + Linux (Playwright is cross-platform, so Linux support is essentially free for chat-based interaction). Zoom/Teams deferred until there's confirmed user demand — each platform requires its own DOM selectors, join flow, and auth story.

**Core reframe:** Voice and chat are interaction layers over a shared capability layer. The audio pipeline stays in the codebase untouched.

| Step | Description | Status |
|------|-------------|--------|
| 8.0 | Clean the house — reorganize root, consolidate docs | ✅ |
| 8.1 | Chat I/O proof of concept — bot reads and writes Google Chat messages during a live meeting (echo test, no LLM). Create `ChatRunner` alongside `AgentRunner` — same `LLMClient`, simpler I/O loop. No codebase reorg needed; existing connector/pipeline separation already fits. | ✅ echo test passing e2e |
| 8.2 | Wire up the brain — connect chat input to LLM, respond in chat | ✅ |
| 8.2.1 | Chat hardening — history cap (configurable), wake phrase gating for multi-participant, sender field extraction from DOM | ✅ |
| 8.2.2 | Meeting lifecycle — pre-join user gate, end-time auto-leave, stale meeting skip, Ctrl+C clean shutdown, alone-exit auto-leave (session 86: after others were present, drop count→1 for 60s triggers leave; chat path tested live, voice path mirrored untested) | ✅ |
| 8.3 | Ship to friend — minimal setup, clear instructions, get it in his hands | ✅ | ~2h |

---

## Phase 9: Hardening & Reliability

*Make what we have bulletproof before shipping to strangers.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 9.1 | UI dependency audit — inventory every DOM selector and UI interaction; classify as stable (API-backed) vs. fragile (class names, layout-dependent) | ✅ | ~2h |
| 9.2 | ~~DOM regression test suite~~ — deferred to Phase 12 (post-MVP maintenance tooling) | ⏭️ | — |
| 9.3 | ~~Self-healing selectors~~ — deferred to Phase 12 (follows regression suite) | ⏭️ | — |
| 9.4 | Race condition audit — systematic review of threading, queue interactions, shutdown paths, and browser thread coordination | ✅ | ~3h |
| 9.5 | Security vulnerability audit — input sanitization, credential handling, MCP server sandboxing, dependency audit | ✅ | ~2h |
| 9.6 | Simultaneous meeting handling — single-meeting design: queue overlaps, skip ended meetings, log warnings | ✅ | ~1h |
| 9.7 | Calendar polling startup latency — profile and optimize the slow path from launch to first meeting join | ✅ | ~1h |
| 9.8 | ~~Log cleanup~~ — deferred to Phase 12 (post-MVP polish) | ⏭️ | — |
| 9.9 | ~~Latency audit~~ — deferred to Phase 12 (post-MVP polish) | ⏭️ | — |
| 9.10 | ~~Comprehensive error handling pass~~ — deferred to Phase 12 (post-MVP polish) | ⏭️ | — |
| 9.11 | Chat message size management — investigate Google Meet chat character limits, truncate/summarize long tool results, fix overly verbose Brainchild responses | ✅ | ~2h |
| 9.12 | Tool call timeout + heartbeat — visible "still working..." in chat for long-running calls, hard timeout with graceful failure | ✅ | ~2h |
| 9.13 | Context window management — strategy for summarizing/truncating older chat history as conversation grows, prevent silent context overflow | ✅ | ~3h |
| 9.14 | ~~Idempotency guards~~ — deferred to Phase 12 | ⏭️ | — |
| 9.15 | Offline/reconnection behavior — detect browser crash/page loss via `is_connected()` on connector; ChatRunner exits loop cleanly; health check tightened to 30s with `page.is_closed()` detection. Decision: exit cleanly (no auto-rejoin). | ✅ | ~2h |
| 9.16 | ~~Edge case pass~~ — deferred to Phase 12 | ⏭️ | — |

---

## Phase 10: MCP Finalization & Hardening

*Finalize MCP as a first-class capability. Trimmed for MVP — see Post-MVP for deferred items.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 10.1 | Per-MCP `hints` field in config — server-specific LLM guidance injected into system prompt | ✅ | ~1h |
| 10.7 | Per-server hints content — Linear + GitHub hints filled, validated via 12-test suite, then audited and trimmed. Removed over-directing hints (list_issues filter mandate, preemptive size check, search_issues preference, project summary follow-up). Added status-recovery hint (project vs team confusion). Revised search_code hint. Confirmation handler rewritten: word-boundary matching for affirmatives only, all non-affirmative responses pass user's message to LLM for interpretation. | ✅ | ~4h |
| 10.4 | BYOMCP guard rails — execution timeouts, result size caps with truncation + guidance message ("Result was too large — add a hint to use more targeted queries"), binary/non-text content detection and rejection (prevents the image-poisoning session-brick from GitHub G7), `confirm-all` default for untested servers, verbose MCP debug logging to `/tmp/brainchild.log` (full request/response/rejection reason so users can self-diagnose and write hints). Covers 3 critical code-change findings: G7 binary poison, G6 large file context blow, L4 unfiltered list_issues | ✅ | ~4h |
| 10.2 | Tool confirmation — auto-approve reads, confirm writes. Hardcoded `READ_TOOLS` allowlist, per-server `confirm_tools` override in config, centralized `_dispatch_result` routing | ✅ | ~2h |
| 10.3 | User-defined MCP servers — users add custom servers in config with command, args, env, hints. Config-load env var warnings, categorized startup failure messages, LLM-aware loaded/failed server status, `--check-mcp` validation CLI | ✅ | ~2h |
| 10.5 | Startup health check — on launch, start each configured MCP server, call `list_tools()`, report failures before joining a meeting. No CI job, no dry-run tool calls — just "can we connect?" | ✅ (via 10.3 `--check-mcp`) | ~1h |
| 10.6 | Runtime failure backoff — per-server consecutive error counter on MCPClient; after 3 failures a server is disabled (tools filtered from `get_openai_tools`, `execute_tool` short-circuits with LLM-facing steering text). One-shot chat announcement + idempotent `inject_mcp_status` re-inject with a new `disabled_runtime` bucket. Timeouts count as failures via `_record_mcp_outcome` in chat_runner. Unit-tested + live-validated with `debug/flaky_mcp_server.py` | ✅ | ~1h |

**Phase total: ~12h**

---

## Phase 11: Multi-Model & Customization

*Break the OpenAI lock-in and deliver the "your AI, not a generic bot" half of the MVP positioning. Voice deferred — see Post-MVP.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 11.1 | Abstract LLM provider interface — swap between OpenAI and Anthropic without code changes | ✅ | ~3h |
| 11.2 | Anthropic API backend — Claude as alternative LLM provider | ✅ | ~3h |
| 11.3a | Meeting record as single source of truth — every chat-panel message (addressed, non-addressed, Brainchild's own replies) appends to a per-meeting JSONL file at `~/.brainchild/history/<meeting_id>.jsonl`. `LLMClient.ask()` reads the tail of that file and replays it as context on each call; no parallel in-memory pair buffer competing with it. Absorbs what was 11.6 (persistence) — the file IS the record. Tool-call/tool-result state stays in a small in-memory scratchpad for the in-flight turn only. `history_turns` renamed to `history_messages`. Meta header line in the JSONL (`kind: meta`, url + slug + timestamp) makes each file self-describing. Config-driven first-contact greeting via `agent.first_contact_hint` with `{first_name}` placeholder. Unit-tested end-to-end; live-test sheet at `docs/11_3_a_testing.md`. | ✅ | ~4h |
| 11.3b | Captions as transcript source — port the Google Meet live-captions DOM scraper from the `voice-preserved` branch (`CaptionsAdapter` / `pipeline/captions.py`) into a new `pipeline/transcript.py`. Captions append to the same meeting-record JSONL with `kind: caption`, unified with chat messages. Gate behind a `transcript.captions_enabled` config flag (default off — user opt-in for privacy). Degrade gracefully when captions aren't turned on in the meeting. | ✅ live-validated session 101 (T1/T2/T6/T9 PASS in `fqx-xndc-odd`) | ~3h |
| 11.4 | Claude Code skill loading (11.5 folded in, session 102) — `config.yaml` gains a `skills:` list of paths. Each entry is either a single skill folder (`<name>/SKILL.md`) or a parent dir scanned one level deep for `*/SKILL.md`. On startup, resolve paths, parse YAML frontmatter (`name`, `description`, optional `allowed-tools`). **Progressive disclosure:** inject only `name + description` into the system prompt as an available-skills list; register a synthetic `load_skill(name)` tool alongside MCP tools so the model can pull a skill body on demand. `allowed-tools` filtering: WARN on tools Brainchild can't honor (`Bash`/`Edit`/`Write`/`Read`), still load the skill so the model can decline. Missing path → WARN + skip (no crash). Malformed frontmatter → WARN + skip. Duplicate names → last-wins + WARN. Startup banner: `SKILLS: N/M loaded (<name> ✓, …)`. Pitched as "Claude Code in Google Meet." See `docs/agent-context.md`. | ✅ live-validated session 104 (T1–T9 all PASS in fresh Meet) | ~3h |
| 11.5 | ~~Claude Code skill interop~~ — **merged into 11.4 (session 102).** Adopting `SKILL.md` shape + progressive disclosure up front was simpler than splitting. `skills.local.yaml` personal-override mechanism deferred to post-MVP (not MVP-critical). | ✅ (by absorption) | — |
| 11.6 | ~~Conversation history persistence~~ — **absorbed into 11.3a.** The meeting record file IS the persistence layer; no separate step needed. | ✅ (by absorption) | — |
| 11.7 | Provider API keys optional at import (L1-c audit) — `config.py:79` now uses `.get(..., "")` for both `OPENAI_API_KEY` and `ANTHROPIC_API_KEY`; `build_provider()` (pipeline/providers/__init__.py) raises `RuntimeError` if the configured provider's key is missing. Anthropic-only and `--check-mcp` runs no longer KeyError on import. | ✅ | ~15m |

**Phase total: ~13.25h** (11.3 split into 11.3a/11.3b with 11.6 absorbed; 11.5 folded into 11.4 at ~3h total)

---

## Phase 12: Validation

*Quick validation pass across both LLM providers. Not a full test matrix. Closes out Phase 11 work before adding more product surface.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 12.1 | Model validation — test core chat + MCP flow with GPT-4.1-mini and Claude (one model per provider). Verify tool calls, confirmations, error handling | ✅ | ~0.5h |
| 12.2 | MCP cross-provider validation — verify Linear + GitHub tools work correctly with both OpenAI and Anthropic. Focus on tool call format differences between providers | ✅ | ~0.5h |

**Phase total: ~3h**

---

## Phase 15.5: Opinionated Quickstart

*The "choose your fighter → add power-ups → go" layer. Ships a three-member roster — an execution team (engineer, PM, designer) that delivers baseline value out of the box and can be personalized with power-ups. Implicit differentiator vs. joinly's framework-shaped positioning. See `docs/mvp-bar.md` for strategy context. **Sequenced before Phase 13 (session 105):** finishes building all new product surface so polish, security, and cross-platform testing run against the final code. Directory is `agents/` (briefly renamed `roster/` in session 107, reverted in session 118 — the gaming "roster / fighter-select / power-ups" metaphor still drives UX framing; row descriptions below reference `roster/` where that was the folder name at time of completion).*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.5.0 | Rename `agents/` → `roster/` — rename directory, update `roster/README.md` references (available-roster-members table, contributing section, folder layout examples), update `config.yaml` comment references, update `docs/roadmap.md` / `docs/agent-context.md` / `docs/mvp-bar.md` references. Update `CLAUDE.md` if it references agents/. | ✅ | ~15m |
| 15.5.1 | `roster/engineer/` — engineering assistant. **MCP servers:** GitHub (battle-tested). **Bundled mini MCP server:** `delegate_to_claude_code` (~130 lines Python) — single tool that runs `claude -p "<task>" --worktree --output-format json` as a subprocess, returns result (branch, PR link, summary). Confirmation-gated as a write tool. Security is Claude Code's responsibility (own permission model + worktree isolation). Prerequisite: `claude` CLI installed. **Skills:** loads user's `~/.claude/skills/` by default. **System prompt:** engineering persona. **README:** documents GitHub MCP + Claude Code delegation + power-up path (shell MCP for direct execution, with security warning). `.env.example`: `ANTHROPIC_API_KEY`, `GITHUB_TOKEN`. Tool discovery verified; no live Meet test yet. | ✅ | ~2h |
| 15.5.2 | `roster/pm/` — PM / standup assistant. The admin, strategist, cognitive partner. **MCP servers:** Linear (battle-tested, the "one killer MCP" — file/query/update tickets from spoken commitments). GitHub for repo access. **Config:** `captions_enabled: true` by default (hero framing: "filed 3 Linear tickets before I finished talking" depends on hearing speech). **Skills:** PRD-from-discussion skill (templatize thought vomit into structured PRD). **System prompt:** PM persona — structured thinker, action-item extractor, blocker tracker. Posts structured standup summary at meeting end. **README:** documents Linear + GitHub + power-up path (Notion, Slack, Brave Search — all commented-out in root config already). `.env.example`: `ANTHROPIC_API_KEY`, `GITHUB_TOKEN`. | ✅ | ~1.5h |
| 15.5.2b | `roster/designer/` — creative / design review partner. **MCP server (default, session 112):** GLips `figma-developer-mcp` (community, PAT-authenticated, read-only — `get_figma_data` + `download_figma_images`). Chosen after Figma's **official** `mcp.figma.com/mcp` rejected Brainchild at OAuth `registerClient` with `403 Forbidden` (hard catalog gate, not soft as first hoped). **Power-ups documented in README + test plan:** Grab's `cursor-talk-to-figma-mcp` for write operations (plugin + local WS bridge; addressability tradeoff — only operates on the Figma file currently open in the plugin), and the official server for if/when Brainchild is added to Figma's MCP catalog. **System prompt:** creative persona with personality — the artist of the team, opinionated on hierarchy, type, spacing. **Skills:** bundled `design-review-feedback` (structured critique with don't-hallucinate guardrails; live-validated in session 112 when Designer iterated fetches on a thin fixture and refused to fabricate critique). **README:** `.env.example`: `ANTHROPIC_API_KEY` + `FIGMA_TOKEN`. Live-validated session 112 against `cyv-utza-jxp` — T1–T7 PASS (T4 N/A on read-only path), one known first-fire trigger over-reach on T5 that self-corrects on redirect. | ✅ | ~1h |
| 15.5.2c | Per-server `read_tools` allowlist — wipe the hardcoded `READ_TOOLS` set in `pipeline/chat_runner.py` and move all auto-execute knowledge into per-server `read_tools:` blocks in each `mcp_servers.<name>` config entry. Pipeline carries no per-MCP defaults — bundles declare their own reads. Eliminates the cross-server tool-name collision (Linear `list_issues` vs GitHub `list_issues`) and removes the pipeline-edit friction whenever a contributor adds a new MCP. Updates: `config.py` schema, `chat_runner.py` `_needs_confirmation`, root `config.yaml`, all three roster bundles, `roster/README.md` contributing section. Safe-by-default: anything not in `read_tools` confirms. *Surfaced during 15.5.2b build (session 111) when Figma was the first MCP we hadn't pre-baked into the pipeline; live-validated alongside 15.5.2b in session 112.* | ✅ | ~30m |
| 15.5.3 | Runtime architecture — roster is the source of truth, not an ambient root config. Positional CLI in `__main__.py`: `brainchild <name> [url]` (run that bot in that meeting), `brainchild setup` (wizard, see 15.5.5), `brainchild list` (print roster + one-liners), bare `brainchild` (usage + roster). `<name>` dispatch loads `roster/<name>/config.yaml` directly; delete the root `config.yaml`. `.env` stays at repo root, shared across bots. `brainchild <name>` with no URL auto-opens `meet.new`, captures the assigned slug, joins as that bot (absorbs old 15.5.4's `meet.new` deliverable — same surface). Ship `./brainchild` bash wrapper at repo root as a provisional launcher until product name is locked and curl installer lands (post-launch). Breaking change: every doc/test reference to `python __main__.py <url>` updates to `brainchild <name> <url>`. *Implemented session 113; partially live-validated session 114; auto-admit landed and live-validated session 115 — positional CLI + meet.new redirect + JSONL slug capture + tray-click admission of knockers all clean on `brainchild pm`. Bot runs in headless Chrome (joins as host of meet.new auto-created room); `webbrowser.open(url)` spawns a visible tab for the human on the same URL. Admission verified via participant-count delta (Meet leaves the "Admit N guest" pill sticky in the DOM after admission, so pill-disappearance is not a usable success signal — count delta is). Cooldown on (pill_text, count) prevents re-attempt spam against the stale widget. Known traps fixed during validation: (a) adapter didn't recognize already-in-meeting state, (b) `_is_real_meet_room` URL-path check replaced substring heuristic that let `/new?authuser=0&hs=178` through, (c) `--headless=new` still right for bot but user needs `webbrowser.open`, (d) `./brainchild` scrubbed to `brainchild` across docs/tests now that wrapper is on PATH, (e) sticky "Admit N guest" pill required count-based verification.* | ✅ | ~2.5h |
| 15.5.4 | Face system + profile banner — deterministic glyph-face generator. *Session 117 deviated from the original pixel-art spec in favor of a stylized glyph face inside a small box frame (~4 rows × 6 cols): 2-char eye + 2-char mouth + `█ ▄ ▀` frame, monochrome (color dropped — legibility wins). `pipeline/face.py` with 25 eye × 20 mouth = 500 combos, `sha256(name)` seeded picks (not Python's randomized `hash()`), hand-curated overrides for engineer/pm/designer matching a fighter-select reference mock (engineer `▲▲/══`, pm `⊙⊙/‿‿`, designer `◠◠/▽▽`). `roster/*/portrait.txt` committed per MVP bot; first-run hook in `__main__._print_startup_banner()` mints a portrait for any contributor-added bot. `agent.tagline` config field holds a punchy one-line capability reminder; banner is face on the left + name/tagline/loadout on the right across 4 lines, prints to stderr BEFORE MCP + browser startup (initial attempt printed after MCP and got buried in ~40 lines of logs — moved to fire as a boot splash). `--plain` flag swaps `█ ▄ ▀` frame for `+----+` and picks ASCII eye/mouth glyphs. `_bot_tagline()` prefers `config.yaml` over README fallback. `tests/test_face.py` (9 cases).* | ✅ | ~4h (actual ~2.5h) |
| 15.5.5 | `brainchild setup` wizard — creation + reconfigure flow. **Pre-step (session 118):** MCP `enabled: true/false` flag on every block in every bot's `config.yaml`; `config.py` filters `enabled: false` at load. Replaces commented-out power-up blocks with real parseable YAML so the wizard reads one data shape. pm is the from-scratch baseline. **Wizard (session 119):** `pipeline/setup.py` with five steps — (1) fighter-select with two modes (from-scratch clones pm baseline + prompts for name/display/trigger/tagline/user-display with collision re-prompt reserving `setup`/`list` and existing agents; preset inherits name and edits in place); (2) power-ups — numbered-list `[ ]`/`[✓]` toggle flipping each MCP's `enabled` flag (Rich swallows literal `[x]` as a markup tag — switched to `[✓]`); (3) skills — user-added paths (folder, parent of folders, or single `.md` wrapped in a stem folder as SKILL.md) + bundled skills from the base bot as same toggle pattern (default all-checked, copied into `agents/<name>/skills/`); (4) API keys — prompt for any `${VAR}` referenced by an enabled MCP that isn't already in `.env`, append-only; (5) atomic write via tempdir + `os.rename`. Edit-in-place **seeds the tempdir with the existing bundle** before overwriting owned files (config.yaml, portrait.txt), fully re-authors `skills/`, and only writes a README stub when absent — preserves `.env.example`, delegate scripts, and hand-written READMEs that the wizard doesn't own (fix landed after live smoke-test revealed wholesale-replacement wiping those files). `.bak-<timestamp>` swap is the abort-recovery path. `rich` added to `requirements.txt`. `tests/test_setup.py` — 10/10 green. Smoke-tested both modes end-to-end. Known tradeoff: YAML comments on config.yaml are lost (no ruamel dep); values semantically preserved. **Session 124:** added `description:` field to every `mcp_servers.<name>` block across engineer/pm/designer; wizard step 2 renders it as the sublabel under each MCP name so "delegate" (and future bundle-specific MCPs) no longer look opaque in the power-ups picker. Runtime ignores the field. | ✅ | ~4h |
| 15.5.6 | `brainchild try <name>` — local test-drive before joining a Meet. New `connectors/terminal.py` implementing `MeetingConnector` via stdin/stdout: `read_chat` pulls from a queue fed by a stdin reader thread (non-blocking poll, returns `[]` when empty), `send_chat` prints `[<bot-name>] <msg>` with a colored prefix, `get_participant_count` returns 2 (forces 1-on-1 mode — no `@brainchild` prefix needed), `is_connected` stays True, `set_caption_callback` no-ops, `join/leave` are no-ops. `/quit` and EOF route through `os.kill(pid, SIGINT)` so shutdown mirrors Ctrl+C and skips ChatRunner's "connection lost" warning. New CLI verb in `__main__.py`: `brainchild try <name>` sets `BRAINCHILD_BOT`, builds provider + MCP + skills like the normal path, prints the startup banner, instantiates `TerminalConnector`, runs the full `ChatRunner`. `try` added to `RESERVED_NAMES` in `pipeline/setup.py`. `agents/README.md` Using-an-agent block gained the `brainchild try` step. Live-validated session 125 end-to-end against engineer bundle — terminal harness booted, confirm gate fired, heartbeats streamed, delegate ran `claude -p --worktree` successfully once permission-mode was fixed (see Hard Won Knowledge). Harness surfaced three other bugs in the same session that would've taken 3× Meet roundtrips to find otherwise: (a) delegate `claude -p` needs `--permission-mode bypassPermissions` (was defaulting to ask-mode with no TTY → every Write/Bash denied); made configurable via `DELEGATE_PERMISSION_MODE` env in `agents/engineer/config.yaml`, (b) Engineer system-prompt routing gap — model reached for `github__get_file_contents` on local path questions and hit 404s; tightened with a "github__* is remote-only, local ops go through delegate" paragraph, (c) tool-failure UX — replaced hardcoded "Sorry, that tool call failed. Check the logs" with LLM-authored recovery via new `ChatRunner._handle_tool_failure` helper (signposts the error back through `send_tool_result` with a disposition instruction, then dispatches the LLM's response). Also added `TIMING llm_call=...s TOKENS in=N cache_read=N cache_create=N out=N cache_hit=P%` logging across `pipeline/providers/{anthropic,openai}.py` and split the `tool_exec` span from the 2nd-LLM span in `chat_runner.py`'s `_execute_and_respond` — confirmed prompt cache is hitting 95–98% across turns; per-turn latency ceiling is the delegate subprocess itself (10–15s for `claude -p` boot + tool loop), not Brainchild overhead. | ✅ | ~1h (actual ~2h incl. harness-driven bug fixes) |
| 15.5.7 | Delegate multi-repo + per-repo continuity (+ session-127 live-smoke fix to confirmation persistence). Live smoke of `brainchild try engineer` in session 126 surfaced three coupled bugs in `agents/engineer/delegate_to_claude_code.py`: (a) every delegate call spawned a fresh `--worktree`, so "prove it" / "now add tests" couldn't see files from the previous call and the LLM silently recreated state; (b) sandbox worktrees lived under Brainchild's own `.claude/worktrees/` with no path surfaced to the user — Engineer reported "confirmed working" for files that never existed in the user's working tree; (c) the tool was hard-pinned to whatever repo Brainchild was launched from, which is almost never the repo the user wants to work in. Rewrite: `delegate_to_claude_code(task, repo_path)` where `repo_path` is a required absolute path to a local git repo; module-level `_SESSIONS` dict maps canonical `repo_path` → `{session_id, worktree_path, base_sha}`. First call per repo runs `claude -p <task> --worktree` with `cwd=repo_path`, captures the new entry from `git worktree list --porcelain` diff and stores `session_id` from the JSON output. Subsequent calls same repo run `claude -p <task> --resume <id>` with `cwd=worktree_path` (critical: `--resume` looks up the session by project dir = cwd, and does NOT accept `--worktree` — that would spawn a new sandbox). Fall back to a fresh worktree if resume returns "No conversation found" (sandbox manually deleted between calls). New `land_worktree_changes(repo_path)` tool runs `git add -A` in the worktree (Claude Code leaves new files untracked — diff would otherwise be empty), then `git diff --cached <base_sha>` → `git apply` into the parent repo. Fails cleanly (reports the conflict) if the user's tree has diverged. Tool result footer on every delegate now includes `[workdir: ...]` and `[repo: ...]` lines; Engineer's system prompt teaches it to ask for `repo_path` on first delegation, reuse it for follow-ups, mention the workdir when reporting back, and offer to land after success. README gains a "How delegation works" section. Spec doc at `docs/delegate_multi_repo_spec.md`. Verified in isolation against three fresh `/tmp/` git repos: create → resume-read → land round-trip clean; two repos in one process hold independent sessions with no cross-contamination. Commit `23bf258`. **Live-smoked end-to-end session 127** — full 8-step plan walked against fresh `/tmp/test-delegate` + `/tmp/test-delegate-2` scratch repos: ask-for-repo-on-first-call ✅, sandbox isolation ✅ (file lived under `.claude/worktrees/<name>/scratch/`, not in user's working tree), `--resume` reused same workdir on follow-ups ✅, multi-repo isolation ✅ (independent sandboxes per repo), `land_worktree_changes` correctly stages-and-applies new files into the user's working tree in 0.1s ✅. Live smoke also surfaced and fixed a separate `pipeline/chat_runner.py` bug (commit `2a7de34`): tool-confirmation messages were persisted with `kind="chat"` and the model started mimicking the harness's "I'd like to run X. OK?" prompt verbatim before raising its hand for a real tool_call — fix tags both the harness's prompt and the user's "ok"/"no" reply with `kind="confirmation"`, which `pipeline/llm.py` already filters out of the LLM prompt. See Hard Won Knowledge for the full diagnosis. | ✅ | ~3h |

**Phase total: ~19.75h**

---

## Phase 13: Polish

*Clean stdout, clean config. No latency audit, no comprehensive error pass. **Sequenced after 15.5 (session 105):** audits the final config keys + log surface, not a moving target.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 13.1 | Config audit & cleanup — remove dead keys, ensure every key is necessary, document what each controls | ✅ (session 128: MCP baseline normalized. Session 129: 9 fields moved into INTERNAL TUNING block in config.py, dead `conversation_timeout` deleted, per-server tool_timeout override plumbed. Pending live smoke.) | ~2h |
| 13.2 | Log cleanup — structured, consistent log levels; clean stdout for normal operation (no debug spam, no stack traces for expected errors), verbose debug stays in `/tmp/brainchild.log` | ✅ (session 136: new `pipeline/ui.py` narrator on stderr with color auto-off, `_run_macos` root stderr StreamHandler dropped, `_run_linux` switched to file-based DEBUG, key user-visible moments in `chat_runner.py` routed through `ui.*` with log pointer, "Launching Chrome…" heartbeat added, README seeded with privacy note. Deferred post-launch: log rotation + per-instance logfile namespacing.) | ~2h |

**Phase total: ~4h**

---

## Phase 15.6: Pre-Launch Security

*Second pre-launch security pass. Phase 9.5 covered general input sanitization, credential handling, and dependency hygiene; this phase targets live-meeting prompt injection into a write-capable tool loop. Motivated by the OpenClaw/ClawHub security-vulnerability backlash on a peer OSS project — being publicly audit-ready at launch matters for both liability and adoption flywheel. **Session 139 reshape:** original 15.6.2 (core-repo `agents/CONTRIBUTING.md` + CODEOWNERS contribution gate) moved to post-MVP after ClawHub case-study research (341/2857 marketplace skills malicious; top-downloaded entries were malware; primary attack vector was prompt injection embedded in skill markdown, not config). Core-repo contributions with a maintainer review gate is the wrong shape for a solo maintainer — pivoted to an external gallery model (users clone from a separate index repo into their own `agents/`; runtime skill-markdown delimiter wrapper is the one non-theater mitigation we'll owe). See Post-MVP → Community & Gallery. **Sequenced after 13 (session 105):** clean code is easier to audit, and any fixes land before packaging + cross-platform testing. **No third-party audit pre-launch** (wrong stage, wrong timeline) — OSS-native transparency substitutes: threat-model doc, disclosure channel, and a self-audit driven by the `security-review` skill. Live-smoke of sessions 131 + 132 + 136 + 137 moves to after this phase (audited code gets smoked, not pre-audit code).*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.6.1 | Runtime security review + public-facing security artifacts. **Step 0:** run the `security-review` skill on the diff since 9.5 to drive the mitigation inventory. **Named surfaces (severity-ranked):** (a) `delegate_to_claude_code` is the highest-severity write path — spawns `claude -p --permission-mode bypassPermissions` against a user-supplied `repo_path` with a user-supplied `task`, so the confirmation gate is the only thing between a chat message and arbitrary code execution in the user's repo. Verify the confirm UI renders full `task` + `repo_path` legibly; check obfuscated-payload cases. (b) MCP tool writes (Linear `save_issue`, GitHub writes) — direct injection via chat + indirect injection via poisoned tool-result strings feeding back into the next prompt. Does the confirmation flow hold under social engineering? Are tool-result strings sanitized before re-prompting? (c) Captions-as-prompt-injection vector — `captions_enabled: true` on pm + designer means anyone *speaking* in the meeting injects into the LLM prompt, bypassing the `@brainchild` trigger gate entirely. Document mitigation or explicit residual risk. (d) MCP server command validation — `config.yaml` declares `command` + `args` for subprocesses; ensure no shell-injection path, and that a contributed `agents/<name>/config.yaml` can't smuggle arbitrary binaries. (e) Log sanitation — grep `/tmp/brainchild.log` for leaked API keys, tokens, full `.env` dumps, OAuth cookies; redact as needed. (f) `browser_profile/` + `auth_state.json` threat-model note — these hold real Google session cookies; document the blast radius if any other surface lands code execution. **Deliverables:** (1) `SECURITY.md` at repo root with GitHub-recognized disclosure email + response-time expectation, (2) `docs/security.md` — user-facing threat model, mitigations, known residual risks, and token-scoping guidance (fine-grained GitHub PATs, narrow Linear/Figma scopes, scoped `.env`), (3) `pip-audit` run against the pinned requirements (coordinates with 14.2). **Session 137:** audit done, 6 behavioural fixes shipped (Fix #0 `auth_state.json` in `.gitignore`, Fix #1 delimiter wrappers + label sanitizers, Fix #2 tool-arg redaction, Fix #3 unsafe env-key strip, Fix #4 confirmation shows all args with head/tail truncation, Fix #5 path-hygiene + `browser_profile/` chmod 0o700) across 5 commits `8d29b23…d1fa9fa`. **Session 138:** `SECURITY.md` (disclosure contact + 72h ack SLA + GitHub recognition) + `docs/security.md` (threat model, trust boundaries, residual risks H1/H2/L1–L4, "never commit" canonical list, "what's been hardened" section citing each fix commit, `NODE_OPTIONS`/`RUBYOPT`/`PERL5LIB`/`CLASSPATH`/`HOME` noted as residual env-key candidates for non-Python MCP runtimes); `pip-audit` flagged 5 CVEs across 4 transitive deps (cryptography, pygments, python-multipart, requests), added `>=` security floors to `requirements.txt` (full pinning defers to 14.2), re-audit clean, smoke tests pass. | ✅ | ~5h |
| 15.6.2 | ~~Contribution security gate — `agents/CONTRIBUTING.md` + CODEOWNERS.~~ **Moved to post-MVP (session 139)** after research into ClawHub (OpenClaw's public skill marketplace — closest OSS peer to what this row originally proposed). Findings: 341/2857 ClawHub skills malicious, 5 of top 7 most-downloaded were malware, attack vector was prompt-injection instructions embedded in `SKILL.md` directing the AI to walk the user through downloading external payloads. "Narrow the contribution surface to config + markdown only" doesn't help because markdown *is* the attack surface. Block-before-publish scanning + verified authors + kill switch are the non-theater mitigations, and they're a team-sized project, not a 1.5h row. Pivoted to a gallery model (separate index repo, caveat-emptor, user owns the trust decision — `npm install` / `brew tap` / Claude Desktop MCP precedent). See **Post-MVP → Community & Gallery**. | → Post-MVP | — |

**Phase total: ~5h** (15.6.1 shipped; 15.6.2 moved to post-MVP)

---

## Phase 14: Package & Distribution

*Expanded from "minimal packaging" (session 141) to a real install path. Ship a `curl | sh` one-liner that installs Brainchild via `uv tool install` from a git URL, with agents extracted to a user dir (`~/.brainchild/agents/`) so the shipped package is immutable. **Deliberately skipping PyPI for MVP** — `uv tool install git+https://github.com/.../brainchild.git` gives the same install ergonomics without PyPI account/metadata overhead; PyPI moves to post-MVP polish. **Sequenced after 15.6 (session 105):** package the audited code, not pre-audit code.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 14.1 | Add `LICENSE` (MIT) | ✅ | ~5m |
| 14.2 | Dependency pinning — `requirements.txt` with pinned versions for reproducible installs. No Python version matrix, no lockfile tooling. **Session 144 follow-up audit (`e1e1c0d`):** built a clean-venv + clean-wheel verification of the 4 `>=` CVE floors; `requests>=2.33.0` was not in the resolved tree of the 7 direct deps (session 138's `pip-audit` flagged it via residual voice-pipeline packages `elevenlabs`/`spacy`/`google-api-core` + dev tools `pip-audit`/`twine` polluting the audit venv) — dropped. The other three floors confirmed real via `pip install --report` (which surfaces extras-based parents that `pipdeptree --reverse` misses): `cryptography` via `mcp → pyjwt[crypto]` (stale "via google-auth" comment corrected), `pygments` via rich, `python-multipart` via mcp. | ✅ | ~1h |
| 14.3 | Package restructure (src-layout) — move flat-root modules (`__main__.py`, `config.py`, `pipeline/`, `connectors/`, `agents/`) into `src/brainchild/`. Update every import (`from pipeline.x` → `from brainchild.pipeline.x`) across ~40 files + 20 test files. Extract a proper `main()` entry point in `src/brainchild/__main__.py`. Ship bundled agents (pm, engineer, designer) as **templates** in `src/brainchild/templates/agents/` — on first run, auto-copy all three to `~/.brainchild/agents/` so `brainchild list` shows something immediately. Agent discovery switches from `_ROOT / "agents"` (repo-relative) to `~/.brainchild/agents/` (user dir). Full test suite green before moving on. **Commit 1 of 3 shipped session 142 (`7be6fd4`):** src-layout moves + import rewrites + test/launcher wiring. **Commit 2 of 3 shipped session 142 (`3504302`):** agents → templates + user-dir auto-copy hook. **Commit 3 of 3 shipped session 142 (`731d662`):** `main()` already existed at `__main__.py:289` with clean import-time semantics; commit adds a smoke test asserting `import brainchild.__main__` exposes `main` as a zero-arg callable — the exact contract 14.4's `[project.scripts]` consumes. | ✅ | ~0m remaining |
| 14.4 | `pyproject.toml` + console script entry — modern `[project]` metadata (name, version `0.1.0`, description, license, Python ≥3.10, dependencies from `requirements.txt`), `[project.scripts] brainchild = "brainchild.__main__:main"` so `uv tool install` creates the `brainchild` CLI on PATH. Build backend: hatchling (uv default). No PyPI upload yet — `uv tool install git+https://...` reads this file directly. **Shipped session 143.** Core commit `dd7e416` (pyproject.toml, hatchling backend, console script entry verified end-to-end with throwaway `pip install` + `brainchild list` auto-copy). Two cleanup commits in the same session: `e7f5240` removed stale build artifacts (root `connectors/__pycache__/`, `build/`, `dist/`, `.eggs/`) and 7 tracked auto-admit debug snapshots; a third commit collapses `src/brainchild/templates/agents/` → `src/brainchild/agents/` (only one kind of bundled content, so the `templates/` wrapper earned nothing) and renames `_TEMPLATES_DIR` → `_BUNDLED_AGENTS_DIR` for readability. | ✅ | ~0m remaining |
| 14.5 | `install.sh` — bash install script at repo root. In order: detect macOS/Linux + Python ≥3.10; bootstrap `uv` if missing (calls Astral's installer); `uv tool install git+https://github.com/.../brainchild.git`; run `playwright install chromium` (the 170MB browser download); seed `~/.brainchild/.env` with commented API-key placeholders; verify `brainchild` on PATH; print a "what's next" message pointing at `brainchild setup`. **Must include 14.5.1 brew-cask Chrome offer when this lands** — see 14.5.1 row. | ⬜ | ~2h |
| 14.5.1 | `install.sh` brew-cask Chrome offer — on macOS, after the `uv tool install` step, detect missing `/Applications/Google Chrome.app` and offer `brew install --cask google-chrome` if Homebrew is present (else print URL + exit 1). Real Chrome is a hard runtime dep on macOS (session 159: profile-binary mismatch between Chrome-for-Testing and real Chrome). Bundling Chrome ourselves was rejected — 600 MB binary, ToS issues, update treadmill, profile concurrency hazards. Brew-cask is the same pattern `gh`/`hugo`/`volta` use for system deps; google-chrome cask is Google-blessed. ~6 lines in `install.sh`. Sequenced as a sub-step of 14.5 because the install script is the right place to plant Chrome before the user ever hits the runtime preflight. Added session 160. | ⬜ | ~10m |
| 14.10.5 | Chrome runtime preflight — new `pipeline/chrome_preflight.py` exporting `chrome_installed()` + `require_chrome_or_exit()`. Wired into `__main__._run_setup` (wizard) and `__main__._run_macos` (bot run) so users without Chrome get a single human line + install URL instead of an opaque Playwright error from `executable_path` pointing at a missing binary. Defense-in-depth duplicates at the launch sites (`google_signin._launch_signin_flow`, `macos_adapter.MacOSAdapter.join`) protect against future code paths that bypass `__main__`. Linux skipped (linux adapter uses bundled chromium); `try` skipped (terminal connector, no browser). 3 new tests in `tests/test_wizard_signin.py` cover darwin-true / darwin-false / non-darwin. Shipped session 160. | ✅ | ~0m remaining |
| 14.6 | DNS + static host for `brainchild.build/install` — Cloudflare DNS wire-up **done async in session 142** (nameservers moved Porkbun → Cloudflare, domain active). Static hosting (Cloudflare Pages, apex path `/install`) deliberately deferred until 14.5 has a real `install.sh` to deploy — one clean GitHub-connected Pages wire-up instead of a throwaway upload. TLS cert automatic via CF. Also unblocks Bluesky domain-handle verification + landing page (Phase 16.3). | 🟡 | ~30m remaining |
| 14.7 | Uninstall docs — three lines in the README (`uv tool uninstall brainchild` + `rm -rf ~/.brainchild` + drop the PATH symlink if it survives). No dedicated subcommand: `uv tool install`-style CLIs don't ship their own uninstaller — package manager owns it, same as pipx/cargo/npm. Homebrew/nvm/Ollama-style wrappers exist because they touch system locations; we don't. **Deferred session 159** — post-MVP polish, not blocking launch. | ⏸️ deferred | ~10m |
| 14.9 | Config discoverability — `brainchild edit <bot \| .env>` opens the file in `$VISUAL`/`$EDITOR`/`vi` (multi-word editors handled via `shlex.split`); `brainchild where <bot \| .env>` prints the absolute path for shell composition (`code $(brainchild where claude)`, `open -R $(...)`). Resolver centralized in `_resolve_config_target` so both subcommands give identical error shapes on unknown targets. Customary UX pattern (matches `git config --edit`, `crontab -e`, `kubectl edit`) — closes the "users shouldn't need to memorize `~/.brainchild/agents/<name>/config.yaml`" papercut surfaced in session 156 live QA. Shipped `01b0868`. Candidate follow-up: add a "for further tweaks, run `brainchild edit <name>`" line to the wizard reveal screen so the command self-discovers at the moment of first config-touch. | ✅ | ~0m remaining |
| 14.10 | Wizard Google-signin step — Meet joining requires a logged-in Chrome profile; pre-14.10 the only way to populate it was to `brainchild run <bot>`, hit a blank Meet screen, and manually navigate to `accounts.google.com` inside the launched browser. Session 159 folded that into `brainchild setup` as the **last wizard step** (after API keys, before bot reveal) so the bot is already on disk when sign-in runs and a skip/failure leaves a valid agent. **Shape:** detect-or-launch via `pipeline/google_signin.py`. Fast-path detect reads `~/.brainchild/auth_state.json` (re-uses `session.validate_auth_state`) + cached `~/.brainchild/google_account.json` (`{"email": "..."}`) — instant, shows `✓ signed in as <email>` with `[enter] continue / [r] sign in with a different account`. Re-auth path navigates to `accounts.google.com/Logout` then `ServiceLogin` inside the same persistent profile (no nuking the 1.1 GB `browser_profile/`). First-time path launches visible Chrome to `accounts.google.com`, polls cookie jar for `.google.com` SID, then captures the email from the live `myaccount.google.com` DOM (free — page is already open) and writes both `auth_state.json` (Playwright `storage_state`, seeds Linux/Docker recovery) + `google_account.json` (cache for next wizard). Skip path is strong `y/N` with no default and an explicit consequence message. **Per session-158 audit discipline,** `GOOGLE_ACCOUNT_FILE` lives on `config.py` (not module-private) and is wired into `tests/test_path_resolution.py`'s allow-list + anti-drift scanner. **Tests:** `tests/test_wizard_signin.py` (11 cases) covers detection across present/absent/malformed artifacts + email regex; the live launch flow is unit-untested by design (Playwright mocking is brittle for persistent contexts) and gets its smoke test in Phase 14.8's clean-mac live-QA. Shipped session 159. | ✅ | ~0m remaining |
| 14.11 | Wizard MCP "context, not just tools" copy tweak — the step-2 MCP toggle screen in `pipeline/setup.py` currently frames MCPs as a pure tool palette (pick what the bot can call). Reframe as "tools + context" so users understand they're also bringing in their working context: what the bot can *see* and act across (Linear backlog, GitHub repos, Gmail inbox, Calendar availability), not just what it can *do*. One-line intro above the picker + per-server caption that leans into the context angle ("Linear — knows your tickets, projects, sprint" rather than "Linear tools"). Picker mechanics unchanged — copy-only change. Added session 159. **Deferred session 159** — copy polish, not blocking launch. | ⏸️ deferred | ~20m |
| 14.8 | Full user-flow smoke test (**in the shoes of a brand-new user — no priming, no prior brainchild state**; absorbs live-smoke of 131+132+136+137 + Phase 15.10.3 claude use-case script + Phase 15.10.4 other-bot checklist) — run on a clean second mac. (a) Paste `curl -fsSL brainchild.build/install \| sh`; verify install completes + browser downloads + stub `.env` appears. (b) Run `brainchild setup`, create an agent, fill API keys. (c) Run `brainchild try pm` terminal test-drive. (d) Run `brainchild run pm <fresh-meet-url>` against a real Meet — this is the live-smoke validating sessions 131 (intro-on-join), 132 (personality/ground_rules split), 136 (stderr narrator), 137 (security-fix delimiter wrappers) on the packaged version, not the clone-and-hack version. (e) Run the full Phase 15.10.3 top-10 claude-code use-case script against `brainchild run claude <fresh-meet-url>` — pass/fail each. (f) Walk the Phase 15.10.4 other-bot live-test checklist against `brainchild run pm`, `brainchild run engineer`, `brainchild run designer`. Fix whatever breaks across (a)–(f). | ⬜ | ~4–5h |

**Phase total: ~10h remaining** (14.1 + 14.2 ✅ = 1h05m done; 14.6 half-done session 142 = 30m shaved; 14.3 fully closed session 142 = 4.25h shaved; 14.10 added session 158, shipped session 159 = 2h closed; 14.11 added session 159 = +20m; 14.8 expanded session 159 to absorb 15.10.3 + 15.10.4 live-QA = +2–3h)

---

## Phase 15: Cross-Platform Testing

*Prove it works on Linux too. macOS smoke is already covered by Phase 14.8 (curl installer on clean mac), so Phase 15 slims to just the Linux run. **Sequenced last before launch:** tests the final, packaged artifact end-to-end on the other platform.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.1 | Linux testing — dedicated session on a real Linux box (not Docker). Paste the curl one-liner, run the same user flow as 14.8 (setup → try → real Meet), fix whatever breaks | ⬜ | ~3h |

**Phase total: ~3h**

---

## Phase 15.7: MCP Hardening + Team Completion

*Full arc for the team roster — MCP infrastructure that gracefully handles missing auth across five new integrations, plus skills that close the discussion → PRD → handoff → scope → build → triage → ship loop. Compounds on Phase 10's MCP hardening and Phase 15.5's three-member roster. **Sequenced before 14.5 `install.sh`** (session 147 revision) so the installer seeds `~/.brainchild/.env` with the full post-15.7 env-var placeholder set in one pass — avoids a second pass on the install template. Approved pre-launch — no post-MVP split on this initiative.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.7.1 | Error classification foundation — see detailed shipped description in current-status block above. Committed `6beff98` (session 148). | ✅ | ~1h |
| 15.7.2 | Proactive failure banner at meeting join — `ChatRunner._post_mcp_failure_banner` posts one compact line in `run()` immediately after `ui.ok("Joined meeting…")` and before the async intro thread (lands deterministically even if intro generation errors). Dispatches on `kind` to render compact labels; multi-failure case joins with "; " and closes with "Ask for details." Committed `43dae3f` (session 148). Pressure-test still deferred to real-Meet smoke (tracked in `docs/deferred-live-tests.md`). | ✅ | ~45m |
| 15.7.3 | OAuth decoupling via cache-path pre-check + `brainchild auth <mcp>` — detailed shipped description in current-status block above. Bundled Linear configs on all 3 agents got `auth: oauth` + `auth_url`. User-owned `~/.brainchild/agents/` intentionally NOT modified (insulation promise from session 147). Committed `ed63026` (session 148). Pressure-test deferred to real-Meet smoke. | ✅ | ~1.5h |
| 15.7.4 | Wizard credentials status screen + claude-code prereq check — `credentials_url` added to every bundled MCP block (pm/engineer/designer × claude-code/figma/github/linear/notion = 15 blocks); passed through to `config.MCP_SERVERS` for the runtime pre-flight to consume. New `pipeline/readiness.py:report_mcp_readiness(mcp_servers)` returns `{name: {status: ok|missing_env|oauth_needed|prereq_missing, fix, fix_url, ...}}`. Env-auth branch re-uses 15.7.1's `missing_vars` when present (runtime shape) or scans `${VAR}` refs in `env` directly (wizard shape). OAuth branch calls into the extracted `pipeline/oauth_cache.py:oauth_cache_exists` (refactored out of mcp_client so the wizard can import without triggering `BRAINCHILD_BOT`-gated config load). Claude-code branch probes `shutil.which("git")` + `shutil.which("claude")` + `claude auth status --json` (checking `loggedIn: true`); 5s timeout with graceful fallback to `prereq_missing` on any failure mode (timeout, non-zero exit, malformed JSON, not logged in). `check_claude_code_auth=False` knob skips the subprocess hop for callers that want binary-only validation. Wizard's `_step2_mcps` now renders `✓/⚠/✗` status after the MCP toggle picker returns, with per-server fix hints + credentials URLs. OAuth `oauth_needed` gets an inline "Authorize X now? y/N" prompt that calls `pipeline/auth.py:run_auth` (extracted out of `__main__._run_auth` so CLI + wizard share one code path); re-renders readiness after each attempt so the user sees the state change. `claude-code` in the enabled report also triggers a one-line git-init reminder below the status table. Mid-meeting `delegate_to_claude_code` against a non-git path now tells the user `"Run git init in that folder (or point at a folder that's already a git repo) and retry"` instead of the bare "no .git found" message, closing the hand-edit-config UX gap. 20 new unit tests in `test_1574_readiness.py` covering every status + every claude-code prereq failure mode; existing 10/10 15.7.3 tests still pass after the oauth_cache extraction. Pressure-test: run wizard, toggle slack + claude-code on with `SLACK_BOT_TOKEN` unset + claude CLI missing, confirm the status screen calls both out with the right URLs (deferred to real wizard run, tracked in `docs/deferred-live-tests.md`). | ✅ | ~2h |
| 15.7.4.5 | Runtime MCP pre-flight (session 148 split-out) — `preflight_mcp_readiness(config.MCP_SERVERS)` runs inside `_run_bot()` after `BRAINCHILD_BOT` is set but before `_run_macos`/`_run_linux`. Platform-agnostic (one call site covers both). All-ok case returns `PREFLIGHT_OK` silently (zero latency cost on the happy path — no output, no prompts). Per-status prompts when gaps exist: oauth_needed → "authorize now? [y/N]" default N (auth-now is opt-in, not default — mcp-remote takes over terminal + opens browser); y invokes `run_auth(name)` inline and continues regardless of rc. missing_env → "continue without it? [Y/n]" default Y; n returns `PREFLIGHT_USER_ABORT=2` so the user can edit `.env` without Ctrl+C mid-join. prereq_missing → "continue anyway? [Y/n]" default Y (claude-code may not be used this meeting). Runtime uses `check_claude_code_auth=False` to skip the 5s `claude auth status` subprocess — the 15.7.2 mid-meeting banner catches stale-login, the wizard's 15.7.4 probe is where the thorough check lives. EOFError on `input()` falls back to default (pipe/redirected-stdin CI launches don't hang). Helper is input/output/run_auth injectable for testability. `--no-preflight` flag bypasses entirely. 11 new tests in `test_15745_preflight.py` + 2 new in `test_entry_cli.py` covering both flag + abort-short-circuit paths; 142/142 regression green across MCP + CLI + chat hardening suites. | ✅ | ~45m |
| 15.7.5 | New MCP scaffolding — shipped `5453080` (session 149 async). Added five blocks to every bundled agent's `mcp_servers:`: `calendar` (@cocal/google-calendar-mcp, `auth: env` via `GOOGLE_OAUTH_CREDENTIALS` pointing at a local credentials.json), `playwright` (@playwright/mcp@0.0.70, no auth), `salesforce` (@salesforce/mcp, `env: {}` with auth delegated to the `sf` CLI's local cache — flagged as a readiness UX gap during pressure-test), `sentry` (hosted remote via mcp-remote OAuth against https://mcp.sentry.dev/mcp, same pattern as Linear), `slack` (@modelcontextprotocol/server-slack + `SLACK_BOT_TOKEN`/`SLACK_TEAM_ID` env — archived ref server, @zencoderai/slack-mcp-server tagged as drop-in fallback in the block comment). Enablement matrix matches session-147 rule: PM gets calendar (flipped github→off — no PM skill uses it), Engineer gets sentry (keeps claude-code + github), Designer keeps only figma (flipped linear→off — no Designer skill uses it). `slack`/`salesforce`/`playwright` scaffolded `enabled: false` on all three. Each block declares `auth`, `credentials_url`, `env`, `read_tools` (5–6 per server, read-only auto-execute allowlist), `confirm_tools`, and `hints` (grounded in the server's known quirks — calendar RFC3339 timestamps, sentry org/project scoping, playwright snapshots-over-screenshots, salesforce PascalCase objects, slack channel-IDs-not-names). Zero Python changes — 15.7.1's `_resolve_env_vars` and 15.7.3's oauth cache check are generic over `${VAR}` and `auth: oauth`, so new blocks flow through without code edits. Three entries ship with `@latest` + a "pin after pressure-testing" inline comment rather than fake a version (playwright + mcp-remote are real pinned; calendar / slack / salesforce pin-pending). Pressure-test matrix per MCP captured in `docs/deferred-live-tests.md`. Existing installs at `~/.brainchild/agents/` intentionally not modified (session-147 insulation promise). Regression: 20/20 test_1574_readiness + 28/28 test_mcp_client + 13/13 test_1571_error_classification + 10/10 test_1573_oauth_cache_check + chat_hardening all green. | ✅ | ~2-3h |
| 15.7.6 | Six new skills across the roster — shipped `4986743` (session 149 async). `design-handoff-spec` (Designer; Figma → engineer-facing spec with layout / spacing / typography / colors / states / interactions / assets / open questions — grounds every number in the fetched canvas, flags WCAG AA contrast failures), `scope-estimate` (Engineer; ticket or spoken ask → T-shirt size + concrete files to touch + blockers + risks + unknowns; no code, no MCP dep), `pr-review` (Engineer; GitHub PR URL via `pull_request_read` + `get_file_contents` → summary + bugs + missing tests + risks + style + questions, cites file:line for every claim), `live-bug-triage` (Engineer; Sentry + optional claude-code; five-phase flow posted as separate messages so the team can redirect between symptom → stack frame → probable cause → proposed fix → optional delegate), `release-notes` (Engineer; GitHub commit range via `list_commits` + `list_pull_requests` → grouped changelog with Features / Fixes / Changes / Internal sections and `#<PR>` links, breaking-change callout at the top), `schedule-followup` (PM; Calendar MCP; spoken commitment → two-phase invite flow — propose → confirm → create with the Linear/PRD URL in description). Each `SKILL.md` follows the existing `prd-from-discussion` / `design-review-feedback` frontmatter + Shape + Rules pattern. Engineer didn't have a bundled skills dir before this commit — created `agents/engineer/skills/` and added `agents/engineer/skills` to `skills.paths` in engineer/config.yaml (last-wins override of user skills). All nine bundled SKILL.md files load cleanly via the real `pipeline/skills.py:load_skills()` path (PM 3, Engineer 4, Designer 2). Three of the six (pr-review, release-notes, scope-estimate) are pressure-testable today (GitHub MCP or no MCP dep); the other three depend on 15.7.5-scaffolded MCPs and light up for users once those are authed. Pressure-test is the 15.7.5 deferred-live-tests entry + a one-turn trigger per skill per agent. | ✅ | ~2-3h |

**Phase total: ~9-11h originally; ~7.75h shipped across session 148 (15.7.1 + 15.7.2 + 15.7.3) and session 149 (15.7.4 + 15.7.4.5 + 15.7.5 + 15.7.6). Phase 15.7 CLOSED.**

---

## Phase 15.8: CLI Verb Rename (`brainchild <name>` → `brainchild run <name>`)

*Disambiguate the agent invocation by requiring a directive verb. Bare `brainchild pm [url]` isn't action-oriented and collapses the "agents are brainchildren of the user" metaphor (reads as "brainchild, meet pm" — peer-to-peer introduction rather than directive). Directive verbs preserve the metaphor: `brainchild run pm` = "brainchild-system, run the brainchild named pm", same shape as `docker run nginx`. Chose `run` over `summon` / `send` / `meet` / `call` in session 149 — neutral developer vocabulary (matches `docker run`, `npm run`), platform-agnostic, keeps `try` / `setup` / `auth` reading as parallel verbs in the command table. Rejected dropping `brainchild` entirely: loose per-agent binaries (`pm`, `engineer`) would collide with generic shell vocabulary and fragment discoverability; OSS convention isn't "package name must lead" but "there must be one anchor users can grep shell history and docs for" — `brainchild <verb>` gives that anchor. **Sequenced before 14.5 `install.sh`** so the installer's "what's next" message, the README quickstart, and the wizard reveal all ship with the new form in one pass instead of a follow-up doc sweep.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.8.0 | Audit — inventory every user-facing surface that echoes the bare `brainchild <name>` form, so the 15.8.1 rename lands atomically. Surfaces to walk: (a) `src/brainchild/__main__.py` dispatcher, module docstring, `_print_usage`, and the "Restart with:" `ui.say` lines in `_run_macos` / `_run_linux`; (b) tests that hard-code the CLI shape — `tests/test_entry_cli.py` plus any wizard/setup tests that echo a launch command; (c) `README.md`, `src/brainchild/agents/README.md`, `CLAUDE.md` (quick-start block + Commands section); (d) wizard reveal / success copy in `pipeline/setup.py` and any `ui.say` strings in `pipeline/ui.py`; (e) log / warning strings that suggest a command (Linux-adapter `A meeting URL is required on Linux:` help block; future install.sh stub references); (f) `docs/mvp-bar.md`, `docs/roadmap.md`, `docs/agent-context.md` example commands; (g) docstring references (e.g. `config.py`'s `BRAINCHILD_BOT` error pointing at `brainchild <name> [url]`). Output: short checklist captured in the 15.8.1 commit message so reviewers can verify nothing slipped. | ✅ | ~30m |
| 15.8.1 | Rename — apply the audited changes atomically per the 15.8.0 inventory below. Dispatcher: `brainchild run <name> [url]` becomes the canonical form; bare `brainchild <name>` hard-fails with exit 2 + hint pointing at `brainchild run <name>` (no dual-accept — cleaner failure mode; avoids a transition period where two forms work and muddy support/telemetry). `try` / `setup` / `auth` / no-arg unchanged (already verbs / already print help). **Shipped session 150.** 21 documented changes landed across the 7 audited files; audit had missed 2 test cases in `test_entry_cli.py` (`test_main_known_bot_dispatches_to_run_bot` + `test_main_unknown_bot_returns_2` both exercised the bare form), so the commit also renames the first to `test_main_run_known_bot_dispatches_to_run_bot` and adds 3 new tests — `test_main_bare_known_bot_rejected_with_run_hint`, `test_main_run_without_name_returns_2`, `test_main_run_unknown_bot_returns_2`. `_print_usage` collapses the two-form lines into one `brainchild run <name> [url]` line with "auto-opens one if no url" inline. Dispatcher adds a `run` branch (with its own "unknown bot" branch pointing at usage) and a bare-known-bot branch that prints a pointed hint (`Run agents via \`brainchild run pm\`. Bare \`brainchild pm\` is no longer supported.`) + exits 2 — distinguishes known-but-bare from truly-unknown so the transition is self-documenting. 26/26 `test_entry_cli.py` pass, `test_setup.py` pass, `python -m brainchild pm` verified emitting the new hint and `python -m brainchild` verified emitting the updated usage. | ✅ | ~1h |

**15.8.0 audit inventory (session 149 output — 21 required changes across 7 files):**

- **`src/brainchild/__main__.py` (7 hits)** — module docstring lines 6–7 (two form lines); `_print_usage` lines 212–213 (two form lines); `_run_macos` line 595 `ui.say("Restart with:")` hint; `_run_linux` line 701 matching `ui.say` hint; `_run_linux` missing-URL help block lines 624–625 (two form lines — group with the line-623 intro in the diff so the error message stays coherent post-rename).
- **`README.md` (2 hits)** — lines 8–9 quick-start block.
- **`CLAUDE.md` (3 hits)** — lines 14–15 Run section; line 87 prose (`brainchild <name> [url]` in an inline-code span, not a fenced block — substitution happens inside the backticks).
- **`src/brainchild/agents/README.md` (2 hits)** — lines 30–31 quick-start. Lines 139 + 142 reference "the `brainchild` agent list" as a no-arg output, not a command — **no change needed.**
- **`src/brainchild/agents/{pm,engineer,designer}/README.md` (6 hits)** — two quick-start lines per agent: pm 29–30, engineer 27–28, designer 32–33.
- **`src/brainchild/pipeline/setup.py` (2 hits)** — line 728 wizard reveal success copy (one line with two form instances — ``Run: `brainchild {name}` or `brainchild {name} <meet-url>` ``); line 754 startup hint (``Take {name} for a spin: brainchild {name}``).
- **`src/brainchild/config.py` (1 hit)** — line 14 `BRAINCHILD_BOT` missing-env error (``Run via the CLI: `brainchild <name> [url]` ``).

**Zero-hit confirmations (no changes needed in these categories):**

- **Tests** — `tests/test_entry_cli.py` assertions cover only `try` / `auth` / `setup` subcommand paths (unchanged). `tests/test_setup.py` mocks dispatch and does not echo launch-command strings in assertions.
- **Living docs beyond category (c)** — `docs/mvp-bar.md` has zero references to the bare form.
- **Subcommand references (`brainchild try`, `brainchild auth`, `brainchild setup`)** — 14 instances across `pipeline/readiness.py`, `pipeline/mcp_client.py`, `pipeline/chat_runner.py`, `pipeline/auth.py`, `pipeline/setup.py`, `pipeline/build_card.py`, `pipeline/picker.py`, `connectors/terminal.py`. **Unaffected** by the rename.

**Out-of-scope per session-149 direction** — archival session-notes blocks in `docs/agent-context.md`; `docs/handoff.md`; the Phase 15.5.3 description in `docs/roadmap.md` (historical); the Phase 15.8 section itself in `docs/roadmap.md` (references the old form on purpose as context).

**Phase total: ~1.5h — shipped session 150 (15.8.0 audit-in-docs + 15.8.1 atomic rename).**

---

## Phase 15.9: `claude` Bundled Agent (Claude Code in your meeting)

*Fourth bundled agent whose defining trait is "auto-inherit the user's existing Claude Code setup." Positioning: target the Claude Code power-user audience with an agent that Just Works against their configured MCPs + skills. Hard-dependency on Claude Code being installed + logged in — the agent identity collapses without it. Sequenced before 14.5 `install.sh` so the installer ships a complete bundled roster of four. Non-blocking alongside 15.7.5/6 but a launch-quality bet: gives us a second launch moment ("Claude Code users get a first-class meeting agent").*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.9.0 | Agent scaffold — `src/brainchild/agents/claude/` with `config.yaml` + empty `skills/` placeholder. Agent block: `name=Claude`, `trigger_phrase=@claude`, tagline "Claude Code, in your meeting — plan-first, sandboxed, ready to delegate", captions off by default (chat-directed delegation, not ambient transcript listening). `claude-code` MCP enabled with beefed-up description calling out "built by Brainchild — sandboxed git worktree, nothing lands in your working tree until you approve" + `tool_timeout_seconds: 600`. All 9 other bundled MCPs present but `enabled: false` — wizard/first-run auto-import flips them on based on user's Claude Code config. Ground_rules: plan-first disposition + delegation rules inherited from engineer. Skills paths: `~/.claude/skills` first so user skills override bundled. | ✅ | ~30m |
| 15.9.1 | `_ensure_user_agents` sync-on-every-run — change the first-run seeding behavior from "copy bundled only if user dir is empty" to "copy any bundled bot missing from user dir." Lets new bundled agents (like `claude`) reach existing users without forcing them to delete `~/.brainchild/agents/`. Tradeoff: a user who deliberately deletes a bundled bot sees it reappear on next run — delete again. Smoke-tested against a fake-home fixture with existing `pm` — confirms all four bundled land including new `claude`. | ✅ | ~10m |
| 15.9.2 | `pipeline/claude_code_import.py` — auto-discovery helpers. Three discovery sources: (a) `~/.claude.json` top-level `mcpServers` (locally-configured stdio and HTTP/SSE), (b) `claude mcp list` subprocess output (the authoritative source for claude.ai-hosted connectors like Gmail/Drive/Linear that don't live in any local file — regex-parsed, 10s timeout, degrades to empty on format drift or non-zero exit), (c) `~/.claude/skills/` and `~/.claude/CLAUDE.md`. Transport classifier: stdio passthrough, HTTP/SSE auto-wrapped via `mcp-remote@0.1.38` (same pinned version as bundled Linear/Sentry) with `auth: oauth` + `auth_url` so the existing 15.7.3 cache-path check and `brainchild auth <name>` flow work unchanged on imports. Public `claude_code_installed_and_logged_in()` wraps `readiness._probe_claude_code(check_auth=True)`. `append_env_placeholders` appends `# VAR=` lines idempotently (checks both set and placeheld vars). 32 unit tests in `tests/test_claude_code_import.py` cover classify/slugify/read/extract/discover-CLI/discover-all/skills/CLAUDE.md/env-append + edge cases (missing file, malformed JSON, subprocess errors, trailing-newline handling). | ✅ | ~1.5h |
| 15.9.3 | Wizard hook — four surgical edits to `pipeline/setup.py`: (a) step 1 picker blocks selection of `claude` preset when Claude Code not installed/logged in (prints hint + recurses back to gallery — "option b" from the plan); (b) `_edit_preset("claude")` invokes new `_auto_import_claude_setup(state)` which calls `discover_all_mcps()`, merges imported MCPs into `state.bot_cfg["mcp_servers"]` (dedup by name — user curation wins), pre-populates `state.user_sources` with `~/.claude/skills/` dirs, stashes CLAUDE.md on state for step 4; (c) step 2 end: if `based_on == "claude"`, collect env-var refs from newly-enabled imports and call `append_env_placeholders` against `.env`; (d) step 3 top: surface auto-imported skills summary; (e) step 4: offer inline "Append ~/.claude/CLAUDE.md to ground rules? [y/N]" when the file exists. Smoke: `_edit_preset("claude")` against live Claude Code setup discovers 4 hosted MCPs (Gmail/Drive/Calendar/Linear) + 2 user skills correctly. | ✅ | ~1h |
| 15.9.4 | CLI first-run hard-fail + auto-import — two hooks in `__main__.py::_run_bot`. (a) Hard-fail gate: when agent name is `claude`, run `claude_code_installed_and_logged_in()` before any config loads; on failure, print a clear stderr block pointing at claude.ai/code + `claude login` and exit 2. (b) `_bootstrap_claude_imports()` helper: idempotent first-run import via a `_claude_import_done: true` marker in the agent's `config.yaml`. First run loads config, calls `discover_all_mcps()`, merges new entries, appends env-var placeholders to `~/.brainchild/.env`, writes config back (PyYAML round-trip — comments lost on first write, matches wizard's existing behavior), sets marker. Subsequent runs skip entirely — keeps the ~3s `claude mcp list` shell-out off the hot path. Smoke: end-to-end against throwaway home (10 MCPs → 14 after, marker set, second call no-op); hard-fail mock test confirms exit 2 + clean stderr. Regression: 80+ tests green across 4 adjacent suites (test_claude_code_import, test_1574_readiness, test_chat_hardening, test_mcp_client). | ✅ | ~45m |
| 15.9.5 | Docs sync — roadmap (this block), `docs/deferred-live-tests.md` (4 new smoke entries: wizard-preset live pick, CLI first-run with real `~/.claude.json`, hard-fail path on a machine with `claude` CLI uninstalled, `claude mcp list` format-drift canary), `docs/handoff.md` session note, `CLAUDE.md` bundled-agent list mention. | ✅ | ~20m |

**Phase total: ~4h — shipped session TBD.**

**Key implementation finding.** `claude mcp list` is the authoritative source for claude.ai-hosted connectors (Gmail, Drive, Calendar, Linear — the MCPs most Claude Code power users actually have). `~/.claude.json#mcpServers` was the obvious candidate but is empty for users who configure MCPs via claude.ai account connectors rather than manual JSON edits. Parsing the CLI text output (10s timeout, non-match skip, degrades to empty) is the only way to surface these. All discovered hosted MCPs auto-wrap via `mcp-remote@0.1.38` with `auth: oauth`, so the existing 15.7.3 OAuth cache-path check and `brainchild auth <name>` flow catch them for free — no new auth code.

**Rejected scope.** Five translated slash-command skills (plan, meeting-recap, security-review, file-bug, code-cleanup) — session-151 audit inventoried them but user deferred post-launch per "ship the agent, skills later." Hosted-MCP CLI parse has a known brittleness axis: if Claude Code changes `claude mcp list` output format, discovery silently returns zero results and the import module degrades to `~/.claude.json` only. Monitor in Phase 17 upstream-drift watch.

---

## Phase 15.10: Pre-Launch Hardening & Live-QA Prep

*Pre-launch quality block added session 159. Three deliverables feed into Phase 14.8's fresh-user live-QA: (1) a clustered edge-case audit of the codebase to flag and fix hardening gaps before they hit a live meeting, (2) a research-backed list of the ten most common Claude Code use cases adapted for group-meeting settings so 14.8's live-QA has a real test script for the flagship claude agent, (3) a live-test checklist for pm / engineer / designer so 14.8 also validates the non-claude bots head-on. **Sequenced after 14.10–14.11 and before 14.5 install.sh + 14.8 live-QA** — fixes land on the packaged artifact, test scripts exist before the marathon. 15.10.3 doubles as launch-campaign material — the list of ten claude use cases is the second hero-framing post of Phase 16.4 in raw form.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 15.10.1 | Edge-case audit — cluster list + sequencing (me). Enumerate all code areas/flows in the codebase worth an edge-case sweep: Meet join + chat-panel open, chat polling + trigger detection, 1-on-1 detection + participant-based auto-leave, tool-confirmation flow, MCP stdio lifecycle + reconnect, OAuth cache + `brainchild auth <name>` flow, wizard flow, setup bootstrap + migration shims, config loading + `BRAINCHILD_BOT` gating, LLM provider switching, claude-code delegation path, skills loading + progressive disclosure, caption pipeline (if enabled), meeting-record JSONL append, guardrails, `session.save_debug`, participant-count heuristics. Produce ordered list of ~8–12 clusters with estimated per-cluster review time; user approves the sequence before 15.10.2 starts. | ⬜ | ~45m |
| 15.10.2 | Edge-case audit — per-cluster review (sequential). For each cluster from 15.10.1: read the relevant code paths + tests, flag candidate edge cases, separate into (a) already-covered (tests or runtime guards exist), (b) unlikely-in-practice — skip, (c) worth fixing. Fix (c) as a per-cluster commit. One cluster per working block to avoid overload; user checkpoints between clusters before moving to the next. | ⬜ | ~3–5h (cluster total) |
| 15.10.3 | Claude Code top-10 use-case research (me, web-searched). Build a research-backed list of the ten most common Claude Code use cases, filtered and weighted for the group-remote-meeting context (pair-coding call, sprint triage, design review with devs present, PRD review, bug standup, etc.) — not solo-at-a-terminal cases. Each use case gets: one-line framing, the typical trigger phrase a user would say in a meeting, the MCP(s) it exercises, and a clear pass/fail signal. Output as `docs/claude-use-cases.md` so it doubles as a launch-campaign asset for Phase 16.4's second hero post. User executes this as the (e) block of Phase 14.8. **Shipped session 159** — `docs/claude-use-cases.md` (10 cases, ordered by meeting-frequency × value): PR review, Sentry bug triage, codebase walkthrough, live coding delegation, sprint scope estimation, refactor/migration plan, test generation, Figma handoff spec, PRD-from-discussion, release notes. Each case has trigger phrase + MCP(s) + pass/fail signal. Includes "what didn't make the cut" tail (docs gen, `--continue`, scheduled runs, Unix-utility mode, image analysis, manual subagent invocation, manual worktree juggling — solo-CLI patterns). Sources: Anthropic team-workflow blog post, official Common Workflows docs, Code Review blog post, two 2026 community workflow surveys. | ✅ | ~0m remaining |
| 15.10.4 | Other-bot live-test checklist (me). Produce a flat checklist of live-meeting test scenarios for pm / engineer / designer: trigger phrase + 1-on-1 mode, intro-on-join, MCP failure banner, participant-based auto-leave grace, tool confirmation, read-auto allowlist, skills firing, captions (on pm/designer), delegate flows. Output as `docs/other-bot-live-tests.md`. User executes this as the (f) block of Phase 14.8. | ⬜ | ~30m |
| 15.10.5 | `brainchild worktrees` CLI for live worktree management. Two subcommands: `list` (path + age + branch + landed-yes/no) and `clean` (interactive prune of unlanded worktrees ≥30 days). Auto-prune on successful `land_worktree_changes` in `agents/engineer/claude_code.py`. Background: session 164 found 11 stale worktrees in `.claude/worktrees/` from prior delegate calls — they're cheap (mostly hardlinks) but accumulate visually and Ctrl-C'd delegations leave orphans on disk that `git worktree prune` won't remove because the metadata is still valid. Never auto-delete unlanded work (assume in-progress = the user's); only landed-and-merged or 30-day-old-and-unlanded. Discovered while wiring stream-json progress (commit `d0ff5a7`) — same Ctrl-C scenario was the trigger. **Session 164 deferred — ship before 14.5 install.sh so the launch artifact has a clean disk story.** | ⬜ | ~1h |

**Phase total: ~6.5–8h**

---

## Phase 16: README & Launch ← MVP GATE

*Ship it.*

| Step | Description | Status | Est. |
|------|-------------|--------|------|
| 16.1 | Rewrite `README.md` — what it is, quick start, architecture, "meetings that produce artifacts" positioning. **Install mechanism is the curl one-liner** (`curl -fsSL brainchild.build/install \| sh`) from Phase 14.5, not `git clone`. **Quick-start must read as 3 steps** after install (choose an agent → add your keys/power-ups → join a meeting); agents gallery (`agents/`) is the entry point; link `claude-code` as the canonical starter. Includes: MCP compatibility notes (tested servers + known quirks), BYOMCP failure patterns and mitigation guidance, one annotated example config. Explicit "Claude Code in your Google Meet" framing in the hero. | ⬜ | ~4h |
| 16.2 | Demo video/GIF — 30s screen recording embedded at top of README. **Hero hook: an artifact (Linear ticket / GitHub comment) materializing in chat while the speaker is still mid-sentence.** Visual surprise is the point; feature enumeration is not. Second demo (optional): drag-and-drop a skills folder and watch behavior change. | ⬜ | ~2h |
| 16.3 | Landing page (Tier 1) — single-page site for `brainchild.build` with: hero pitch ("Claude Code in your Google Meet"), **copy-paste `curl \| sh` one-liner as the above-the-fold install CTA** (the install script hosted at `brainchild.build/install` from Phase 14.6 is already live by this point), 60-second screen-recorded demo (the artifact-in-chat moment from 16.2), three-step quickstart preview (post-install), link to GitHub repo, "book a live demo with the maintainer" Calendly link as the soft-touch alternative. Static — no auth, no hosted bot, no API keys, no infra cost. Hosted demo (Tier 3 from session 105 discussion) explicitly out of scope for MVP — that's Joinly's lane and would undermine the "install and run" promise. Plain HTML or a minimal Astro/Next site is fine; deploy via Vercel/Netlify or Cloudflare Pages (latter reuses the 14.6 DNS wire-up). | ⬜ | ~2h |
| 16.4 | Launch campaign prep — draft 3 hero-framing posts (see `docs/mvp-bar.md` Launch Strategy section) tailored to their target channels: (a) "Claude Code in your Google Meet" for r/ClaudeAI + Claude-focused creators; (b) "The AI in my standup filed 3 Linear tickets before I finished talking" for r/ExperiencedDevs + eng newsletters; (c) a role-specific build tied to the second chat-native agent from 15.5.2. Prepare the seeded-PR plan for `agents/` (personas + use cases, 3 PRs queued for week 1). Direct-outreach shortlist: 10–20 Claude Code power users in SF. Identify 2–3 AI/PM newsletter brainchilds for earned or paid mentions (Pika's playbook). | ⬜ | ~3h |

**Phase total: ~11h**

---

**MVP total: ~34–37.5h remaining** (session 159 shipped Phase 14.10 = 2h closed + added Phase 14.11 wizard MCP copy tweak + Phase 15.10 pre-launch hardening cluster + expanded 14.8 to absorb 15.10.3 + 15.10.4 live-QA = +~8–10h pre-launch; session 150 shipped Phase 15.8 = 1.5h closed; session 149 added Phase 15.8 CLI verb rename = +~1.5h pre-launch; session 147 added Phase 15.7 MCP hardening + team completion = +~9–11h pre-launch; session 142 progress: DNS async = 30m shaved, 14.3 Commit 1 package-restructure core = 1.5h shaved, 14.3 Commit 2 agents → templates = 2h shaved, 14.3 Commit 3 main() export + smoke test = 45m shaved; session 141 reshape of Phase 14 from "minimal packaging" to "curl-installable package" added ~11h of new work — package restructure + install.sh + DNS/hosting + clean-mac smoke test that absorbs the five-session-overdue live-smoke into 14.8). **Execution order (revised session 159):** 12 ✅ → 15.5.0–15.5.7 ✅ → 13 ✅ → 15.6.1 ✅ → ~~15.6.2~~ (moved to post-MVP gallery) → 14.3 ✅ → 14.4 ✅ → 15.7 ✅ → 15.8 ✅ → 15.9 ✅ → 14.10 ✅ → **14.11 (wizard MCP "context, not just tools" copy tweak)** → **15.10 (pre-launch hardening — clustered edge-case audit + claude top-10 use-case research + other-bot live-test checklist)** → 14.5 (install.sh) → 14.6 (DNS + host) → 14.7 (uninstall) → 14.8 (clean-mac fresh-user live-QA marathon — absorbs 131+132+136+137 + 15.10.3 + 15.10.4) → 15.1 (Linux run) → 16 (README + landing + launch). PyPI upload deferred to post-MVP (see Packaging & Community) — shape B (`uv tool install git+https://...`) gives the same install UX without PyPI account overhead. Core-repo security (15.6.1) is uncuttable and shipped. Gallery trust model is the post-MVP evolution of what 15.6.2 was trying to solve. **Launch target: session 159 added ~8–10h of pre-launch hardening (edge-case audit + live-QA scripts + MCP wizard copy); previous Apr 28–30 aspirational / May 1–2 acceptable slip pushes out correspondingly. Session 147 absorbed the 15.7 scope as a deliberate launch-quality bet — shipping hardened MCP flows and a complete team-arc story is worth 2 days of calendar. Session 159 is the same bet for pre-launch hardening and a real flagship-agent test script.**

---

## Phase 17: Upstream Drift Monitoring

*First post-MVP priority. Detect silent breakage from upstream changes (model deprecations, MCP tool schema drift, DOM changes, dep bumps) before users hit them in a live meeting.*

**Why:** The product sits on top of many moving surfaces we don't control — LLM APIs, hosted MCP servers, pinned binaries, Google Meet DOM, Playwright/Chromium, virtual audio stack, OS APIs. Nothing currently watches any of them. Failures only surface mid-meeting.

| Step | Description | Est. |
|------|-------------|------|
| 17.1 | Automated diff checks (weekly cron, opens GitHub issue on diff): (a) OpenAI + Anthropic model-list endpoints vs. configured models; (b) `list_tools()` schema snapshot diff per configured MCP server (catches hosted Linear/Gmail/Calendar changes + local GitHub binary drift); (c) GitHub releases for pinned MCP binaries (GitHub MCP `v0.32.0`, `mcp-remote`); (d) `pip list --outdated` filtered to critical deps (openai, anthropic, playwright, faster-whisper, mlx-whisper, rumps, kokoro); (e) Python + macOS EOL date reminders | ~3h |
| 17.2 | Weekly smoke canary — headless run joins a test Meet, sends a chat message, invokes one read tool per configured MCP server, asserts success. Single pass catches Meet UI drift, OAuth expiry, hosted MCP outages, BlackHole/mpv regressions, Playwright/Chromium bumps | ~2h |
| 17.3 | Manual checkpoint runbook — quarterly ~15min checklist for fuzzy surfaces not amenable to automation: Google Meet / OpenAI / GitHub / Linear ToS pages, Meet UI walkthrough, macOS version compatibility, kext/codesigning requirements, Kokoro model repo status | ~1h |

**Phase total: ~6h**

---

## Post-MVP

*Everything below was scoped out of the 7-day MVP window. Prioritize based on user feedback after launch.*

### MCP Enhancements
| Item | Origin | Description |
|------|--------|-------------|
| Meeting-history MCP + skill | session 161 (2026-04-25) | Build a local MCP server that exposes the per-meeting JSONL at `~/.brainchild/history/<slug>.jsonl` as searchable/queryable history (e.g. `search_transcript`, `get_window(start, end)`, `summarize_speaker(name)`), plus a bundled skill that teaches the bot when to call it. Goal: stop carrying the rolling caption tail in the system prompt at all — the LLM pulls only the slice it needs, on demand, when asked about earlier in a long meeting. Removes the `llm.history_messages` cap as a context-window pressure point and lets a 1-hour meeting be referenced without prompt bloat. Pairs with skill frontmatter `mcp-required: [meeting-history]` so the wizard locks the toggle on. |
| Configurable confirmation modes | 10.2 (original) | Full 4-mode system: `auto-all`, `read-auto`, `confirm-all`, `session-trust` + `batch_preview` toggle |
| Read-only tool classification engine | 10.3 (original) | Auto-classify tools as read/write from descriptions at discovery time |
| Managed MCP client layer | 10.6 | Point at an MCP proxy/gateway (Cloudflare, etc.) instead of local stdio servers |
| Pin MCP server versions | 10.7 | Lock `mcp-remote` and GitHub binary to specific versions in config |
| MCP server CI health check | 10.8 (original) | CI job that starts servers + dry-run tool calls, alert on failure |
| Runtime failure monitoring dashboard | 10.9 (original) | Per-server failure rate tracking, threshold alerts, diagnostics surface |
| Idempotency guards | 10.10 | Detect duplicate tool actions from repeated requests; dedup logic beyond write confirmation |
| Parallel tool calls | 11.2 | Currently disabled on both providers (`parallel_tool_calls=False` on OpenAI, `disable_parallel_tool_use=True` on Anthropic) so the one-tool-at-a-time loop in `LLMClient.ask()` stays safe. Re-enable for skills that fan out across MCP servers (e.g. "what's on my plate across Linear + GitHub?"). Requires `LLMClient` to execute N tool_calls per turn and feed back N tool_results before the next LLM turn. |

### Multi-Modal & Voice
| Item | Origin | Description |
|------|--------|-------------|
| Local LLM support | 11.3 | Ollama/llama.cpp for zero-API-key deployment. Experimental tier — local models are weak at tool use |
| Voice: premature finalization fix | 11.5 | Resolve 0.7s silence threshold cutting off mid-sentence |
| Voice: TTS reliability | 11.6 | Error handling and retry logic for TTS |
| Voice: partial-wake validation | 11.7 | Validate idea #6 from latency.md |
| Voice: MODE config key | 11.8 | `voice` \| `chat` \| `both` in config.yaml |

### Testing & Hardening
| Item | Origin | Description |
|------|--------|-------------|
| OpenAI model matrix | 12.1 (original) | Full matrix: GPT-4.1-mini, GPT-4.1, GPT-4o, o3-mini |
| Full MCP pressure testing | 12.2 (original) | Every tool x every model, explicit + implicit + indirect requests |
| DOM regression test suite | 12.3 / 9.2 | Automated tests against live Meet on a schedule |
| Self-healing selectors | 12.4 / 9.3 | Fallback strategies when primary selectors fail |
| Edge case pass | 12.5 / 9.16 | Boundary conditions: empty meetings, rapid join/leave, Unicode, concurrent tool calls, memory leaks |
| Latency audit | 13.3 / 9.9 | Profile end-to-end chat path, shave unnecessary delays |
| Comprehensive error handling | 13.4 / 9.10 | Rate limiting, runaway loop prevention, full graceful failure pass |
| Telemetry / diagnostics | 13.5 | Opt-in anonymous usage stats |
| Dependabot + pip-audit | 13.6 | Automated dependency PRs + CVE detection in CI |
| Log cleanup (advanced) | 9.8 | Beyond MVP log cleanup — structured logging, log rotation |
| Idempotency guards | 9.14 | Duplicate action prevention |

### Packaging & Community
| Item | Origin | Description |
|------|--------|-------------|
| PyPI publication | session 141 (Phase 14 reshape) | Pre-MVP ships via `uv tool install git+https://github.com/.../brainchild.git` — no PyPI account, no metadata polish, no ongoing upload workflow. Post-MVP: publish `brainchild` to PyPI (the reservation `brainchild@0.0.0.dev0` already exists; first real release at `0.1.0`). Unlocks `pipx install brainchild`, `uvx brainchild`, `uv tool install brainchild` (no git URL), and cleaner upgrade UX via `uv tool upgrade brainchild`. ~1.5h one-time (account/token + build + `twine upload` + verify). Cheap marginal cost once the `pyproject.toml` from 14.4 exists, but not MVP-essential — our audience isn't primarily Python-native developers, and the curl one-liner is the hero install path either way. |
| Python version matrix | 14.3 (expanded) | Test across 3.11, 3.12; lockfile tooling |
| CI/CD pipeline | 14.4 | Automated tests on PR, release tagging, PyPI publish |
| Contributing guide | 14.5 | Code standards, PR process, how to add MCP servers |
| MCP compatibility matrix (full) | 14.6 | Detailed tested servers doc with model-specific behavior notes |
| Changelog / release notes | 14.7 | CHANGELOG.md, semver tagging |
| Issue templates | 14.8 | GitHub issue/bug/feature request templates |
| Code of conduct | 14.9 | Community standards |
| Architecture docs | 14.10 | Visual diagrams for contributors |
| Example configs (multiple) | 14.11 | Pre-built configs for common setups (minimal, full MCP, local-only) |

### Community & Gallery
*Decoupled from the core repo after session 139's ClawHub research. Original plan was an in-repo `agents/CONTRIBUTING.md` + CODEOWNERS gate (Phase 15.6.2); rejected because (a) the solo maintainer can't realistically review community MCP server commands + skill markdown for prompt injection at scale, and (b) any central-registry shape with a "sort by downloads" UI will be weaponized — ClawHub's data shows popularity-sorting becomes a malware discovery tool. Gallery model is the defensible shape: contributions live outside the core repo; users read `config.yaml` + `README.md` on GitHub, then clone the folder into their own `agents/` directory; `brainchild <name>` works unchanged. User owns the trust decision (the `npm install` / `brew tap` / Claude Desktop MCP precedent). No pre-flight consent screen — that's security theater when the user will approve regardless, and it implies Brainchild-side review we aren't doing.*

| Item | Origin | Description | Est. |
|------|--------|-------------|------|
| Gallery index repo | session 139 (reshape of 15.6.2) | Separate repo (working name `brainchild-agents-gallery`) with a single README indexing community-contributed agents: name, one-liner, author, link to the author's repo or a sub-folder. No auto-install, no central registry, no ranking by downloads. Optional "reviewed by maintainer" badge for the small set we've eyeballed. | ~1h |
| Skill-markdown untrusted wrapper | session 139 | **The one non-theater mitigation we owe in the core.** Extend the Fix #1 delimiter-wrapper machinery (`<spoken>` / `<tool_result>` with ZWSP close-tag neutralizer, `pipeline/llm.py`) to also wrap skill-file content loaded into the system prompt inside `<untrusted_skill name="…">…</untrusted_skill>`. Defends against the ClawHub-style "skill markdown tells the AI to walk the user through running malware" attack. Invisible to the user; purely code-internal hardening. Applies equally to bundled + user-added skills. | ~1h |
| `docs/security.md` gallery-trust section | session 139 | Add a "Community agents" subsection: explicit gallery trust model, "read `config.yaml` and `README.md` before you clone" as the stated user responsibility, no implication of Brainchild-side review. Cross-link from the gallery repo's README. | ~30m |
| (Future, not near-term) Verified-publisher model | session 139 (ClawHub post-mortem) | If adoption scales past solo maintainer: block-before-publish scanner (regex + LLM-intent analysis over skill markdown), account-age gate, community reporting with auto-hide at N reports, per-agent malicious-skill registry. Directly mirrors what ClawHub retrofitted post-incident. Team-sized project, not a solo item. | — |
| Item | Origin | Description |
|------|--------|-------------|
| Participation scalar | session 105 | Configurable how-much-the-bot-talks dial. Modes: `silent` (notetaker, never replies — implementation is trivial: skip `send_chat`), `prompted` (current default — only responds when triggered or in 1-on-1), `interjects` (LLM judges when its input is wanted and chimes in unprompted). Lives as `agent.participation` in `config.yaml`. `silent` is ~30min and could underpin a `notetaker` agent flavor. `interjects` is the hard one — needs a tuned prompt + heuristic gate (e.g. silence threshold + topic-match score) to avoid being annoying. Defer the full thing; consider shipping `silent` early as a quick gallery-multiplier. |

### Setup & Onboarding
| Item | Origin | Description |
|------|--------|-------------|
| Setup wizard (full) | 15.1 | `brainchild setup` — full guided experience: API key entry, voice selection, MCP server auth, model selection UI, rerun-safe state. Minimal version shipped in Phase 15.5.2 (agent-picker + keys + config write); this is the expanded version with voice, MCP OAuth, and polished UX. |
| Roster expansion | 15.5 | Grow `agents/` beyond the 3 launch members (engineer, pm, designer). **Week-1 seeded PRs (committed session 105):** `debriefer` (joins with prior-meeting context via Granola or local meeting-record search), `spec-scribe` (drafts a PRD from product discussion, opens GitHub PR with the doc), `incident-commander` (opens GitHub issues during incident calls, cross-links, pings oncall), `customer-call` (joins sales/CS calls, files feature requests as Linear tickets in real time), `research` (live web lookup via Tavily MCP, drops citations mid-meeting). **Heavier candidates pending the security pass:** `engineer-bash` (engineer + bash/shell MCP for direct code execution), `scraper` (Playwright MCP for live web scraping), `computer-use` (Anthropic computer-use). Translator and Shopify-expert also queued as voice/vertical examples. Curate community contributions, maintain an `awesome-brainchild` list for externally-hosted roster members. Ongoing post-launch. |
| MCP OAuth setup step | 15.2 | Authenticate each MCP server during onboarding so tokens are cached |
| Auto-populate per-MCP hints | 15.3 | Resolve identity (GitHub `get_me`, etc.) during onboarding |
| First-run smoke test | 15.4 | Automated health check after setup |

### Stealth / Privacy Mode
| Item | Origin | Description |
|------|--------|-------------|
| Privacy mode | session 113 | Brainchild joins a meeting silently — scrapes captions and chat without posting in meeting chat. User communicates with Brainchild via a separate sidebar channel (TUI, web dashboard, or Slack DM — channel TBD, each is a real build). Use case: passive meeting intelligence without other participants knowing. Requires a non-Meet communication surface for the user, which is the bulk of the work. Builds on `captions_enabled` + meeting-record JSONL but needs a new I/O path. |

### Screen Share / "Pull Up X"
| Item | Origin | Description |
|------|--------|-------------|
| Presentation mode | session 113 | User says "pull up the Q3 deck" and Brainchild shares its screen with the relevant document open. Playwright can drive screen sharing programmatically, but the hard part is finding, opening, and navigating to the right content — effectively browser-agent territory. Depends on privacy mode or a similar command channel so the user can direct Brainchild without typing in meeting chat. Furthest-horizon item; post-privacy-mode. |

### Platform Expansion
| Item | Origin | Description |
|------|--------|-------------|
| `ChatConnector` interface | 17.1 | Abstract chat read/write for multi-platform support |
| Zoom connector | 17.2 | Chat DOM selectors, join flow, auth for Zoom |
| Microsoft Teams connector | 17.3 | Chat DOM selectors, join flow, auth for Teams |

### Hosted Brainchild — Inviteable Bot
*The north-star product vision: Brainchild is a service users invite to their meeting by calendar invite or email (e.g. `brainchild@yourdomain.com`), no local install required. Shopify-style: "invite the bot, it joins and participates as an expert." Recall.ai-shaped architecturally (one ephemeral container per meeting), not daemon/API-shaped. Explicit scope note: this phase does NOT require the Layer 7 "headless core + chat API + reference client" refactor — the current monolithic Python process IS the container. Path B from the session 92 discussion.*

| Item | Description | Est. |
|------|-------------|------|
| Containerize the monolith | Package the existing chat-mode `__main__.py` flow into a Docker image. Strip macOS-only paths at build time; target Linux-in-container as the canonical runtime. Build on top of existing Phase 3/4 Linux adapter work. One container = one meeting, stateless, dies on meeting end. | ~4h |
| Meeting-invite receiver | A service endpoint that detects when the hosted bot is invited to a meeting. Options: (a) email listener that parses calendar invites sent to `brainchild@domain`, (b) Google Calendar webhook on a service account. Extract the meeting URL; hand off to the orchestrator. | ~6h |
| Orchestrator | On each new invite: spawn a fresh container pinned to that meeting URL, with per-user config/skills/MCP credentials mounted in. Tear it down when the meeting ends or times out. Target platform: Kubernetes, Fly.io Machines, or similar lightweight container-per-request runtime. | ~8h |
| Hosted Google auth | The bot needs to log into Google Meet as a real account to join. Single service account + domain-wide delegation, OR per-user OAuth where each user grants the hosted bot permission to join on their behalf. Second option is the OSS-friendly path. Non-trivial auth work; tokens stored encrypted per-user. | ~6h |
| Multi-tenant config isolation | Each user's skills, MCP credentials, and system prompt injected into *their* container only. Process-boundary isolation (one container per user-meeting) means no in-core multi-tenancy logic needed, but the config-mounting path has to be bulletproof. | ~3h |
| Observability + kill switch | Per-container logs aggregated somewhere queryable. Global kill switch for abuse. Rate limiting per user. Cost tracking (each meeting burns LLM tokens + container minutes). | ~4h |
| Billing / limits (if commercial) | Only if this goes beyond "free service for friends." Out of scope until product direction settles. | — |
| Hosted demo bot (try-before-clone) | Tier 3 from session 105 discussion. Landing-page CTA spawns a fresh Meet with Brainchild pre-joined for a ~5min capped session, costs eaten by us (or BYOK). Requires bot pool, key management, rate limiting, abuse handling — overlaps significantly with the rest of this phase. Explicitly **not** MVP because it leans into Joinly's hosted-service lane and undermines the "clone and run" wedge. Revisit only if landing-page conversion data shows the clone step is the funnel killer. | ~6h on top of the rest of this phase |

**Phase total: ~30-35h** (excluding billing). Large phase; not a single-session item. Real prerequisite: the `ChatSource` seam lands first so the container doesn't carry Meet-specific quirks into its generic chat loop.

---

### OSS Ethos — Audit Fixes
*Findings from the session 91 read-only audit (`docs/oss-audit-report.md`). **All 10 layers parsed in session 92.** Two items promoted to Phase 11 pre-MVP (11.6 persistence, 11.7 optional provider keys). Dropped: L6-b, L7, L8-a. Absorbed: L8-b into L1-c; L9-a into L3-b + L3-d; L9-b + L10-b into L1-b. L10-a is a compliance affirmation (zero-telemetry verified intact). The remaining items below are split into two buckets: **fast-follow candidates** (small enough to squeeze in pre-MVP if time permits) and **post-MVP** (must wait — mature-product shape or too large).*

#### Fast-follow candidates *(pre-MVP if time permits, else immediately after launch)*
*Total: ~10h 45m across all five — none individually gates the demo, but bundled they meaningfully sharpen the OSS shape.*

| Item | Origin | Description | Est. |
|------|--------|-------------|------|
| Configurable log destination + level | L1-b + L9-b + L10-b | Hardcoded `/tmp/brainchild.log` in `__main__.py:214` (referenced in user-facing error strings in `llm.py` and `chat_runner.py`). `/tmp` is world-readable on multi-user systems and cleared on reboot. Log level is also hardcoded to DEBUG — at that level, logs contain tool call arguments, first 500 chars of tool results, and user utterances (local-only, but users may not realize). Add `logging.destination` AND `logging.level` to `config.yaml` with platform-appropriate defaults (e.g. `~/.brainchild/brainchild.log`, level `INFO`); resolve path once and interpolate into user-facing strings instead of hardcoding; at `INFO` level, skip content-dump debug lines; sanity-pass for token/secret redaction. | ~1.5h |
| `ChatSource` seam + stdin source | L1-a + L2 + L3-c | Today `ChatRunner` can only be driven by the Meet connector — every end-to-end test spins up Playwright + Chromium, and Meet-specific concerns (participant count, DOM echo dedup via duplicate-ID quirk, `ONE_ON_ONE_THRESHOLD`, `PARTICIPANT_CHECK_INTERVAL`, alone-exit grace) live inside the generic chat loop. Extract a `ChatSource` protocol (`read_chat`, `send_chat`, `is_connected`); Meet connector becomes one implementation; push Meet-specific concerns down into `MeetAdapter`; add a `StdinChatSource` for fast local dev and isolated integration tests. While refactoring, promote the "leave when alone" behavior to a proper config toggle (`leave_when_alone: true\|false` + existing `alone_exit_grace_seconds`) and drop `ONE_ON_ONE_THRESHOLD` as a magic constant in favor of a simple "≤1 human" check. Standard ports-and-adapters pattern. Not user-facing — internal tool. Also a prerequisite for the future Hosted Brainchild phase. | ~6h |
| Per-server quirk config | L3-b + L9-a (partial) | Today Linear's `limit`-arg stripping and GitHub's `get_me` identity injection are hardcoded as `if server == "linear"` / `if tool == "github__get_me"` branches across `chat_runner.py` and `mcp_client.py`. Adding a Notion/Slack quirk today means editing three files. Add declarative fields to each MCP server block: `strip_args: [name, ...]` (framework strips these from any tool call on that server) and `identity_tool: <tool_name>` (framework calls it at startup, resolves the identity value, and injects guidance via an overridable `prompts.identity_guidance` template). Removes all per-server branches. Absorbs `inject_github_user` from L9-a. | ~2h |
| Open provider registry | L4-b | `pipeline/providers/__init__.py:11-29` is a closed two-way `if name == "openai" / elif "anthropic" / else raise`. Swap for a module-level `PROVIDERS = {"openai": _build_openai, "anthropic": _build_anthropic}` dict plus a public `register_provider(name, factory)` so users can plug in Ollama/Gemini/local proxies without forking. | ~30m |
| Framework-owned strings as config templates | L3-d + L9-a (partial) | Strings like *"Still working on that..."*, *"That took too long — no response after Ns. Try again."*, *"Sorry, that tool call failed. Check the logs for details."*, the confirmation-request prompt, and the history-truncation notice are hardcoded in `chat_runner.py:263-414`. Also absorbs `inject_mcp_status`: add a global `prompts.mcp_server_unavailable` template with `{server_name}` and `{log_path}` interpolation. Move each string to a named template in `config.yaml` under a `prompts.*` block. | ~45m |

#### Post-MVP *(must wait — size or shape)*

| Item | Origin | Description | Est. |
|------|--------|-------------|------|
| Skill manifest + neutral `ToolSchema` + install consent | L3-a + L4-a + L5-a + L5-b | The largest item in the OSS fixes list; bundles four audit findings because they describe the same missing thing from four angles. Define a skill manifest (YAML alongside or replacing the current `mcp_servers` block) declaring each tool's name, description, input schema, and sensitivity tier (`read` \| `write` \| `sensitive`). Introduce a neutral `ToolSchema` dataclass in `pipeline/providers/base.py`; `MCPClient` discovers MCP tools and converts them to `ToolSchema` at ingestion, merging declared manifest metadata (especially sensitivity). Each provider translates `ToolSchema` at its own boundary; rename `get_openai_tools` → `get_tools`. `ChatRunner` derives confirm-vs-auto from `ToolSchema.sensitivity` (unknown = confirm). Install-time consent: on first-enable of a skill, print the manifest (tools + sensitivities) and require a yes/no to proceed; cache consent in user state. Sensitive-tier tools always prompt mid-conversation regardless of install consent. Replaces the hardcoded `READ_TOOLS` set (L3-a), the OpenAI-shaped tools surface (L4-a), and the MCP-SDK-class-as-neutral-type (L5-b) all at once; delivers the named ethos shape commitment (L5-a). Ship default manifests for Linear/GitHub in the repo; document the format so users/authors can write their own. Too large for MVP: ~10-14h (manifest format + loader, `ToolSchema` + MCPClient conversion, provider translation updates, sensitivity-driven confirmation + READ_TOOLS removal, install consent flow, docs + example manifests, integration buffer). | ~10-14h |

---

## Key Decisions

- **Architecture:** Three-layer separation (pipeline / connector / platform shell)
- **Primary platform:** Local machine (macOS + Linux). Cloud is upgrade path.
- **Input (macOS Meet):** DOM caption scraping. Audio pipeline preserved behind `connector.type: audio`.
- **STT (audio fallback):** mlx-whisper base on macOS; faster-whisper base on Linux.
- **LLM:** GPT-4.1-mini (default), Claude (alternative). User picks provider in config.
- **TTS:** Three-tier — local (Kokoro) / openai / elevenlabs. Default: Kokoro af_heart.
- **Meeting detection:** Browser-based Google Calendar scraping (30s interval).
- **Licensing:** MIT
- **Python target:** 3.11
- **Pivot (April 2026):** Chat-first v1, voice layered on later. Motivated by real user demand for task delegation via meeting chat.
- **MVP scope (April 2026):** Google Meet only, Mac + Linux. Original 7-day ship window (April 12–19) elapsed; revised launch target (session 141): end of week Apr 25–26 aspirational, Apr 27–28 acceptable slip. Scope expansion: Phase 14 grew from "clone and run" to "curl one-liner + package restructure" after session 141 concluded a real install path is load-bearing for MVP credibility. See `docs/mvp-bar.md` for the full MVP bar definition.
- **MVP model support (April 2026):** OpenAI + Anthropic. No local models — too weak at agentic tool use, would undermine the demo.
- **V1 positioning (April 2026):** "Meetings that produce artifacts, not just words." Tool use during meetings is the moat — no competitor does this. Pika wins on presentation (avatar/voice), Recall wins on infrastructure (multi-platform), Brainchild wins on capability (MCP tool use, live context, extensibility).

### Open Questions

- **Recall.ai as optional connector?** Recall offers managed meeting bot infrastructure ($0.50/hr) covering Zoom, Meet, Teams, Webex via a single API. Could add `connector: recall` in config.yaml as an alternative to self-hosted connectors — eliminates browser automation, audio routing, and platform maintenance. Tradeoff: proprietary dependency vs. drastically reduced plumbing burden. Hybrid model (self-hosted default, Recall optional) preserves open source spirit. Could skip building Zoom/Teams connectors entirely.

---

## Not On This Plan

- DigitalOcean droplet deployment (preserved in `cloud/`)
- Loadout sharing / registry
- Windows support
- Multi-agent concurrency
