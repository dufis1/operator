# Session 156 handoff (2026-04-23)

**Shipped `brainchild edit` and `brainchild where` subcommands as `01b0868`.** ~45 min of code, ~45 min of architecture conversation, zero new phase — this is a focused quality-of-life add on top of the session 155 live-QA work. The session started with a captions-QA bug ("I said something aloud, the bot didn't know") that traced not to the caption pipeline but to bundled-vs-user config drift: user had flipped `captions_enabled: true` in the bundled `src/brainchild/agents/claude/config.yaml`, but `_ensure_user_agents` is a create-only seed, so the runtime copy at `~/.brainchild/agents/claude/config.yaml` still had `false` and the caption subsystem never wired on boot. Diagnosed by grepping `/tmp/brainchild.log` for caption subsystem lines — total silence → wiring missing, not wiring failed. Fixed the runtime copy for the active session and rewrote the bundled config's stale "off by default" comment to match the true value (that comment edit rode along with session 155's `87255e0`, not a new commit this session).

**Long architecture conversation in the middle** — user challenged whether the two-dir split (`src/brainchild/agents/` bundled vs `~/.brainchild/agents/` user) is overengineered, and whether agents should be repo-proximal instead of per-user. Made the case for both current choices: the split matches standard template-plus-overlay pattern (`/etc/gitconfig` + `~/.gitconfig`, VS Code bundled vs user extensions, `~/.claude/` overlay) and survives the upgrade/read-only-install/multi-user scenarios that a collapsed-to-one-dir design fails; agents are user-scoped because brainchild is a meeting-participant bot whose identity travels with the human, not with whatever repo tab is open (cwd is irrelevant to the Meet it joins). The dev-mode confusion is a real-but-narrow papercut caused by the user being both package author and end user on the same machine.

**Shipped resolution to the discoverability gap the bug surfaced.** `brainchild edit <bot|.env>` opens the runtime config in `$VISUAL`/`$EDITOR`/`vi` (multi-word editors via `shlex.split`); `brainchild where <bot|.env>` prints the absolute path for shell composition (`code $(brainchild where claude)`, `open -R $(brainchild where claude)`). Centralized `_resolve_config_target` gives both subcommands identical error shapes. Accepts `.env` or bare `env` as the env-file token. Wired into the `main()` dispatch, top-of-file docstring, and `_print_usage`. Smoke-tested happy path, missing file, unknown target, and no-arg cases — rc values clean across all four. Nothing left uncommitted; working tree clean except for four `debug/*.html`/`*.png` scratch files that have been untracked since session 151.

**Exact next step: Phase 14.5 `install.sh` (~2h),** still blocked on the GitHub `operator → brainchild` repo rename — ask about it at session start. Alternatives if rename still pending: **14.7 uninstall docs (~10m, README-only)** as the quick-hop, OR session 155's still-open **Issue 3 — text-block-before-tool-call wiring in `pipeline/llm.py:295-308`** (~30–45m, genuine chat-feel improvement; Anthropic's API returns both a text block and a tool_use block in the same response when the model wants to acknowledge before calling, and the harness currently drops the text on the floor instead of sending it first as a "let me check…" buffer).

**Open items / blockers.** (a) GitHub repo rename `operator → brainchild` — still deferred, blocks 14.5. (b) Candidate follow-up offered but not yet taken: append a "for further tweaks, run `brainchild edit <name>`" line to the wizard reveal screen so the new commands self-discover at first config-touch. (c) Larger open architectural gap — `brainchild setup --sync-bundled` (per-key diff of bundled vs user with accept/skip prompts) would propagate bundled-config edits to existing installs and remove the class of bug that triggered this session. Not yet built. (d) Separate gap — dev-mode symlink (`~/.brainchild/agents/<bot>` → `src/brainchild/agents/<bot>`) would let repo edits hit runtime immediately during development; also not yet built, lower priority. (e) Incidental finding — user's `~/.brainchild/.env` doesn't exist on this machine (they run with shell-exported env vars); Phase 14.5 installer will seed an empty one. (f) Deferred-live-smoke carry-forward from prior sessions unchanged (15.7.2 banner, 15.7.3 OAuth, 15.7.4/4.5 wizard + runtime pre-flight, 15.7.5 per-MCP pressure-tests, 15.9 wizard/CLI/hard-fail/format-drift). (g) MVP budget: ~17.5–19.5h remaining (session 156 burned ~45 min). Launch target unchanged — Apr 28–30 aspirational, May 1–2 acceptable. (h) Git identity: commit `01b0868` carries auto-identity `jojo@jojos-MacBook-Pro.local` — consistent with sessions 153–155, flag if you want it normalized.
